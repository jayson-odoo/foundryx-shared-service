# Backlog Register

Deferred work, with linkage back to the plan that spawned it. Add a row when a plan
consciously defers something; close it when a later plan/sprint picks it up.

| ID | Title | Source plan | Priority | Status |
|----|-------|-------------|----------|--------|
| BL-125 | **Move existing job tables onto centralized `background_jobs`** - sprint-4/10 introduces a generic `type`-dispatched `background_jobs` table + worker + unified "Jobs" drawer (storage migration = first type), deliberately leaving `import_jobs`/`document_download_jobs`/`workflow_runs`/`email_outbox` untouched (load-bearing, typed columns). Retrofit them onto the central table (generic `payload_json`/`result_json`) and fold their separate drawers into the one Jobs drawer. Workflow runs (graph snapshot + node rows + replay) are the hardest - may stay separate. | [sprint-4/10-storage-migration](../plans/sprint-4/10-storage-migration.md) | Medium | Open |
| BL-126 | **Scheduled orphan-GC sweep of migration destination bucket** - sprint-4/10 ignores blobs orphaned in bucket B by mid-migration replace/delete (copied then de-referenced; never served, harmless). Add a guarded scheduled task that diffs bucket B contents against live `conn:B:` keys and deletes the unreferenced (must not delete a just-uploaded blob whose key hasn't committed - race-guarded). | [sprint-4/10-storage-migration](../plans/sprint-4/10-storage-migration.md) | Low | Open |
| BL-127 | **Drop legacy `conversation_messages.media_url` column** - after sprint-4/10's one-time `media_url`→`media_key` backfill is verified in prod (all omnichannel media key-based), drop the retained legacy URL column. | [sprint-4/10-storage-migration](../plans/sprint-4/10-storage-migration.md) | Low | Open |
| BL-128 | **Conversation-automation workflow engine + embeddable builder (respond.io-style)** - build ONCE here for all consumers: triggers (message received, conversation opened, tag/assignment) → actions (auto-reply, route/assign, keyword branch, CSW-aware template, drip, wait, webhook), reusing the sibling workflow engine from the EMS strip. Ship an embeddable builder UI (same embed pattern as the inbox). Boundary: this automates the CONVERSATION; a consumer's business orchestration stays in the consumer's engine via the `message.received` webhook (see the embed contract §6). Its own plan. | [sprint-4/11-omnichannel-embed-host](../plans/sprint-4/11-omnichannel-embed-host.md) | High | Open |
| BL-129 | **Promote external-agent → optional real shared-service user (OIDC)** - sprint-4/11 provisions federated `external_agent` identities from signed assertions (attribution only, no login). If consumer agents ever need to log into the shared-service app directly as themselves, add full OIDC SSO (this repo as RP / consumer as IdP) and link the external_agent to a real user row. | [sprint-4/11-omnichannel-embed-host](../plans/sprint-4/11-omnichannel-embed-host.md) | Low | Open |
| BL-130 | **Async/buffered high-volume integration-activity writer** - sprint-4/12 Slice 3 ships a lightweight per-PROCESS synchronous volume guard (`integration_activity_max_writes_per_second`, default 500/s): under a burst beyond the cap a write is DROPPED (logged running counter) rather than blocking. Trade-off: the cap is per-process (N workers ⇒ N×cap aggregate) and best-effort - no sampling/aggregation. The scale path is a `background_jobs`/Celery-backed async buffer (batch-insert, no drops) if synchronous best-effort proves insufficient under real load. | [sprint-4/12-developer-logs-console](../plans/sprint-4/12-developer-logs-console.md) | Low | Open |
| BL-131 | **AutoCount vendor JWT embeds the user password in cleartext** - the `JWTToken` sent on every request base64-decodes to claims including `"Password"`. Our masking covers it (`Authorization`/`jwt_token`/`Password`/`appid` all redact, verified), and that masking stays because the same client will face a real vendor. **Deliberately deprioritised:** the instance this was observed on is V-Soft's public *demo* box, not a counterparty we integrate with - so there is no vendor to escalate to. Re-raise if a production AutoCount deployment exhibits the same token design. Practical standing constraint: no "show raw request" debug surface for this provider. | [sprint-4/13-autocount-esb](../plans/sprint-4/13-autocount-esb.md) | Low | Open |
| BL-132 | **Explicit per-connection TLS policy for ERP connections** - plan §11 requires TLS policy be explicit per connection and never silently downgraded. Today `provider.py` reads a `verifyTls` config key that `fields()` never exposes: unreachable-but-secure, so a customer on a self-signed cert cannot connect and is told no reason why. Add the field + a deliberate opt-in for plain-HTTP/self-signed, made sharper by BL-131 (credentials ride every request). | [sprint-4/13-autocount-esb](../plans/sprint-4/13-autocount-esb.md) | High | Open |
| BL-133 | **Wire the AutoCount consumer sink to Sorento** - slice 1 deliberately stops at an approved canonical record; the sink is a tagged logging no-op, so AC-13-14's "the GRN appears in the consumer" is DEFERRED, not met. Staged records reaching `PUSHED` means "handed to the sink", not "delivered". Wire hop 2 to real Sorento ingest endpoints and make the status distinguish handed-off from delivered. | [sprint-4/13-autocount-esb](../plans/sprint-4/13-autocount-esb.md) | High | Open |
| BL-134 | **`test_url_fetcher_blocks_ssrf` performs live DNS** - `_is_blocked_fetch_url` calls `socket.getaddrinfo()` and treats `gaierror` as "blocked", so `assert not blocked("https://example.com/...")` fails on any transient resolution hiccup and in offline CI. Pre-existing (not from sprint-4/13); observed failing once inside a full-suite run and passing in isolation. Monkeypatch resolution in the test. | [sprint-4/13-autocount-esb](../plans/sprint-4/13-autocount-esb.md) | Medium | Open |
| BL-135 | **`ActivityLogService.record` commits the caller's session** - and rolls it back on failure. Shared house behaviour, not module-specific. Every current call site sits at a transaction boundary so it is safe today, but a future call placed mid-transaction will commit or discard uncommitted work with no warning. Give it its own session, or rename it so the commit is not a surprise. | [sprint-4/13-autocount-esb](../plans/sprint-4/13-autocount-esb.md) | Medium | Open |
| BL-136 | **AutoCount `tax_rate` typed as string while sibling numerics are `Decimal`** - `qty`/`unit_price`/`sub_total`/`tax` map to `Decimal`; `tax_rate` maps via the `string` transform. Possibly deliberate (preserving the vendor representation) but a rate that may later be computed with should be `Decimal` for consistency and safe arithmetic. | [sprint-4/13-autocount-esb](../plans/sprint-4/13-autocount-esb.md) | Low | Open |
| BL-137 | **AutoCount model/migration index-name drift** - migration 0002 names the connection index `ix_ac_company_connection` while `create_all` generates `ix_ac_company_connection_id`, so a create_all-first host ends up with both. Harmless duplication; reconcile the names. | [sprint-4/13-autocount-esb](../plans/sprint-4/13-autocount-esb.md) | Low | Open |
| BL-138 | **`mask_payload` over-masks on the `"pan"` substring** - `_SENSITIVE_KEY_PARTS` includes `"pan"` (card PAN) and matching is literal substring, so `CompanyName`, `panel`, `expand`, `company_name` etc. all redact to `***` in EVERY integration source's stored request/response, not just payments. Fail-safe (over-masks, never leaks) but it quietly degrades observability system-wide - found while making autocount log real payloads, where `CompanyName` came back `***`. Fix by matching PAN on a word boundary / exact key rather than substring, or drop `"pan"` and rely on the existing PAN digit-run masker. Shared core file - needs its own change with a full-suite run, not a drive-by. | [sprint-4/13-autocount-esb](../plans/sprint-4/13-autocount-esb.md) | Medium | Open |
| BL-139 | **`default(value, fb)` can't fill a BLANK source** - `MappingRow.coerce` (`mapping.py`) short-circuits a blank source value to `None` BEFORE evaluating any formula (deliberate: protects the Boolean preset from turning a blank `is_active` into `False`, and keeps "absent ≠ unconvertible" for `is_required`). Side effect: a `default(value, "N/A")` formula never yields the fallback on a blank source - the exact case `default` exists for. `default` still works for null-producing sub-expressions (`default(null, fb)` → `fb`), so it isn't dead, but the operator-facing common case surprises. Foolproof-UI fix: bypass the blank short-circuit only when the formula's ROOT call is `default(...)` (mirrored client-side for sim parity), so `default` handles blanks while every other formula keeps the protective short-circuit. | [sprint-4/16-autocount-mapping-formulas](../plans/sprint-4/16-autocount-mapping-formulas.md) | Low | Open |
| BL-140 | **Formula number→string parity gap at magnitudes ≥1e16** - `formula.py` stringifies numbers with `repr(n)` while `lib/autocount-formula.ts` uses `String(n)`; they diverge for integer-valued numbers ≥1e16 in `&`/`concat` text contexts (Python `"1e+16"` vs JS `"10000000000000000"`). Not in the golden matrix; vanishingly unlikely in AutoCount master data, but the parity invariant is stated as absolute. Pin with a shared stringify helper + a matrix case. | [sprint-4/16-autocount-mapping-formulas](../plans/sprint-4/16-autocount-mapping-formulas.md) | Low | Open |
| BL-001 | **Login rate-limiting / brute-force protection** - no throttle, lockout, or captcha on `/auth/login`. Unlimited password guesses. Needs Redis/slowapi or DB attempt counter. (Security gap #3.) | [sprint-1/01-login-page](../plans/sprint-1/01-login-page.md) | High | **Closed** → [sprint-1/10-auth-hardening](../plans/sprint-1/10-auth-hardening.md) |
| BL-002 | **`rememberMe` honored end-to-end** - currently dead wiring: schema/hook/service/auth-options still carry the field but the rewritten login page renders no checkbox (frozen `false`), and the backend issues a fixed-expiry JWT regardless. Either restore the control + wire long vs short session expiry off the flag, or remove the field end-to-end. (Security gap #4 + review #5.) | [sprint-1/01-login-page](../plans/sprint-1/01-login-page.md) | Medium | **Closed** → [sprint-1/10-auth-hardening](../plans/sprint-1/10-auth-hardening.md) |
| BL-005 | **Distinguish auth-failure from infra-error on login** - NextAuth v4 only surfaces `error='CredentialsSignin'`, so `auth-service.ts` shows "Invalid email or password." for a backend 500 / network outage too. Surface a distinct message for non-credential failures. (Review #4.) | [sprint-1/01-login-page](../plans/sprint-1/01-login-page.md) | Low || **Closed** - [sprint-2/04-account-security](../plans/sprint-2/04-account-security.md) (authorize() infra-vs-creds split, merged 2026-06-06) |
| BL-003 | **Migrate signup / reset-password to Foundryx design** - reuse `AuthBrandPanel` + `AuthFooter` + Foundryx tokens; currently on old Metronic layout. | [sprint-1/01-login-page](../plans/sprint-1/01-login-page.md) | Medium | **Closed** → [sprint-1/10-auth-hardening](../plans/sprint-1/10-auth-hardening.md) |
| BL-004 | **Full multi-tenancy model** - tenant onboarding, tenant resolution (subdomain / email domain / selector), per-tenant RBAC, tenant-scoped queries across all repositories. Sprint 1 only lays groundwork (`tenant_id` on `users` + JWT claim + default tenant); the full SaaS tenancy model is its own epic. See CLAUDE.md → Architecture → Multi-tenancy. | [sprint-1/01-login-page](../plans/sprint-1/01-login-page.md) | High | Open |
| BL-006 | **Production mailer provider** - wire a real SMTP/Resend/SES adapter behind the `EmailService` interface; sprint-1 ships only the dev console-log adapter (invite links printed, not sent). | [sprint-1/02-user-management](../plans/sprint-1/02-user-management.md) | High | **Closed** → [sprint-1/09-integration-core-email](../plans/sprint-1/09-integration-core-email.md) |
| BL-007 | **Avatar upload + blob storage** - currently initials-only (no upload). Needs storage backend (local disk / S3) + file-upload wiring; `users.avatar` already holds a URL. | [sprint-1/02-user-management](../plans/sprint-1/02-user-management.md) | Low | **Planned** → [sprint-2/06-storage-integrations](../plans/sprint-2/06-storage-integrations.md) |
| BL-008 | **Change-email flow + re-verification** - email is read-only after create. Add an admin/self change-email flow that re-verifies the new address. | [sprint-1/02-user-management](../plans/sprint-1/02-user-management.md) | Medium || **Closed** - [sprint-2/04-account-security](../plans/sprint-2/04-account-security.md) (dual-confirmation ceremony + admin instant path, merged 2026-06-06) |
| BL-009 | **RBAC / permissions per role** - `roles` + `user_roles` ship as identity only (name + membership). Add permission definitions, role→permission mapping, and enforcement. | [sprint-1/02-user-management](../plans/sprint-1/02-user-management.md) | High | **Closed** → [sprint-1/03-roles-permissions](../plans/sprint-1/03-roles-permissions.md) |
| BL-013 | **App-Store installer → `sync_permissions` lifecycle wiring** - `sync_permissions(csv_rows, module_name)` ships parametrized + run for core at bootstrap; wire it into the installer's module install/update (upsert) and uninstall (`DELETE WHERE module=X` + cascade grants) hooks. | [sprint-1/03-roles-permissions](../plans/sprint-1/03-roles-permissions.md) | Medium | **Closed** → [sprint-1/08-app-store](../plans/sprint-1/08-app-store.md) (module `install()` syncs its CSV; catalog rows are global - grants revoked per tenant on uninstall) |
| BL-014 | **Sidebar / nav-item visibility gated by `can(key)`** - route + action gating ships in sprint-1/03; prune menu items the user lacks `<resource>.read` for so the nav reflects effective permissions. | [sprint-1/03-roles-permissions](../plans/sprint-1/03-roles-permissions.md) | Low || **Closed** - core-item pruning landed via [sprint-2/05-datetime-hygiene](../plans/sprint-2/05-datetime-hygiene.md) (`MenuItem.permission` + recursive `lib/menu-filter.ts`); module items landed via plan 08 |
| BL-015 | **Populate association `tenant_id` on assignment** - `user_roles.tenant_id` (and `role_permissions.tenant_id`) are written via the column default `DEFAULT_TENANT_ID`, not the real tenant, because assignments go through the ORM relationship (`user.roles.append(...)`) which only sets the FK columns. Harmless under the single-tenant MVP (everything is the default tenant) and routed-around in `user_counts` (counts by `role_id`, which is tenant-scoped), but the column is wrong the moment a 2nd tenant onboards. Set `tenant_id` explicitly on assignment (association object / `before_insert`), then any query may trust it. Pairs with BL-004. | [sprint-1/03-roles-permissions](../plans/sprint-1/03-roles-permissions.md) | Medium || **Closed** - already fixed: `tenant_id_from_role` default derives tenant from owning role (`models/permission.py`); status was stale |
| BL-010 | **List power features** - saved filters + OR/group filter presets UI, XLSX export, permanent-delete from trash. Sprint-1 ships the AND/OR builder, CSV export, and soft-trash/restore only. | [sprint-1/02-user-management](../plans/sprint-1/02-user-management.md) | Low | Open |
| BL-011 | **Keyset pagination** - switch from `LIMIT/OFFSET` to keyset if any list table grows large enough for deep-offset to hurt. | [sprint-1/02-user-management](../plans/sprint-1/02-user-management.md) | Low | Open |
| BL-012 | **UTC datetime end-to-end** - enforce the "store UTC, render in user tz" rule (CLAUDE.md → Datetime & timezone). Current gap: columns are `DateTime(timezone=False)` with naive UTC + `func.now()` (server-tz dependent), and the API serializes without a `Z`/offset, so the frontend `Intl` formatting parses them as **local** time. Fix: store tz-aware UTC (or guarantee server UTC), serialize ISO-8601 with `Z`, parse-as-UTC on the frontend and format with the authenticated user's timezone. Audit all datetime columns (`created_at`, `last_sign_in_at`, etc.). | [sprint-1/02-user-management](../plans/sprint-1/02-user-management.md) | High || **Closed** - landed via [sprint-2/05-datetime-hygiene](../plans/sprint-2/05-datetime-hygiene.md): `UTCDateTime` timestamptz columns (aware-UTC reads on every engine), Z-suffixed wire (`ApiModel`), frontend `lib/datetime.ts` + `useDatetime` renders in the user's tz (`users.timezone` preference, My Account picker) |
| BL-016 | **App-Store installer wiring for `omnichannel`** - auto-run the module's Alembic upgrade + `sync_permissions("omnichannel", rows)` on install/update, and drop schema + version table + cascade grants on uninstall. Until built, `omnichannel` is installed manually. Depends on / extends BL-013. | [sprint-1/04-omnichannel-foundation](../plans/sprint-1/04-omnichannel-foundation.md) | Medium | **Closed** → [sprint-1/08-app-store](../plans/sprint-1/08-app-store.md) (manifest-driven loader + per-tenant lifecycle; schema DDL stays `create_all` - BL-029 still open) |
| BL-017 | **Meta App Review + Business Verification** (ops, not code) - get Foundryx's Meta app `whatsapp_business_management` + `whatsapp_business_messaging` Advanced Access + Business Verification so Embedded Signup works for arbitrary tenants beyond Dev Mode (owner/tester numbers). | [sprint-1/04-omnichannel-foundation](../plans/sprint-1/04-omnichannel-foundation.md) | High | Open |
| BL-018 | **Additional channel adapters** - Messenger / Instagram / Douyin / Xiaohongshu behind the `ChannelAdapter` interface (MVP = WhatsApp Cloud only). | [sprint-1/04-omnichannel-foundation](../plans/sprint-1/04-omnichannel-foundation.md) | Low | Open |
| BL-019 | **Multi-workspace switcher UI** - MVP auto-creates one default workspace per tenant; add full multi-workspace management + switcher. Pairs with BL-004. | [sprint-1/04-omnichannel-foundation](../plans/sprint-1/04-omnichannel-foundation.md) | Low | Open |
| BL-020 | **Full WhatsApp template management** - in-app authoring + submission to Meta + approval-status tracking. MVP only mirrors approved templates read-only. | [sprint-1/05-omnichannel-message-processing](../plans/sprint-1/05-omnichannel-message-processing.md) | Medium | Open |
| BL-021 | **Auto-assignment / smart routing** - round-robin + content/time-based routing for conversations; belongs to the deferred Rule engine. MVP = manual assign + unassigned queue + self-claim. | [sprint-1/05-omnichannel-message-processing](../plans/sprint-1/05-omnichannel-message-processing.md) | Medium | Open |
| BL-022 | **WebSocket scale-out hardening** - presence, reconnect/backfill on socket drop, message backfill since last_seen. | [sprint-1/05-omnichannel-message-processing](../plans/sprint-1/05-omnichannel-message-processing.md) | Low | Open |
| BL-023 | **Interactive message compose** - outbound INTERACTIVE (buttons/lists) composer; MVP renders inbound interactive but compose is text/media/template only. | [sprint-1/05-omnichannel-message-processing](../plans/sprint-1/05-omnichannel-message-processing.md) | Low | Open |
| BL-024 | **Core Template engine** (merge-block rendering) - EMS-wide foundation; BSP consumes for auto-replies/surveys. | [sprint-1/06-omnichannel-engine-integration](../plans/sprint-1/06-omnichannel-engine-integration.md) | High | **Closed** → [sprint-2/07-template-engine](../plans/sprint-2/07-template-engine.md) (email surface, merged) |
| BL-025 | **Core Workflow engine** (trigger→action) - must support omnichannel triggers (`onNewMessageReceived`, `onCSWExpired`, `onThreadClosed`) + actions (`SendChannelMessage`, `AssignContact`, `SetPriority`, `TransitionThread`) - added by an omnichannel module update registering its TriggerDefs/ActionDefs (engine extends via the code-side registry, no core change). | [sprint-1/06-omnichannel-engine-integration](../plans/sprint-1/06-omnichannel-engine-integration.md) | High | **Done** - shipped as 3 vertical slices: [sprint-2/08-workflow-engine](../plans/sprint-2/08-workflow-engine.md) (foundation), [sprint-2/09-workflow-triggers-actions](../plans/sprint-2/09-workflow-triggers-actions.md) (breadth), [sprint-2/10-workflow-integration-polish](../plans/sprint-2/10-workflow-integration-polish.md) (polish - run retention, audit-log seam, IF-branch debug staleness; **closes BL-025**). Omnichannel trigger/action registration = the module's own update. |
| BL-026 | **Core Rule engine** (condition→effect) - content/time conditions driving routing + priority side effects. | [sprint-1/06-omnichannel-engine-integration](../plans/sprint-1/06-omnichannel-engine-integration.md) | High | Open |
| BL-027 | **Core Status & State-Machine engine** - configurable transitions over the `statuses` table (Plan 01 ships it static). | [sprint-1/06-omnichannel-engine-integration](../plans/sprint-1/06-omnichannel-engine-integration.md) | Medium | **Closed** → [sprint-2/01-status-engine](../plans/sprint-2/01-status-engine.md) |
| BL-028 | **Omnichannel ↔ engine wiring** - add the §2.1 emit-points (event bus) + register the §2.2 actions once the core engines exist. Plan 02 deliberately emits no events yet. | [sprint-1/06-omnichannel-engine-integration](../plans/sprint-1/06-omnichannel-engine-integration.md) | Medium | Open |
| BL-029 | **Per-module Alembic history for `omnichannel`** - Phase B creates the `app_omnichannel` schema + tables via `OmniBase.metadata.create_all` (idempotent, dev-friendly) instead of the plan's per-module Alembic (`alembic_version_omnichannel`). Add a module Alembic chain so prod migrations + clean uninstall (drop schema + version table) are governed. Pairs with BL-016. | [sprint-1/04-omnichannel-foundation](../plans/sprint-1/04-omnichannel-foundation.md) | Medium | Open |
| BL-030 | **DB-level cross-schema FKs into core** - module tables store `tenant_id`/`user_id`/`assigned_user_id`/`created_by` as plain indexed columns (no FK) because `create_all` can't resolve cross-`MetaData` FKs into core `public` tables; referential integrity is enforced app-side by tenant-scoped repos. Add real cross-schema FKs (e.g. `app_omnichannel.* → public.users(id)`) once the module-Alembic/installer story matures. | [sprint-1/04-omnichannel-foundation](../plans/sprint-1/04-omnichannel-foundation.md) | Low | Open |
| BL-031 | **Set `OMNICHANNEL_FERNET_KEY` in prod** - channel `credentials_json` is Fernet-encrypted; with no key set the module generates an ephemeral per-process key (a restart invalidates stored ciphertext). Provision a stable key in prod/on-prem env. | [sprint-1/04-omnichannel-foundation](../plans/sprint-1/04-omnichannel-foundation.md) | High | Open |
| BL-032 | **Public tenant self-signup** - stranger creates tenant + first admin via public `/signup`; calls `TenantProvisioningService` (plan 07). Needs email verification, abuse/rate protection, slug-collision UX, onboarding wizard. Spun out of BL-004. | [sprint-1/07-tenant-core](../plans/sprint-1/07-tenant-core.md) | High | Open |
| BL-033 | **First-admin invite email + force-password-change** - plan 07 ships operator-set temp password handed out-of-band. Replace with invite-token email (reuse `EmailService`; pairs BL-006) + forced password change on first login. | [sprint-1/07-tenant-core](../plans/sprint-1/07-tenant-core.md) | Medium | Open |
| BL-034 | **Custom domain support** - `tenants.custom_domain` column ships in plan 07; wire CNAME verification + infra routing (wildcard certs / per-domain TLS) so a tenant can use `app.acme.com`. | [sprint-1/07-tenant-core](../plans/sprint-1/07-tenant-core.md) | Low | Open |
| BL-035 | **Tenant hard-delete / GDPR purge** - archived keeps data forever; add an explicit operator purge action (typed confirm) that wipes the tenant's `public` rows + calls every module's `uninstall_tenant` purge hook. | [sprint-1/07-tenant-core](../plans/sprint-1/07-tenant-core.md) | Medium | **Closed** → [sprint-2/02-rule-engine](../plans/sprint-2/02-rule-engine.md) (archived-only purge, typed slug confirm, module hooks + core cascade; bulk delete in console) |
| BL-036 | **Per-tenant module entitlements + billing** - chargeable installs/upgrades. Attach billing/entitlement checks to the App Store install/update actions + the `installed_version` feature gating (plan 08 D4). | [sprint-1/08-app-store](../plans/sprint-1/08-app-store.md) | High | Open |
| BL-037 | **Omnichannel `statuses` → core engine migration** - the management UI shipped with sprint-2/01 (BL-027); remaining scope = migrate omnichannel's per-tenant `statuses` (3 entity_types) onto the core engine: register them in `app/status_engine/registry.py` at module install, remap `app_omnichannel` status FKs, retire the module-local table. | [sprint-1/07-tenant-core](../plans/sprint-1/07-tenant-core.md) | Medium | Open |
| BL-038 | **Email-template management + per-tenant email branding** - user-editable email templates (subject/body, merge fields) + tenant logo/colors in mail. Plan 09 ships fixed Jinja2 Foundryx-branded templates only. Ties BL-024 (core Template engine). | [sprint-1/09-integration-core-email](../plans/sprint-1/09-integration-core-email.md) | Medium | **Closed** → [sprint-2/07-template-engine](../plans/sprint-2/07-template-engine.md) (email surface, merged) |
| BL-039 | **Per-tenant security policy page** - tenant-configurable token TTLs (reset/invite), session lengths, password policy, throttle thresholds (bounds-clamped + audited). Plan 10 ships env-level config only. Pairs plan 07. | [sprint-1/10-auth-hardening](../plans/sprint-1/10-auth-hardening.md) | Low | Open |
| BL-040 | **Adopt Redis when scale demands** - pub/sub for websocket scale-out (BL-022), hot caching, Redis impls of `ThrottleStore`/queue stores. Deliberately NOT introduced for plan 09/10 (low-QPS auth/email; on-prem stays one-service). | [sprint-1/09-integration-core-email](../plans/sprint-1/09-integration-core-email.md) | Low | Open |
| BL-041 | **Real captcha on public auth endpoints** - backend-verified captcha (login/forgot-password under sustained attack). Plan 10 strips the Metronic reCAPTCHA stub (site key unset, token never verified server-side = theater). | [sprint-1/10-auth-hardening](../plans/sprint-1/10-auth-hardening.md) | Low | Open |
| BL-042 | **Migrate omnichannel secrets onto core `FERNET_KEY`** - re-encryption pass moving channel `credentials_json` off `OMNICHANNEL_FERNET_KEY` onto the core secrets helper (plan 09); retires the module-specific key. Pairs BL-031. | [sprint-1/09-integration-core-email](../plans/sprint-1/09-integration-core-email.md) | Medium | Open |
| BL-043 | **Multiple connections per provider per tenant** - e.g. marketing vs transactional SMTP; needs purpose-routing at send time. Plan 09 enforces UNIQUE(tenant, provider). | [sprint-1/09-integration-core-email](../plans/sprint-1/09-integration-core-email.md) | Low | Open |
| BL-044 | **Email log page** - Resource-shell list over `email_outbox` (status, attempts, errors), manual retry, per-tenant view. Plan 09 ships DB + logs only (wizard test-send covers setup UX). | [sprint-1/09-integration-core-email](../plans/sprint-1/09-integration-core-email.md) | Low | Open |
| BL-045 | **Converge omnichannel channel connect onto the integrations Resource form flow** - plan sprint-2/06 retires the `integration-connect-wizard` (integrations = ResourceList + ResourceForm, D6); omnichannel's channel-connect wizard should converge on the same list+form design language instead of a wizard shell (original wording targeted the now-deleted wizard). | [sprint-1/09-integration-core-email](../plans/sprint-1/09-integration-core-email.md) | Low | Open |
| BL-046 | **API-native mail adapters** - Resend REST / SES SDK providers in the integration registry (better deliverability feedback than raw SMTP). Generic SMTP adapter ships in plan 09. | [sprint-1/09-integration-core-email](../plans/sprint-1/09-integration-core-email.md) | Low | Open |
| BL-047 | **Operator cross-tenant impersonation ("login as tenant admin")** - extend the impersonation engine (sprint-1/03) so a platform-tenant admin holding `users.impersonate` can impersonate a user in ANY tenant for support/troubleshooting: relax the same-tenant rule for platform actors only, cross-tenant target lookup, audited `impersonation_sessions` row, amber banner, no-escalation rule re-evaluated for the operator grant. Until then the console ships an "Open workspace" link (subdomain URL) and operators use known credentials. | [sprint-1/07-tenant-core](../plans/sprint-1/07-tenant-core.md) | Medium | Open |
| BL-048 | **Tenant-core review fast-follows (findings 6-10)** - (a) shared `visibleMenu()` filter applied at every `MENU_SIDEBAR` consumer (breadcrumb/toolbar/other layouts still render platformOnly items); (b) `X-Tenant-Slug` header: backend cross-check vs JWT tenant or remove (currently dead defense); (c) conftest should call `seed_all` + test-only extras instead of hand-replaying the seed sequence; (d) move the tenant-Admin grant rule out of `app/seed.py` into PermissionService/RoleService (omnichannel bootstrap imports core seed - layering) - plan 08 review widened this: the "which modules' keys may this tenant hold/see" policy now lives in THREE places (`seed.tenant_admin_grant`, `AppStoreService._grant_admin`, `RoleService._grantable` + `PermissionService.catalog` filter) - consolidate into one grant/visibility policy helper; also: module card icons are a hardcoded registry in core `module-card.tsx` (third-party module icon strategy belongs with BL-049 packaging); (e) consolidate duplicated `listParams`/`_parse_filter`/`csvEscape`/paginate-export scaffolds + replace e2e `waitForTimeout(800)` with `waitForResponse`. | [sprint-1/07-tenant-core](../plans/sprint-1/07-tenant-core.md) | Low | Open |
| BL-049 | **Module package export/import + certification pipeline v1** - operator-level (platform console) zip export of a module dir + import wizard with staged validation and a user-facing Certification Report (blockers disable Import, warnings don't; report persisted + downloadable). Import criteria: archive checksum/signature, manifest schema (`module_name`/semver/`required_core_version`/routers/CSV/display fields), core-version compatibility, install-vs-update + no-downgrade rule, folder structure + 4 bootstrap contract hooks, DB sandbox static scan (`app_<module>` schema only, no `DROP/ALTER/TRUNCATE` on `public`), D5 migration discipline on update, permissions-CSV parse + resource collision check, code hygiene (no raw SQL in routers, no own auth, no core injection, dependency allowlist), frontend rules (no global CSS, error boundary). Constraint: Next.js can't load uncompiled React at runtime - import triggers rebuild/redeploy or module frontends ship as precompiled bundles; package import is deployment-level (D4), tenants still install per-tenant via the store. Per-tenant DATA export/import (tenant migration) is explicitly out of scope - separate item. Depends on BL-029 (per-module Alembic). | [sprint-1/08-app-store](../plans/sprint-1/08-app-store.md) | Medium | Open |
| BL-050 | **Webhook channel routing via `phone_number_id`, not URL path** - Meta Cloud API allows ONE app-level callback URL, but onboarding registers a per-channel `/omnichannel/webhooks/{channel_id}` path; a second WABA/channel on the same app breaks (events hit one URL). Resolve the channel from the payload's `value.metadata.phone_number_id` inside the worker instead of trusting the path segment. Also closes the spoofing vector (guessable channel_id in URL = forged attribution). | [sprint-1/05-omnichannel-message-processing](../plans/sprint-1/05-omnichannel-message-processing.md) | High | Open |
| BL-051 | **Authenticated/signed media URLs** - `/omnichannel/media/*` is mounted public (no auth, no tenant scope) so a leaked/guessed path returns another tenant's WhatsApp media. Move to short-lived signed URLs (HMAC + expiry) or an authenticated proxy that checks tenant + `conversations.read`. Path-traversal already hardened (`is_relative_to`). | [sprint-1/05-omnichannel-message-processing](../plans/sprint-1/05-omnichannel-message-processing.md) | High | Open |
| BL-052 | **Template sync off the request path** - `GET /channels/{id}/templates` syncs from Meta Graph synchronously on every call (drawer open / channel switch); a slow Meta API stalls the handler. Serve from the mirrored `whatsapp_templates` rows and move `_sync_templates` to a webhook (`message_template_status_update`) + periodic worker. | [sprint-1/05-omnichannel-message-processing](../plans/sprint-1/05-omnichannel-message-processing.md) | Medium | Open |
| BL-053 | **Thread-list pagination / infinite scroll** - inbox `listThreads` sends a fixed `pageSize=200` (interim cap) and drops `total`; a workspace with >200 threads can't reach older threads from the UI. Add cursor/infinite-scroll over `last_message_at`. | [sprint-1/05-omnichannel-message-processing](../plans/sprint-1/05-omnichannel-message-processing.md) | Medium | Open |
| BL-054 | **Inbox query efficiency** - (a) `unread_counts_for` loads every CONTACT message row for the page then counts in Python - use a `GROUP BY` with `created_at > agent_last_read_at`; (b) `find_by_phone_in_workspace` loads all workspace contacts per inbound webhook - store an indexed digits-only phone column and do an indexed lookup; (c) filtered `useConversations` refetches the whole list on every socket event - debounce/coalesce or evaluate the filter predicate client-side. | [sprint-1/05-omnichannel-message-processing](../plans/sprint-1/05-omnichannel-message-processing.md) | Medium | Open |
| BL-055 | **Omnichannel shared-helper cleanup** - (a) promote `_digits()` to a module util (phone-normalization idiom copy-pasted across 5 backend files - a missed site silently breaks contact stitching); (b) one `initials()` (3 copies: drawer, thread-list, lib/format) with consistent fallback; (c) extract `ConversationService.build_reply_ref(quoted)` (reply_to metadata built identically in message_service + inbound_service, both reaching into private `_user_names`); (d) WS `is_active(..., "omnichannel")` and the module-active re-check hardcode the module name inside the module - derive from the manifest. Root cause of (d): `"public": true` routers lose the centralized `require_module` gate. | [sprint-1/05-omnichannel-message-processing](../plans/sprint-1/05-omnichannel-message-processing.md) | Low | Open |
| BL-056 | **WS auth ticket instead of token-in-query** - the WS handshake passes the full 24h JWT as `?token=`, which lands in proxy/LB access logs and browser history (replayable Bearer). Issue a short-lived single-use WS ticket (or use `Sec-WebSocket-Protocol`) and keep the long-lived token out of URLs. | [sprint-1/05-omnichannel-message-processing](../plans/sprint-1/05-omnichannel-message-processing.md) | Medium | Open |
| BL-057 | **`User.status` enum → status engine adoption** - users still carry a hardcoded `UserStatus` string enum; register a `user` entity in the status-engine registry, add `status_id` FK, migrate ACTIVE/INACTIVE/INVITED + trashed semantics onto trait flags, retire the enum. | [sprint-2/01-status-engine](../plans/sprint-2/01-status-engine.md) | Medium | Open |
| BL-058 | **In-app notification inbox (`IN_APP` channel dispatch + bell UI)** - `notification_specs.channel = IN_APP` is modeled but inert (logged no-op). Add a `notifications` table, dispatch on transition, bell/unread UI, mark-read. | [sprint-2/01-status-engine](../plans/sprint-2/01-status-engine.md) | Medium | Open |
| BL-059 | **Graph-driven action buttons in consoles** - the backend exposes `GET /platform/tenants/{id}/transitions` (outgoing edges the actor may fire, label = button text) + a generic `POST …/transition`, but the tenants console still renders the hardcoded Suspend/Reactivate/Archive actions (they DO route through the machine server-side). Render the action menu from the edge list so operator-added statuses/edges get buttons without code. | [sprint-2/01-status-engine](../plans/sprint-2/01-status-engine.md) | Medium | **Closed** → same plan (post-review iteration) |
| BL-060 | **Status-engine fast-follows** - (a) tenant-status filter options in the tenants list are still the 3 hardcoded lifecycle values (custom statuses aren't filterable); (b) notification specs are inline-managed per edge - surface a reusable spec library once sharing across edges is wanted; (c) platform-default edges can't carry role restrictions (roles are tenant-scoped) - decide a platform-role story if operator-tier gating is needed; (d) `statuses.category` is now a cosmetic uppercase-key mirror kept for wire/filter compat - remove once tenant list/export wire moves to key+label. | [sprint-2/01-status-engine](../plans/sprint-2/01-status-engine.md) | Low | Open |
| BL-061 | **E2E suite shared-state hardening (plan-10 fallout)** - (a) `password-reset-live.spec.ts` throttle test IP-locks `127.0.0.1`, breaking every parallel spec's logins mid-run (needs serial project or per-spec source-IP isolation); (b) the spec hard-requires a local `aiosmtpd` maildir daemon - wire it into the E2E environment setup so the suite is one-command again. | [sprint-1/10-auth-hardening](../plans/sprint-1/10-auth-hardening.md) | Medium | Open |
| BL-062 | **Searchable dropdowns everywhere (UX mandate)** - every dropdown over a data-driven list gets search (`SearchSelect` combobox / `MultiSelect` with select-all); sweep pre-existing plain Selects across the system (user form role picker OK, check channel wizard, integrations wizard, omnichannel filters, workspace member pickers, …). New `components/platform/search-select` is the building block; fixed 2-3-option micro-enums may stay plain. | [sprint-2/01-status-engine](../plans/sprint-2/01-status-engine.md) | Medium | Open |
| BL-063 | **Status-engine review deferrals (perf + cleanup)** - (a) `/status-entities` stats + `get_graph` record counts load full row sets / N count-queries - switch to COUNT(*) and batch; (b) `resolve_tier` re-queried 2-3× per request - thread the resolved tier into repo calls (transition repo already takes it); (c) reorder endpoint re-runs the whole graph read (incl. counts) to echo a response the hook discards - return the ordered rows; (d) `_fork` id/edge maps ride hidden service attrs - thread explicitly; (e) Tidy fires N per-node position PATCHes - add a batch position endpoint; (f) notification dispatch resolves recipients with per-row queries on the transition hot path - batch by id/type; (g) tenant actions graph + status entities fetched uncached per mount - adopt react-query caching; (h) consolidate csv escape/download + client list-query helpers (extends BL-048e). | [sprint-2/01-status-engine](../plans/sprint-2/01-status-engine.md) | Low | Open |
| BL-064 | **FlowCanvas undo/redo + non-destructive Tidy** - canvas position changes (drags AND the Tidy auto-layout) persist immediately on writable tiers; Cancel/no-Save doesn't restore the previous arrangement, so one stray Tidy permanently discards a hand-made layout. Add client-side undo/redo (position history) and make Tidy a previewable/undoable action - positions commit on Save (or explicit confirm), not on every change. Applies to every FlowCanvas consumer (status engine now, Workflow engine BL-025 later). Found during sprint-2/02 frontend iteration. | [sprint-2/01-status-engine](../plans/sprint-2/01-status-engine.md) | Medium | **Done** - shared `useHistory` hook in `components/platform/flow-canvas/`; built into the workflow canvas in [sprint-2/08-workflow-engine](../plans/sprint-2/08-workflow-engine.md), retrofitted to the status canvas (positions = client draft, previewable Tidy, **Save layout**) + closed in [sprint-2/10-workflow-integration-polish](../plans/sprint-2/10-workflow-integration-polish.md) (D3) |
| BL-065 | **Platform-tier edge uniqueness not DB-enforced** - `uq_transition_edge(tenant_id, from_status_id, to_status_id)` is non-binding for `tenant_id NULL` rows (Postgres NULLS DISTINCT default): a duplicate platform-default edge slipped in via manual canvas testing and shadowed a rule-engine condition (found in sprint-2/02 E2E). Service-level dup check exists but isn't race/bypass-proof. Fix: partial unique index on (entity_type, from, to) WHERE tenant_id IS NULL (or `NULLS NOT DISTINCT` on PG15+). Also dedupe-on-migrate. | [sprint-2/02-rule-engine](../plans/sprint-2/02-rule-engine.md) | Medium | Open |
| BL-066 | **Per-tenant email branding adoption** - email templates render the tenant's uploaded logo/colors from `tenant_branding` (plan sprint-2/03 assets) instead of fixed Foundryx branding; overlaps the template side of BL-038/BL-024. | [sprint-2/03-tenant-branding](../plans/sprint-2/03-tenant-branding.md) | Medium | **Closed** → [sprint-2/07-template-engine](../plans/sprint-2/07-template-engine.md) (email surface, merged) |
| BL-067 | **SVG upload sanitization** - branding accepts SVG logos/illustrations served via `<img>` (scripts inert there), but defense-in-depth wants server-side SVG sanitization (strip scripts/foreignObject/event attrs) before storing, in case an asset URL is ever opened directly or embedded inline. | [sprint-2/03-tenant-branding](../plans/sprint-2/03-tenant-branding.md) | Low | Open |
| BL-068 | **Branding on custom domains** - `tenants.custom_domain` exists but branding resolution is slug-subdomain only; resolve branding (and tenant login) for custom domains end-to-end. | [sprint-2/03-tenant-branding](../plans/sprint-2/03-tenant-branding.md) | Low | Open |
| BL-069 | **E2E self-cleaning fixtures** - extends BL-061: (a) an E2E teardown step purges `e2e-%` tenants via `TenantService.purge` (each full-suite run leaves ~10 tenants; page-1 assertions in `tenants.spec.ts` break once seeded rows are crowded off); (b) omnichannel demo-thread CSW windows expire by wall-clock because `seed_demo_conversations` is idempotent-skip - refresh `csw_expires_at` on every seed run (or in global-setup) so `inbox.spec.ts` doesn't rot. | [sprint-2/04-account-security](../plans/sprint-2/04-account-security.md) | Medium | Open |
| BL-070 | **Generalize single-use-token + redeem primitives (review deferral)** - three parallel implementations exist after plan 04: (a) backend `invite_tokens` vs `email_change_requests` both hand-roll single-use/expiry/invalidate-outstanding semantics - extract a shared token primitive before a third ceremony copies again; (b) frontend token-redeem surfaces duplicate the dead-link/success/processing state machine (`change-password` page vs `email-change-redeem`) - fold into one `<TokenRedeemPage>` on the auth DX (carries the explicit-click anti-prefetch invariant); (c) the throttle-around-credential-check triad (enforce → record fail) is copy-pasted between `/auth/login` and `/me/change-email` - make it a FastAPI dependency so policy changes can't drift. | [sprint-2/04-account-security](../plans/sprint-2/04-account-security.md) | Low | Open |
| BL-071 | **Badge/ticket fixed-canvas designer + PDF** - second template-engine surface: Konva/Fabric fixed-dimension canvas editor (x/y placement, nudge, alignment guides, per-element fact binding + rule-engine visibility - Webex Events reference) → SVG → fixed-size HTML → WeasyPrint PDF; batch render (Canva Bulk-Create model); Zebra direct-print later. Rides plan-07's template store/merge/conditions foundation. Needs attendee/event entities to bind. **CLOSED = F2 slice 2 MERGED** (Konva editor + canvas-doc polymorphic on the Template row + absolute-HTML→WeasyPrint render + segno QR + `render_canvas_batch` Celery seam + `badge.preview` sample context + "Attendee badge" starter; render REVISED from SVG to absolute-HTML+CSS for native text wrap). Real attendee/event binding = Cluster H. | [sprint-3/03-multi-format-render](../plans/sprint-3/03-multi-format-render.md) | High | Closed |
| BL-072 | **Repeater/loop block** - template-engine block iterating a list fact (invoice line items, agenda rows); needed for invoice mails/PDFs (project plan §5.1). Merge renderer stays substitution-only; repetition is structural (block-level), not string templating. **Closed = F2 slice 1** (table block + free-layout repeater, merge-only rows; merged `f0b0562`). | [sprint-3/03-multi-format-render](../plans/sprint-3/03-multi-format-render.md) | Medium | **Closed** |
| BL-073 | **Template draft/versioning + audit** - plan 07 ships edit-live (preview + test-send as the safety net); add staged draft→publish, version history, and restore. | [sprint-2/07-template-engine](../plans/sprint-2/07-template-engine.md) | Low | Open |
| BL-074 | **Marketing unsubscribe/compliance machinery** - List-Unsubscribe headers, suppression list, unsubscribe landing page; sender-address/footer compliance fields live in `tenant_branding` (plan 07 D4) - the machinery that honors them. Required before any campaign/bulk-send feature. | [sprint-2/07-template-engine](../plans/sprint-2/07-template-engine.md) | Medium | Open |
| BL-075 | **Saved-blocks library** - tenant-reusable composed blocks in the email editor (research: the no-code composition tier every structured builder grows); save selection as named block, insert across templates. | [sprint-2/07-template-engine](../plans/sprint-2/07-template-engine.md) | Low | Open |
| BL-076 | **Website builder (CMS) plan** - own future plan, not a backlog-sized item: Puck (MIT, JSON→own React components) = research front-runner (Craft.js downgraded - bus-factor/maintenance); event blocks (registration form, live agenda, speakers, tickets, countdown), subdomain publishing (project plan §2.4). Consumes plan-07 schema philosophy + brand/fact seams; blocked on Projects/Events entities existing. See `documentation/research/template-engine-builder-landscape.md`. | [sprint-2/07-template-engine](../plans/sprint-2/07-template-engine.md) | High | Open |
| BL-077 | **Asset re-upload migration on storage-connection switch** - plan sprint-2/06 D1: keys carry `conn:<id>:` prefixes so old blobs stay resolvable through the connection that wrote them, but switching connections strands prior assets in the old bucket. Add a migration job: re-upload all of a tenant's stored keys through the new connection + rewrite key prefixes, then allow disconnecting the old one. | [sprint-2/06-storage-integrations](../plans/sprint-2/06-storage-integrations.md) | Low | Open |
| BL-078 | **Omnichannel key-based media resolution** - `TenantStorage.put` persists URLs, so connection-backed media is CDN-only (non-CDN buckets fall back to local disk - presigned URLs expire and must never be persisted, plan-06 review). Move omnichannel `media_url` to stored KEYS resolved per request (avatar/branding pattern) so any S3/R2 bucket works without a CDN; extends BL-077's key migration. | [sprint-2/06-storage-integrations](../plans/sprint-2/06-storage-integrations.md) | Medium | Open |
| BL-079 | **Integrations provider picker: pre-disable connected types** - one-per-type (D7) is enforced at service+DB but the create form lets the user fill a whole R2 form before the 409 toast discards their work. Disable/annotate exhausted types in the provider picker (the picker already groups by type). From the plan-06 code review. | [sprint-2/06-storage-integrations](../plans/sprint-2/06-storage-integrations.md) | Low | Open |
| BL-080 | **Plan-06 review cleanup sweep** - (a) delete `integration-service.mock.ts` + its self-test (dead Phase-A scaffolding, no other consumer); (b) dedupe `ConnectionRepository.resolve_for_type`/`resolve_for_send` into one `_resolve(getter, …)`; (c) extract shared e2e `helpers/tenant.ts` (provisionTenant/loginTenantAdmin copied in 6+ specs); (d) shared frontend `buildListParams` (5+ per-service copies) + shared backend CSV writer (3 copies); (e) `_delete_blob` best-effort helper shared by avatar/branding services; (f) FormRow label `htmlFor` wiring (specs fall back to input-type selectors); (g) required+advanced provider fields would error invisibly behind the collapsed Advanced section (latent - no current provider has one). | [sprint-2/06-storage-integrations](../plans/sprint-2/06-storage-integrations.md) | Low | Open |
| BL-081 | **Notification-spec template picker UI** - the status-engine TransitionDrawer notification editor still authors inline subject/body; the backend already accepts `templateId` (D10, render-through-engine path tested). Add a SearchSelect over `GET /templates?context=` so an operator can point a transition notification at an engine template instead of inline text. | [sprint-2/07-template-engine](../plans/sprint-2/07-template-engine.md) | Medium | **Done** - `GET /templates?context=` filter + `listTemplateOptions`; "Email template" SearchSelect in the TransitionDrawer (inline subject/body kept as fallback) in [sprint-2/10-workflow-integration-polish](../plans/sprint-2/10-workflow-integration-polish.md) (D2) |
| BL-082 | **Rich-text formatting on heading blocks** - Text blocks have the WYSIWYG toolbar; headings stay inline plain-text. Add inline bold/link to headings if organizers want it (low demand - headings are usually plain). | [sprint-2/07-template-engine](../plans/sprint-2/07-template-engine.md) | Low | Open |
| BL-083 | **Canvas Text block client sanitizer hardening** - `lib/sanitize-html.ts` is a minimal render-time net (strips script/style/iframe/on*/javascript:-data: URLs) atop the nh3 save gate. If the editor ever renders untrusted HTML from a new source, adopt a vetted sanitizer (DOMPurify) instead of the hand-rolled walk. From the plan-07 review (#2). | [sprint-2/07-template-engine](../plans/sprint-2/07-template-engine.md) | Low | Open |
| BL-085 | **Guided product tour / onboarding** - the foolproof-UI mandate (CLAUDE.md → Design system) bans inline instructional hint text on screens; the *proper* way to onboard new users is an opt-in, dismissible guided tour (spotlight/coach-marks over real controls, e.g. driver.js / Shepherd / react-joyride - vet for the no-global-CSS + Metronic constraints). Per-feature step sets (workflow editor first: palette → canvas → connect → publish/run → logs/debug), "don't show again" persisted per user, replayable from a help affordance. Until this exists, screens stay hint-free. Spawned by the sprint-2/08 workflow-engine UI review. | [sprint-2/08-workflow-engine](../plans/sprint-2/08-workflow-engine.md) | Medium | Open |
| BL-084 | **Audit log (system-wide change history)** - a queryable audit trail of who-did-what-when across tenant-scoped entities, for compliance/forensics. Distinct from the Workflow engine's per-run execution log (that records automation runs, not human/system CRUD). The plan-08(sprint-2) workflow engine adds an explicit service-layer `emit_entity_event(entity_type, action, record, actor, changed_fields)` seam (created/updated/deleted, actor-attributed) - the audit log can subscribe to that SAME seam to persist an append-only `audit_log` row per change, rather than re-instrumenting every service. Decide retention/PII-redaction/tenant-scoping; surface as a Resource list (read-only). | [sprint-2/08-workflow-engine](../plans/sprint-2/08-workflow-engine.md) | High | Open - seam **finalized** in [sprint-2/10-workflow-integration-polish](../plans/sprint-2/10-workflow-integration-polish.md) (D5): `register_event_subscriber(fn)` in `entity_events.py` is the stable, after-commit, isolated-per-subscriber hook; the log registers here at startup and re-instruments nothing. |
| BL-086 | **Form `payment` field type** - collect payment inside a form (registration fee at submit). Deliberately omitted from the form-engine v1 taxonomy: no payment provider exists yet (F6), and a field that cannot work violates the foolproof-UI mandate. Doc schema is additive - lands with the F6 payment provider + Cluster F. | [sprint-3/01-form-engine](../plans/sprint-3/01-form-engine.md) | Medium | Open |
| BL-087 | **Entity-sourced choice options** - form choice fields currently take static author-typed options (`{kind:'static'}`, shaped for extension). Add `{kind:'entity'}`: options fed from a registered entity list (e.g. "pick your session" from agenda sessions). Needs an option-source registry + careful public-endpoint data exposure (only whitelisted, tenant-scoped lists). | [sprint-3/01-form-engine](../plans/sprint-3/01-form-engine.md) | Medium | Open |
| BL-088 | **`entity.create` workflow action** - create a domain record from a workflow (the form→participant binding: `form.submitted` trigger → create participant). Whitelisted to **non-auth domain entities only** (participant, lead - never User/Role/Tenant; auth-user creation stays the locked provisioning/signup path, BL-032 kill-switch must not be routable-around). Deferred from the form-engine plan because today's triggerable-entity set contains nothing safe to create - lands with the first F4 domain entities (Cluster B/D). | [sprint-3/01-form-engine](../plans/sprint-3/01-form-engine.md) | High | Open |
| BL-089 | **Conditions on repeater sub-fields** - form-engine v1 allows `conditionsJson` on a repeater as a whole but not on its sub-fields (rule-fact keys would need row-indexing). Design row-scoped fact resolution (`answers.<repeaterKey>[row].<subKey>`) if per-row conditional logic is demanded. | [sprint-3/01-form-engine](../plans/sprint-3/01-form-engine.md) | Low | Open |
| BL-090 | **Anonymous fill browser-local autosave** - anonymous public respondents get no server-side drafts (no identity); long forms lose progress on tab close. Add localStorage autosave keyed by form slug + version, restored on revisit, cleared on submit. | [sprint-3/01-form-engine](../plans/sprint-3/01-form-engine.md) | Low | Open |
| BL-091 | **Form-engine slice-1 review follow-ups (bundle, low-risk)** - deferred from the sprint-3/01 code review: (a) `statuses` UNIQUE(entity_type,tenant_id,scope_id,key) loses the DB dup-key backstop for tenant-forked UNSCOPED rows (scope_id NULL ⇒ Postgres NULL-distinct); app-side `get_by_key` still guards (concurrency-only gap) - restore with a PG15+ `NULLS NOT DISTINCT` or partial unique indexes if a DB backstop is wanted. (b) Delete orphaned `form-service.mock.ts` (Phase-A scaffolding, now unwired). (c) Add a cross-language parity test pinning `lib/rule-eval.ts` + `lib/computed-expr.ts` to the backend evaluators (only `validateFormDoc`/taxonomy are pinned today). (d) Generalize `FormService._fireable_map`'s per-row `available_transitions` loop into `status_machine.fireable_edge_ids` (a force-return flag) so the next scoped entity reuses shared infra. (e) Scoped status reads route through `resolve_tier` (coincidentally correct - pin `tier=tenant_id` directly for scoped entities). (f) A single signature-pad tap stores a blank-but-non-empty PNG that passes `required` - track `hasInk` and emit '' when nothing was drawn. | [sprint-3/01-form-engine](../plans/sprint-3/01-form-engine.md) | Low | Open |
| BL-092 | **Authed file/signature download in the submission detail view** - the slice-2 serve route (`GET /submissions/{id}/files/{fieldKey}/{n}`, CSP-sandboxed, `submissions.read`) exists + is tested, but the read view can't link to it directly (an `<img src>`/anchor can't carry the Bearer). Wire an authed `fetch`→object-URL (or a short-lived signed link) so a reviewer can open/download an uploaded file or rendered signature; today the read view shows file names + a "Signature captured" chip. | [sprint-3/02-form-engine-public-workflow](../plans/sprint-3/01-form-engine.md) | Medium | Done (sprint-3/02 - apiFetchBlob + SubmissionContext) |
| BL-093 | **Widen the upload sniff-gate to office docs** - `detect_upload_mime` (slice 2) accepts images + PDF only; add zip-container magic-byte sniffing for docx/xlsx/pptx (and a careful allow-list) so review/registration forms can collect Office attachments. Keep SVG excluded (script-bearing). | [sprint-3/02-form-engine-public-workflow](../plans/sprint-3/01-form-engine.md) | Low | **Done (sprint-4/04)** - `detect_upload_mime` widened to images/PDF/GIF/zip-family/UTF-8 text; zip refined to the specific OOXML mime by filename extension so the builder's allowed-types config can target docx/xlsx/pptx; exe/SVG/HTML hard-blocked. `COMMON_MIMES` gained PowerPoint. Tests in `test_form_uploads.py`. |
| BL-094 | **Per-folder restricted ACL** - slice-04 Drive visibility is flat-RBAC (anyone with `documents.read` sees the whole tenant Drive). Add folder-level access restriction (e.g. a Finance folder visible only to a role/user set) - recursive ancestry-checked, with the same polymorphic target-validation discipline. | [sprint-3/04-document-mgmt-drive](../plans/sprint-3/04-document-mgmt-drive.md) | Medium | Open |
| BL-095 | **ZIP-expand bulk import** - slice-04 bulk import = flat multi-file drag into the current folder. Add: upload a `.zip` and recreate its internal folder hierarchy in the Drive (path-sanitize against traversal, per-entry collision handling, depth cap). | [sprint-3/04-document-mgmt-drive](../plans/sprint-3/04-document-mgmt-drive.md) | Low | Open |
| BL-096 | **Office-doc inline preview** - slice-04 previews images + PDF inline; docx/xlsx/pptx show an icon + download. Add a server-side converter (LibreOffice headless / Gotenberg, no-Node stance) → PDF/HTML preview, served through the existing CSP-sandbox route. | [sprint-3/04-document-mgmt-drive](../plans/sprint-3/04-document-mgmt-drive.md) | Medium | Open |
| BL-097 | **Restore-previous-version UI** - `file_versions` history is kept (replace/edit append); slice-04 only exposes the current version. Add a version-history panel per file: list versions, preview/download a prior one, restore-as-current (appends a new version pointing at the old blob - never destroys). | [sprint-3/04-document-mgmt-drive](../plans/sprint-3/04-document-mgmt-drive.md) | Low | Open |
| BL-098 | **Blob prune / Trash auto-purge** - slice-04 retains all version blobs and keeps soft-deleted rows in Trash indefinitely. Add (a) a retention setting to auto-purge Trash after N days, (b) optional old-version pruning (keep last K), both failure-isolated background passes that drop the orphaned blobs. Frees quota. | [sprint-3/04-document-mgmt-drive](../plans/sprint-3/04-document-mgmt-drive.md) | Low | Open |
| BL-099 | **Antivirus scan on upload** - slice-04 sniff-gates by magic byte + blocks exe/html/svg, but stored binaries aren't malware-scanned. Add an AV pass (e.g. ClamAV) on upload (quarantine until clean), especially before slice-05 public download surfaces them. | [sprint-3/04-document-mgmt-drive](../plans/sprint-3/04-document-mgmt-drive.md) | Medium | Open |
| BL-100 | **Widen Drive sniff allow-list** - slice-04 allows images/PDF/MS-Office/OpenDocument/text-csv/zip. Extend the magic-byte allow-list to more document/media types as tenants need them, keeping the exe/html/svg hard floor. | [sprint-3/04-document-mgmt-drive](../plans/sprint-3/04-document-mgmt-drive.md) | Low | Open |
| BL-101 | **Wire the entity-link seam to a real consumer (Cluster B)** - the polymorphic `file_links(entity_type,entity_id,file_id)` table + link/unlink API ship in slice 05 as an untested seam (no domain entity exists to link). Wire + test it against the first consumer (quotation document attach, Cluster B): attach/detach a Drive file to a record, list a record's files, tenant-scoped resolve. | [sprint-3/05-document-mgmt-sharing](../plans/sprint-3/05-document-mgmt-sharing.md) | High | Open |
| BL-102 | **`file.shared` workflow trigger** - slice-04 registers `file` as triggerable (free `entity.created/updated/deleted`). Add a dedicated `file.shared` trigger (fires when a share link is created, with tier/capability/target in the payload) so workflows can react to sharing (e.g. notify on a new public link). | [sprint-3/05-document-mgmt-sharing](../plans/sprint-3/05-document-mgmt-sharing.md) | Low | Open |
| BL-103 | **Share-link analytics** - track per-link access (view count, last-accessed, unique IPs, download count) for share links; surface on the Share dialog + `/documents/shares` oversight list. Useful for "did the client open the quotation?" Privacy: aggregate-only for public links. | [sprint-3/05-document-mgmt-sharing](../plans/sprint-3/05-document-mgmt-sharing.md) | Low | Open |
| BL-104 | **Public-view watermark / download protection** - optionally watermark a publicly-shared PDF/image preview (tenant name / "shared via link" / recipient) and/or disable download (view-only-in-browser). Ties into F2 render. For sensitive doc sharing. | [sprint-3/05-document-mgmt-sharing](../plans/sprint-3/05-document-mgmt-sharing.md) | Low | Open |
| BL-105 | **Notify-on-access / access-request flow** - (a) notify the link creator (workflow/email) when a share is first opened; (b) for an `off`/expired link, let a visitor request access → routes to the owner to re-enable. Needs the `file.shared`/access events (BL-102). | [sprint-3/05-document-mgmt-sharing](../plans/sprint-3/05-document-mgmt-sharing.md) | Low | Open |
| BL-106 | **WABA Billing & Usage tab (read-only)** - no spendable-wallet/top-up API exists for a Tech Provider (respond.io's balance is *their* reseller ledger). Build a read-only tab from real Meta data: `GET /{business_id}/extendedcredits` (credit-line status) + `pricing_analytics`/`conversation_analytics` (spend chart) + a WhatsApp-Manager deep-link for payment. Analytics-shaped work, deferred out of Slice A. | [sprint-3/06-omnichannel-waba-config-profile](../plans/sprint-3/06-omnichannel-waba-config-profile.md) | Low | Open |
| BL-107 | **WhatsApp Calls - settings + in-app call handling** - Meta calling-settings API is real (`POST /{phone_number_id}/settings` calling object) but the product has no call-answer surface (no dialer/WebRTC/SIP). Enabling "allow contacts to call you" with nowhere to receive = foolproof dead-end. Ships with the actual call-handling feature: the 3 settings toggles + an inbound-call inbox. | [sprint-3/06-omnichannel-waba-config-profile](../plans/sprint-3/06-omnichannel-waba-config-profile.md) | Low | Open |
| BL-108 | **WhatsApp profile photo upload** - Profile tab (Slice A) shows the photo read-only; uploading needs Meta's resumable-upload flow (`POST /{app_id}/uploads` → handle → `profile_picture_handle`), which needs `META_APP_ID` + can't be cleanly dev-stubbed. The upload helper (`adapter.upload_resumable`) gets built for template media headers in plan 07, so fold profile-photo upload in afterwards. | [sprint-3/06-omnichannel-waba-config-profile](../plans/sprint-3/06-omnichannel-waba-config-profile.md) | Medium | Open |
| BL-109 | **`template.status_changed` workflow trigger** - plan-07 webhook handler (`apply_webhook_event`) is emit-ready when a template's Meta review status flips. Wire a dedicated workflow trigger (payload: channel, template name/id, from/to status, rejection reason) + per-channel selectivity so workflows can react (e.g. notify on a rejected template). | [sprint-3/07-omnichannel-templates](../plans/sprint-3/07-omnichannel-templates.md) | Low | Open |
| BL-110 | **Flow / Catalog / Carousel template buttons + product templates** - omitted from the plan-07 builder because they reference resources we don't manage (published WhatsApp Flow ID, connected Meta product catalog). Foolproof-gated: build only once those prerequisite features exist, with a disabled-until-configured warning. | [sprint-3/07-omnichannel-templates](../plans/sprint-3/07-omnichannel-templates.md) | Low | Open |
| BL-111 | **Named template variables + multi-language sets** - plan-07 v1 uses positional `{{1}}` vars and one language per template. Add Meta named variables (`{{order_id}}`) and multi-language template groups (submit/manage the same template across languages as a set). | [sprint-3/07-omnichannel-templates](../plans/sprint-3/07-omnichannel-templates.md) | Low | Open |
| BL-112 | **Filtered/scoped aggregate facts** - plan-03 Slice 4 ships unfiltered count + column ops only. Add named, code-declared filters to `AggregatableRelation` (e.g. "count of *Confirmed* participants") so the expander generates filtered facts (`"Participants · Confirmed · Count"`). Filter predicate stays code-side (status-key or clause), tenant-scoped. Interim: `aggregate_fact(where=)` escape hatch. | [sprint-4/03-status-engine-derived-status](../plans/sprint-4/03-status-engine-derived-status.md) | Medium | Open |
| BL-113 | **Relation sub-grouping in the RuleBuilder fact picker** - Slice 4 disambiguates aggregate facts by label only (`"Participants · Count"`). When an entity has many aggregates, sub-group the picker by relation under the entity's source. Polish, not correctness. | [sprint-4/03-status-engine-derived-status](../plans/sprint-4/03-status-engine-derived-status.md) | Low | Open |
| BL-114 | **Reusable Resource-shell tree view-mode** - categories ship as a standalone tree component (`app/(protected)/ems/categories`); the Resource shell is flat-list-only. Build a generic `tree` view-mode into `ResourceListConfig` (expand/collapse, search, dnd drag-to-reparent + drag-to-reorder persisting `sort`, top-level drop zone) so categories - and the document-drive folder-tree - run on the shell instead of bespoke trees. User mandate: no standalone components. | [sprint-4/08-crm-module-split-and-catalog-core](../plans/sprint-4/08-crm-module-split-and-catalog-core.md) | Medium | Open |
| BL-115 | **Drive document list → Resource shell** - the core Drive (`document-drive`) is still a bespoke folder-tree + file list. Move the file list onto the Resource shell (likely depends on BL-114 tree mode for the folder hierarchy). Flow works today; this is consistency fine-tune. | [sprint-4/08-crm-module-split-and-catalog-core](../plans/sprint-4/08-crm-module-split-and-catalog-core.md) | Low | Open |
| BL-116 | **Cluster D money → Numeric/Decimal** - finance `invoices`/`invoice_lines` + ems `project_products` price/tax_rate store money as `Float` (slice-2 expedience). plan-08 mandates money as `Numeric`; convert columns to Numeric + do the totals math in `Decimal` (offering pricing, invoice subtotal/tax/total, cart payload) to avoid float rounding. Pervasive - own pass. | [sprint-4/05-cluster-d-registration-ticketing-venue](../plans/sprint-4/05-cluster-d-registration-ticketing-venue.md) | Medium | Open |
| BL-117 | **Cluster D list N+1 queries** - `OfferingService._enrich` (per-offering unit/sold/held counts + product name), `CartService._cart_payload`/`checkout`/`_ga_remaining` (per-row `_product_name` + counts), `VenueService._enrich` (per-venue zone/seat counts) run per-row. Batch into grouped aggregate queries before the loop. Lists are small today; optimize before large catalogs/venues. | [sprint-4/05-cluster-d-registration-ticketing-venue](../plans/sprint-4/05-cluster-d-registration-ticketing-venue.md) | Low | Open |
| BL-118 | **Finance install sweep for existing ems tenants** - EMS now `requires: finance`; new installs cascade, but tenants that already had ems (pre-finance) won't have finance ACTIVE → registration confirm 409s. Add a migration/bootstrap sweep installing finance + granting `invoices.*` for every tenant with ems active (the tenant_admin_grant lesson). | [sprint-4/05-cluster-d-registration-ticketing-venue](../plans/sprint-4/05-cluster-d-registration-ticketing-venue.md) | Medium | Open |
| BL-119 | **Cart hold TTL scheduler** - holds expire via a lazy sweep on read/confirm (`CartService._sweep`); a stale hold blocks capacity until someone next reads. Add a Celery-beat minute-tick to release expired RESERVED units + GA holds proactively (mirrors the workflow scheduler). | [sprint-4/05-cluster-d-registration-ticketing-venue](../plans/sprint-4/05-cluster-d-registration-ticketing-venue.md) | Low | Open |
| BL-120 | **Admin Tickets/Registrations list = real endpoint** - the event Tickets tab is mock (`event-billing-service`); needs a real `GET /ems/projects/{id}/tickets` (attendee/offering/seat/status/paid/invoice) on the Resource shell, replacing the mock + the portal "my order" mock. | [sprint-4/05-cluster-d-registration-ticketing-venue](../plans/sprint-4/05-cluster-d-registration-ticketing-venue.md) | Medium | Open |
| BL-121 | **Review portal write actions gated by `.read` keys** - `modules/ems/routers/portal_review.py` gates `revise`/`resubmit` on `my_submissions.read`, `save_review_draft`/`submit_review` on `my_reviews.read`, `decide` (POST) on `decisions.read`. The true boundary (`may_decide`/author-ownership/assignment-ownership) is backend-enforced so it's not a hole, but a write gated by a read key is a least-privilege wart. Add distinct write keys (e.g. `my_reviews.grade`, `decisions.decide`) + persona grants. | [sprint-4/06-cluster-e-submissions-review](../plans/sprint-4/06-cluster-e-submissions-review.md) | Low | Open |
| BL-122 | **New core `reviews.*` perms re-grant sweep for existing tenants** - `reviews.read/manage/grade/decide` reach tenant Admin only on NEW provisions (`tenant_admin_grant` is install-time). Existing tenants need a re-grant sweep (the documented tenant_admin_grant gotcha) or they get "no access" to Review processes until re-seeded. | [sprint-4/06-cluster-e-submissions-review](../plans/sprint-4/06-cluster-e-submissions-review.md) | Medium | Open |
| BL-123 | **Portal context labels for tenant-wide scope** - the portal event-filter pill resolves project scope names, but a `tenant`-scope membership maps to no `contextId` (reviews bind to projects). If tenant-wide "narrowing" is later wanted it needs a distinct contextType. Also: `submit-options`/`MySubmission` expose only the config `name` (no event link beyond `context_id`); a richer event label could be surfaced. | [sprint-4/06-cluster-e-submissions-review](../plans/sprint-4/06-cluster-e-submissions-review.md) | Low | Open |
| BL-124 | **Trait-as-rule-fact / workflow exposure** - sprint-4/09 keeps `has_trait`/`status_for_trait` as PURE functions and deliberately does NOT expose status traits to the rule engine or workflow triggers in v1 (Q8 boundary). Wrap them in a rule-engine fact resolver (e.g. a `record.status.has_trait('x')` boolean fact per traited entity) and/or a workflow condition so auto-edges and workflows can branch on a status's semantic trait instead of its key - completing the "configurable semantics, end to end" story. Tenant-scoped resolution; the trait catalog (`traits_for`) drives the fact list. | [sprint-4/09-configurable-status-semantics](../plans/sprint-4/09-configurable-status-semantics.md) | Medium | Open |
| BL-SS-001 | **Rename internal Python package / ops identifiers to Foundryx** - `service_backend`/`app` package paths, image + DB role/db names, env prefixes still read EMS. Cosmetic + non-tenant-facing (tenant UI already white-labelled in Slice 1), but rename for clarity before the codebase grows. | [sprint-1/01-shared-service-platform-omnichannel](../plans/sprint-1/01-shared-service-platform-omnichannel.md) | Low | Open |
| BL-SS-002 | **Interactive (button/list) message type on the public send API** - v1 `POST /api/v1/omnichannel/messages` supports text/template/media only; interactive returns `unsupported_type`. Add the interactive payload shape + adapter support. | [sprint-1/01-shared-service-platform-omnichannel](../plans/sprint-1/01-shared-service-platform-omnichannel.md) | Medium | Open |
| BL-SS-003 | **Consumer-facing WebSocket stream** - v1 delivery is outbound webhooks + `GET …/messages?since=` backfill only; internal Redis WS powers the platform inbox but is not exposed to consumers. Add an authenticated consumer WS (workspace-key-gated) as an alternative to webhooks. | [sprint-1/01-shared-service-platform-omnichannel](../plans/sprint-1/01-shared-service-platform-omnichannel.md) | Low | Open |
| BL-SS-004 | **Kafka `EventBus` implementation** - the `EventBus` interface is built in Slice 4 with a Redis Streams impl (sufficient given Meta per-number caps). Add a Kafka drop-in when throughput warrants it; no consumer code changes (interface already abstracts it). | [sprint-1/01-shared-service-platform-omnichannel](../plans/sprint-1/01-shared-service-platform-omnichannel.md) | Low | Open |
| BL-SS-005 | **LLM as service #2** - sibling module `modules/llm` with its own `/api/v1/llm/*` + webhooks, consuming the message stream + calling the omnichannel send API. Slice-1 registry seams support it with zero core changes; not built in this plan. | [sprint-1/01-shared-service-platform-omnichannel](../plans/sprint-1/01-shared-service-platform-omnichannel.md) | Medium | Open |
| BL-SS-006 | **API-key granular scopes / test-mode keys** - v1 keys are live-only, no scopes. Add per-key scopes (send-only, read-only) + a `fxw_test_` sandbox key class if consumer demand appears. | [sprint-1/01-shared-service-platform-omnichannel](../plans/sprint-1/01-shared-service-platform-omnichannel.md) | Low | Open |
| BL-SS-007 | **Per-workspace rate-tier auto-detection** - v1 per-number token-bucket uses a super-admin default tier; auto-detect the number's actual Meta messaging tier and adjust the bucket. | [sprint-1/01-shared-service-platform-omnichannel](../plans/sprint-1/01-shared-service-platform-omnichannel.md) | Low | Open |
| BL-SS-009 | **Metronic demo-cruft cleanup (white-label + foolproof)** - post-strip leftovers surfaced during Slice-3 verification: (a) `MENU_MEGA`/`MENU_MEGA_MOBILE` still carry dead Metronic demo routes (`/account/home/*`, `/account/security/*`, etc.) that render as dead links in the header menu; prune to real surviving routes. (b) The demo1 footer is Keenthemes/Envato-branded ("Keenthemes Inc.", Purchase/Envato links) - replace with Foundryx/neutral. (c) The `/` dashboard is Metronic demo content ("KeenThemes Network", fake stats). (d) Dead demo menu arrays `MENU_SIDEBAR_CUSTOM`/`MENU_SIDEBAR_COMPACT` + the unused demo2/3/6/10 layout components + the now-orphaned mega-menu sub-components (`MegaMenuSubProfiles/Network/Account/Apps`) can be deleted. The demo1 `MegaMenu` was fixed to render generically (Slice 3), so those bespoke sub-renderers are unused. | [sprint-1/01-shared-service-platform-omnichannel](../plans/sprint-1/01-shared-service-platform-omnichannel.md) | Low | Open |
| BL-SS-008 | **De-EMS residual couplings in core routes** - the orphan-cleanup sweep (Slice 1) kept `services/ems-service.*` (used by `settings/general` for `setTenantSettings` + `imports/[jobId]`), `services/registration-service.ts` + `types/registration.ts` (used by `imports/[jobId]/page.tsx`) because deleting them would break kept core routes. These are EMS-named/EMS-domain services still wired into generic core pages. Refactor: move the generic tenant-settings call off `emsService` onto a properly-named core service, and remove the EMS registration importer coupling from the generic imports page. | [sprint-1/01-shared-service-platform-omnichannel](../plans/sprint-1/01-shared-service-platform-omnichannel.md) | Low | Open |
| BL-SS-010 | **WhatsApp Flows (multi-screen in-chat forms)** - deferred from rich-message-types; effectively a second form engine, own plan. | [sprint-3/12-omnichannel-rich-message-types](../plans/sprint-3/12-omnichannel-rich-message-types.md) | Medium | Open |
| BL-SS-011 | **WhatsApp commerce messages** - product / multi-product / catalog messages + carousel + OTP/authentication templates; need a Meta product catalog + commerce setup. | [sprint-3/12-omnichannel-rich-message-types](../plans/sprint-3/12-omnichannel-rich-message-types.md) | Low | Open |
| BL-SS-012 | **On-device message recall** - WhatsApp Cloud API has no delete-for-everyone endpoint; revisit if Meta ships one (local-hide only for now, also deferred). | [sprint-3/12-omnichannel-rich-message-types](../plans/sprint-3/12-omnichannel-rich-message-types.md) | Low | Open |
| BL-SS-013 | **Location map-tile embed** - location cards show coords + "Open in Maps" link; embed a real map (self-hosted tiles, CSP-safe) later. | [sprint-3/12-omnichannel-rich-message-types](../plans/sprint-3/12-omnichannel-rich-message-types.md) | Low | Open |
| BL-SS-016 | **Cross-agent live reaction sync** - an AGENT reaction is applied optimistically on the acting client; its WS echo is suppressed to avoid the add-echo-after-remove race, so a SECOND agent's reaction only appears on the first agent's next thread load (CONTACT reactions do sync live). Add ordered/last-write-wins WS handling (e.g. per-target sequence) so agent↔agent reactions update live without the echo race. | [sprint-3/12-omnichannel-rich-message-types](../plans/sprint-3/12-omnichannel-rich-message-types.md) | Low | Open |
| BL-SS-015 | **EMS consumer-webhook ticket update (cross-repo)** - the rich-message-types additions (Slice 1/2/3: `message.inbound` media+payload fields, new `message.reaction` event) must be reflected in the EMS integration ticket in the sibling repo `dreamz_ems` (`documentation/plans/sprint-4/10-…`). This repo can't edit `dreamz_ems`; contract is finalized in [12-omnichannel-consumer-webhook-contract](../plans/sprint-3/12-omnichannel-consumer-webhook-contract.md). | [sprint-3/12-omnichannel-rich-message-types](../plans/sprint-3/12-omnichannel-rich-message-types.md) | Medium | Open |
| BL-SS-014 | **Membership-scoped conversation reads** - omnichannel conversation/media reads are tenant+permission scoped (not `WorkspaceMember` scoped); `install_tenant` never adds admins as members, so a hard membership gate would 403 seeded admins. Adopt membership-scoped reads consistently across `/contacts` + the media endpoint if per-workspace read isolation within a tenant becomes required. | [sprint-3/12-omnichannel-rich-message-types](../plans/sprint-3/12-omnichannel-rich-message-types.md) | Low | Open |
| BL-SS-017 | **EMS consumer widget (cross-repo, host counterpart of Slice 6)** - the EMS repo (`dreamz_ems`) must build the embed WIDGET per `11-omnichannel-embed-widget.md`: mint HS256 assertions server-side, mount the iframe with the non-secret connection id as `?c=<connectionId>` (drives `frame-ancestors`, contract §4), run the postMessage handshake, add the `omnichannel.message_received` workflow trigger, and DROP its own WABA config/profile/template copies (now owned here - AC-11H-19). This repo can't edit `dreamz_ems`. | [sprint-4/11-omnichannel-embed-host](../plans/sprint-4/11-omnichannel-embed-host.md) | High | Open |
| BL-SS-018 | **Embed jti consumed before agent provisioning** - `EmbedSessionService.exchange` commits the single-use `jti` (step 7) BEFORE the external-agent upsert (step 8); a provisioning failure burns the assertion so it can't be retried. Reorder to provision-then-consume, or make the jti insert share the agent-upsert transaction, so a mid-exchange failure leaves the assertion replayable. Robustness only (no security impact). | [sprint-4/11-omnichannel-embed-host](../plans/sprint-4/11-omnichannel-embed-host.md) | Low | Open |
| BL-SS-019 | **One-consumer-per-shared-tenant topology limit** - core `connections` is `UNIQUE(tenant_id, provider)` (+ `UNIQUE(tenant_id, type)`), so a shared-service tenant holds exactly ONE `omnichannel_shared` connection → two consumers must be two shared-service tenants. Identity isolation is sound (`(connection_id, sub)`), but if multiple consumers must embed into ONE shared tenant/workspace, relax the constraint or add a consumer sub-key. Document the constraint until then. | [sprint-4/11-omnichannel-embed-host](../plans/sprint-4/11-omnichannel-embed-host.md) | Medium | Open |
| BL-SS-025 | **Consumer self-serve webhook registration on `/api/v1`** - CLOSED 2026-08-09. Shipped `GET/POST /api/v1/omnichannel/webhooks` + `PATCH|/rotate|/enable|/disable|DELETE /{id}`, scoped to the API key's workspace + active channel, with a cross-workspace guard (`_own_endpoint` - `WebhookService._get` is tenant-scoped only, so a multi-workspace tenant could otherwise reach across; mismatch returns the same uniform 404 as a miss). Guide §7 documents it. Originally: Webhook endpoints are created/rotated/inspected only via `/omnichannel/webhooks` gated `webhooks.read`/`webhooks.manage` (session JWT), so a consumer holding ONLY a workspace API key cannot create or even list its own callbacks - yet webhooks are the intended inbound path AND the only source of several fields the Rio read shapes omit (see BL-SS-026). Either expose registration under `/api/v1` scoped to the key's workspace, or state in the guide that registration is operator-only by design. Product decision first. | [omnichannel consumer gateway](../omnichannel/consumer-integration-guide.md) | Medium | Closed |
| BL-SS-026 | **`RioContactItem` is lossy vs `ThreadItem`** - the 2026-08-09 reconciliation made the MESSAGE shape lossless but not the CONTACT shape: `priority`, `channelId`, `channelType`, `unreadCount`, `lastMessageAt`, `lastIncomingMessageAt`, `lastMessagePreview` remain webhook-only, so an API-key consumer cannot build an inbox list (unread badge, last-message preview) from reads alone. Inconsistent with the rule the same commit wrote down ("dropping a field the internal shape carries is not fine - a consumer has no other source for it"). CLOSED 2026-08-09 - all seven added to `RioContactItem` as explicit Foundryx extensions, sourced from the ThreadItem the Rio shape is now derived from (so the two cannot drift). | [omnichannel consumer gateway](../omnichannel/consumer-integration-guide.md) | Medium | Closed |
| BL-SS-027 | **Webhook delivery still has a DNS-rebinding TOCTOU** - `assert_deliverable` re-resolves the callback host immediately before each POST, then `httpx` resolves it AGAIN when connecting, so a low-TTL attacker-controlled record can still flip between the two. Closing it properly means pinning the validated IP and connecting to it directly (custom transport / `httpx` `Client(transport=...)` with a resolved address + `Host` header, or an allowlist proxy). Materially harder to exploit than the fail-open path fixed in this pass, but the last remaining hole on a surface any API-key holder can aim. | [omnichannel consumer gateway](../omnichannel/consumer-integration-guide.md) | Medium | Open |\n| BL-SS-028 | **No rate limit on the `/api/v1` gateway** - `get_api_workspace` does key resolution + module-active only; the platform throttle (`app/services/throttle.py`) covers auth/forms, not this surface. Webhook create/rotate and the send endpoint are all unmetered per key. A per-key token bucket (reusing ThrottleStore) would bound both abuse and accidental hammering. Related: BL-SS-020 (embed session success-side throttling). | [omnichannel consumer gateway](../omnichannel/consumer-integration-guide.md) | Medium | Open |\n| BL-SS-029 | **A pruned pagination cursor silently restarts at the present** - `contact_repository.list_messages_recent` drops the keyset filter when the `before`/`after` anchor no longer resolves, returning the newest page instead. A consumer following `nextBefore` whose target was pruned loops forever instead of terminating. Now that `nextBefore` is the documented paging mechanism this matters more; return a typed 404/422 on an unresolvable cursor. Pre-existing on main. | [omnichannel consumer gateway](../omnichannel/consumer-integration-guide.md) | Low | Open |\n| BL-SS-022 | **CI guard: a gateway wire change must carry a guide diff** - the 07-11 respond.io reshape broke a live consumer silently because it changed `/api/v1/omnichannel/*` without touching the consumer guide. Note the guide was **already tracked in git** (`documentation/omnichannel/consumer-integration-guide.md`, 3 commits of history) - being in the repo did NOT prevent the drift, so this is a **review-gate** gap, not a file-location one. Add a CI check that fails (or warns) when `modules/omnichannel/routers/api_v1.py` or the `Rio*` block of `modules/omnichannel/schemas.py` changes without `documentation/omnichannel/consumer-integration-guide.md` also changing. Separately, wire publishing so the repo copy is the source for doc.foundryx.my rather than a parallel CMS copy that can drift. | [omnichannel consumer gateway](../omnichannel/consumer-integration-guide.md) | Medium | Open |
| BL-SS-023 | **`phone:` identifier is ambiguous when two contacts share a number** - `find_by_phone_in_workspace` digit-normalises and returns the FIRST match, so a workspace holding two rows for the same number (seen locally: demo `cnt-001` "+60 12-345 6789" alongside a send-created "+60123456789") resolves non-deterministically - a consumer using `phone:` can read the wrong, empty thread. Either de-dup on send-stitch, or make the lookup deterministic (prefer the contact with recent messages) and document the tie-break. | [omnichannel consumer gateway](../omnichannel/consumer-integration-guide.md) | Medium | Open |
| BL-SS-024 | **Voice notes are sent as `video/webm` and Meta rejects them** - every VOICE row in the local dev DB is `failed` with "Could not process the voice recording." (`mimeType: video/webm`). WhatsApp accepts voice as **ogg** only (guide §4.2); the browser recorder's webm is never transcoded. Transcode to ogg/opus before upload, or restrict the recorder's mime. Unrelated to the contract drift - spotted in the same data. | [omnichannel consumer gateway](../omnichannel/consumer-integration-guide.md) | Medium | Open |
| BL-SS-020 | **`/embed/session` success doesn't pump the throttle** - the IP bucket records on FAILED exchanges only (matches the login-IP model), so a flood of VALID single-use assertions isn't rate-limited. Add a light success-side counter if abuse via valid-assertion floods appears (each is single-use + 15-min TTL, so blast radius is bounded). | [sprint-4/11-omnichannel-embed-host](../plans/sprint-4/11-omnichannel-embed-host.md) | Low | Open |
| BL-SS-021 | **Admin screen for the embed connection** - CLOSED. Shipped as a dedicated **Omnichannel ▸ Settings ▸ Embed access** page (`app/(protected)/omnichannel/settings/embed/`, gated `workspaces.manage`) rather than the integrations Resource shell: connection-id copy, generate/rotate `embedSecret` (shown once), allowed-origins editor, workspace+route iframe-snippet builder. Backed by `embed_config_service` + `/omnichannel/embed-config` routes. Guide §12.1 updated to point at the screen. | [sprint-4/11-omnichannel-embed-host](../plans/sprint-4/11-omnichannel-embed-host.md) | Medium | Closed |
| BL-SS-030 | **Workflow node palette's module-visibility filter is a one-off** - the omnichannel trigger/actions added in plan 17 are hidden behind a single hand-written `useInstalledModules()` check in `node-palette.tsx`, not a generic mechanism. Generalize into a backend-driven `module` field consumed uniformly for every future module-contributed node instead of hand-filtering the frontend array per addition. | [sprint-4/17-omnichannel-ai-workflow-agent](../plans/sprint-4/17-omnichannel-ai-workflow-agent.md) | Low | Open |
| BL-SS-031 | **MCP / function-calling tool attachment on the AI Agent workflow node** - explicitly out of scope for plan 17 (brief said: architect for it, don't build it). The executor's single `messages=[...]` build site (`app/workflow_engine/actions/ai_agent_actions.py`) and `AiSpan`'s reserved `tool:*` span kind (`app/models/ai.py`, Bi-D17) are the intended seam - a future project-management-lookup tool (or any other) attaches there. | [sprint-4/17-omnichannel-ai-workflow-agent](../plans/sprint-4/17-omnichannel-ai-workflow-agent.md) | Low | Open |
| BL-SS-032 | **Additional Code action languages and package policy** - plan 19 ships restricted Python only. Evaluate JavaScript plus a curated, immutable package allowlist only after the external runner, permission, isolation, and resource-limit contract is proven in production. Network, filesystem, environment, and subprocess access remain separate security decisions rather than implicit language features. | [sprint-4/19-stateful-ai-workflow-runtime](../plans/sprint-4/19-stateful-ai-workflow-runtime.md) | Low | Open |
| BL-SS-033 | **Read-only "Agent State" workflow node** - IN PROGRESS (sprint-4/20). A generic canvas node `ai_agent.read_state` that outputs the current accepted Agent state for the run's Correlation key (`nodes.<id>.<field>` + `stateRevision`/`pendingField`/`exists`) so a builder inspects/routes it into IF / Code / Send Message without an AI Agent node. READ-ONLY by decision (writes stay through the evidence-checked AI Agent reducer, plan 19 line 29). Pairs with `ai_agent.clear_state`. State is per-field structured state keyed by conversationId, NOT last-N-turns transcript (out of scope, plan 19 D11 / line 434). (Re-numbered from a BL-SS-032 collision with the Code-languages item.) | [sprint-4/20-agent-state-read-node](../plans/sprint-4/20-agent-state-read-node.md) | Medium | In progress |
| BL-SS-034 | **AutoCount ETL: no tenant-level timezone for "Daily at HH:MM" reconcile scheduling** - Foundryx has only a per-user `users.timezone` preference, which has no natural owner for an unattended scheduled task, so `EtlService.next_run_times`/the beat sweep resolve `reconcileAt` as UTC (documented in `modules/autocount/scheduler.py`). Add a tenant-level timezone setting (and re-resolve `dailyAt` against it) once a customer needs true local-wall-clock reconcile scheduling in a non-UTC tenant. | [sprint-4/22-autocount-db-etl](../plans/sprint-4/22-autocount-db-etl.md) | Low | Open |
| BL-SS-035 | **AutoCount ETL: the Runs grid's "Deleted" column is a push-verdict count, not a staged-delete-intent count** - `AcSyncRun.deleted_count` (plan 22 S3) counts `deleted+deactivated+not_found` PUSH verdicts (the ones the consumer resolved), mirroring `pushed_count`'s meaning for upserts - NOT how many delete intents a reconcile classified (unlike `added_count`/`updated_count`, which ARE fetch-time classifications). So a reconcile that detects a delete but can't reach the consumer (Sorento down, anchor error) shows "Deleted: 0" on that run row even though a delete intent WAS staged and is visible via the batch's staged-record detail. Consider surfacing a distinct "delete intents staged" count on the run row (or a badge) so this is visible without opening the batch. | [sprint-4/22-autocount-db-etl](../plans/sprint-4/22-autocount-db-etl.md) | Low | Open |
| BL-SS-036 | **AutoCount ETL documents: line-only-edit detection has no fallback for a source that never bumps the header's LastModified** - S5's design leans entirely on the AutoCount convention "editing a line always bumps its header's LastModified" (see plan 22 S5 §2.8). If a future document source (or a customisation on the customer's AutoCount DB) doesn't honour that convention, a line-only edit is invisible to incremental sync forever - no reconcile-only or lines-digest fallback exists to catch it. Add a lines-digest fallback (hash the header's own line set, compare on reconcile) for a source that can't be trusted to bump the header watermark. | [sprint-4/22-autocount-db-etl](../plans/sprint-4/22-autocount-db-etl.md) | Low | Open |
| BL-SS-037 | **AutoCount ETL documents: batch the per-header lineQuery fan-out with a real `IN` expansion instead of per-header round trips** - S5 review SHOULD-FIX 3 added hard caps (`MAX_DOCUMENT_HEADERS_PER_RUN`/`MAX_DOCUMENT_LINES_PER_HEADER`, `sql_source/source.py`) as the safe stopgap for the N+1 (one bound `lineQuery` per changed header, same session) rather than rewriting the operator-authored scalar `WHERE ... = :doc_key` into an `IN :doc_keys` (rejected as fragile string surgery, plan 22 S5 §2.8). Revisit once a real customer's document volume regularly approaches the caps: chunk changed header keys into pages (e.g. 500) and run ONE query per page with the predicate rewritten to `IN`, cutting round trips by ~500x without touching the operator's authored text semantics (would need a principled, tested SQL-text rewrite, not a substring swap). | [sprint-4/22-autocount-db-etl](../plans/sprint-4/22-autocount-db-etl.md) | Low | Open |
| BL-SS-038 | **AutoCount ETL: on-prem connector agent for customer DBs that cannot be port-forwarded** - plan 22 ships direct SQL connections only (grill Q1; the first customer port-forwards). Customers behind NAT with no VPN need an outbound-connecting agent (FineDataLink-style) that proxies the read-only queries. | [sprint-4/22-autocount-db-etl](../plans/sprint-4/22-autocount-db-etl.md) | Medium | Open |
| BL-SS-039 | **AutoCount ETL: cron-expression scheduling** - plan 22 ships interval (>= 1 min) + "Daily at HH:MM" only (grill Q15). Add 5-field cron via the existing `scheduler.compute_next_run_at` (croniter) once an operator asks for business-hours-only or weekday schedules. | [sprint-4/22-autocount-db-etl](../plans/sprint-4/22-autocount-db-etl.md) | Low | Open |
| BL-SS-040 | **Extract the SQL source + `sql_database` provider into a platform-core data-integration engine** - plan 22 built them inside the autocount module behind the `EntitySource` seam (grill Q3). `sql_source/` (runtime, guard, introspection, preview, hashing) and `SqlDatabaseProvider` are already module-agnostic; a second Service needing DB extraction is the trigger to lift them into `app/`. | [sprint-4/22-autocount-db-etl](../plans/sprint-4/22-autocount-db-etl.md) | Low | Open |
| BL-SS-041 | **AutoCount stock balances via the vendor API** - deliberately OUT of plan 22 (grill Q18): stock quantities are volatile and Sorento's `stock.quantity_available` is DB-generated. Agreed path = the AutoCount API integration ~2 weeks after plan 22 lands, into a Sorento `stock` ingest spec that writes `quantity_on_hand` only. | [sprint-4/22-autocount-db-etl](../plans/sprint-4/22-autocount-db-etl.md) | High | Open |
| BL-SS-042 | **SO/PO write-back into AutoCount via the vendor API** - plan 22 is read-only AutoCount -> Sorento (grill Q2/Q10); writing documents INTO an accounting DB directly is forbidden (doc numbering, stored procs). Orders created in Sorento that must land in AutoCount need a separate API-based plan. | [sprint-4/22-autocount-db-etl](../plans/sprint-4/22-autocount-db-etl.md) | Medium | Open |
| BL-SS-043 | **Connections form renders provider `select` fields with a bare shadcn `Select`** - pre-existing before plan 22 (surfaced by the S1 review when the `sql_database` provider's `dbType` select rendered through it); the design mandate says every dropdown is a `SearchSelect`. Swap the registry `select` renderer in `connection-form-fields.tsx`. | [sprint-4/22-autocount-db-etl](../plans/sprint-4/22-autocount-db-etl.md) | Low | Open |
| BL-SS-044 | **`e2e/autocount.spec.ts` `clickSyncNow` located its entity row via `.first()`** - fragile against catalogue order: entity rows are `ORDER BY entity_type ASC`, so which row lands first shifts as more entities are added ("Customer" now sorts before "Goods received note"), breaking the spec with no code regression. | [sprint-4/22-autocount-db-etl](../plans/sprint-4/22-autocount-db-etl.md) | Low | **Closed** - same plan (S6 merge-gate review nit): `clickSyncNow` now scopes to the row by its visible entity label (`page.getByRole('row', { name: /Goods received note/i })`) instead of `.first()`. |
| BL-SS-045 | **TanStack Query adoption for lists** - plan 23 keeps `useResourceList` (hand-rolled `useEffect`) and builds the keep-rows-while-loading + prefetch behaviour into it (D3). React Query is installed but has zero `useQuery` call sites. Adopting it would give a query cache, `LIST_QUERY_OPTIONS` (`placeholderData: keepPreviousData`), `staleTime`/`retry` defaults and a page-scoped client-side record pager like Sorento D4. Its own plan; touches every list hook. | [sprint-4/23-design-language-alignment](../plans/sprint-4/23-design-language-alignment.md) | Medium | Open |
| BL-SS-046 | **Sidebar collapse transform-only rewrite** - plan 23 T3 tokenises and gates the existing `width`/`padding-inline-start`/`inset-inline-start` transition (`css/demos/demo1.css`) and records a DevTools trace of a collapse on the Users list. Sorento tried a transform-only rewrite and reverted it. Open this only if the T3 trace shows dropped frames; otherwise make the collapse instant. | [sprint-4/23-design-language-alignment](../plans/sprint-4/23-design-language-alignment.md) | Low | Open |
| BL-SS-047 | **Dark-mode toggle** - plan 23 T1 defines the full `.dark` token set (materials, scrim, z, semantic inks, three lightness steps) but ships no user-facing switch (Sorento D12 parity). Add the toggle under Account preferences with the `users.timezone`-style preference column and honour `prefers-color-scheme` by default. | [sprint-4/23-design-language-alignment](../plans/sprint-4/23-design-language-alignment.md) | Low | Open |
| BL-SS-048 | **Canvas node add/remove and context-menu motion** - the React Flow surfaces (`flow-canvas`, `workflow-canvas`, `status-engine/entity-flow`) mount and unmount nodes instantly and their cursor-anchored context menus appear without origin-anchored scaling. Plan 23 deliberately leaves canvases alone (frequency gate: tens/day interactions). Revisit with `find-animation-opportunities` once the rest of the product is on the spring. | [sprint-4/23-design-language-alignment](../plans/sprint-4/23-design-language-alignment.md) | Low | Open |
| BL-SS-049 | **Sync-back with Sorento after it merges Motion Round 2** - plan 23 ports M1/M2/M3/M4/M6 from `sorento_crm` `origin/integration/ui-motion-round2` (unmerged there on 4 Sep 2026) and the never-built M5/M7 rules. When Sorento merges, diff `lib/motion.ts`, `primitive-classes.ts`, `config.reui.css`, `styles.css` and `DESIGN-LANGUAGE.md` against this repo's `docs/reference/design-language.md` and reconcile in both directions so the two products stay one system. | [sprint-4/23-design-language-alignment](../plans/sprint-4/23-design-language-alignment.md) | Medium | Open |
| BL-SS-050 | **Impersonation banner is unclickable under a Radix modal** - it paints above the scrim (z 60 > 50) but the body has `pointer-events: none` while a Dialog/Sheet/AlertDialog is open, so "Exit impersonation" swallows clicks until the modal closes. Fix = portal the banner with `pointer-events: auto` and exempt it from outside-pointer dismissal, or accept and document. | [sprint-4/23-design-language-alignment](../plans/sprint-4/23-design-language-alignment.md) | Low | Open |
| BL-SS-052 | **`tenants.transition` per-row-payload deferred action** - T5's tenant-lifecycle deferred actions (`tenants.archive/suspend/reactivate`) are edge-agnostic (key-keyed lookups against the platform-owned, system-locked seeded statuses). An operator-added custom tenant status sharing a label with a seeded one but a different key falls through to `confirm:` (disclosed carve-out). A fully general fix needs `useDeferredAction`/`ActionMenu`/`BulkActions` to support a PER-ROW payload (not one shared payload for the whole park batch) and a generic `tenants.transition` deferred action keyed by `transitionId`. | [sprint-4/23-design-language-alignment](../plans/sprint-4/23-design-language-alignment.md) | Low | Open |
| BL-SS-053 | **`documents.trash` deferred action has no frontend caller** - T5 registers `documents.trash` (core `app/deferred_actions/handlers.py`) but the document drive is a bespoke explorer, not a Resource-shell list, so nothing in the frontend parks it yet (`entityType: 'document_file'` never appears in a `deferred:` config). Either wire the drive's own delete action onto it or drop the registration if the drive's delete stays intentionally immediate/bespoke. | [sprint-4/23-design-language-alignment](../plans/sprint-4/23-design-language-alignment.md) | Low | Open |
| BL-SS-054 | **Memoise/cache module activation per request in `PendingActionService`** - `_module_active` (T5 fix round 2 S4, fix round 3 item 1) calls `active_modules(db, tenant_id)` on every `park`/`current`/`cancel`/`commit_one` call, so a frontend `current()` poll re-queries the tenant's active-module set on every tick. Fine at today's poll cadence (a few seconds); if the poll window ever tightens below ~60s aggregate load, memoise per-request (e.g. cache on `db.info` like the workflow event bus's `workflow_origin`) or add a short-TTL cache. | [sprint-4/23-design-language-alignment](../plans/sprint-4/23-design-language-alignment.md) | Low | Open |
| BL-SS-055 | **Sweep the remaining 16 record forms onto Next's `notFound()`** - T6 (AC-DLA-50) wired `UserFormView` to call the real `notFound()` (rendering `app/(protected)/not-found.tsx`) instead of its own inline "User not found." paragraph; the other ~16 `use-XForm` hooks (roles, workflows, forms, templates, tenants, ai agents/skills, connections, workspaces, channels, ideas, business requirements, autocount companies, ...) still render their own ad-hoc inline "X not found" text. Convert them for one consistent 404 surface. | [sprint-4/23-design-language-alignment](../plans/sprint-4/23-design-language-alignment.md) | Low | Open |
| BL-SS-056 | **Live frame-diff for the loading.tsx "no vertical shift" claim (AC-DLA-50)** - `agent-browser` exposes no network-conditions/chunk-throttling API (only response mocking), and delaying the page's own `fetch()` calls doesn't extend `loading.tsx`'s on-screen time (Next's Suspense boundary is driven by the route segment's code-split chunk fetch, not by a client `useEffect` data fetch). T6 verified the claim by construction (`ListPageSkeleton`/`PageHeader` share the same `Container` + spacing utilities, pinned by `skeletons.test.tsx`'s structural assertions) rather than a live pixel-diff. Revisit if `agent-browser` gains CDP network-conditions/throttling, or accept the structural proof as sufficient. | [sprint-4/23-design-language-alignment](../plans/sprint-4/23-design-language-alignment.md) | Low | Open |
| BL-SS-057 | **Demo1 dashboard still renders Metronic marketing widgets with fake data** - T7's AC-DLA-71 sweep white-labeled the footer and the one live "KeenThemes" copy leak (`entry-callout.tsx`), but the rest of the mounted `/` dashboard (`ChannelStats`, `EarningsChart`, `Highlights`, `TeamMeeting`, `Teams`) is still Metronic demo content unrelated to any real Foundryx entity (fake follower counts, a static "Teams" table of made-up rows) - not a T7 AC target, a much bigger dashboard-redesign effort. `channel-stats.tsx` also still has a raw `<style>` tag (background-image, pre-existing, same class as `entry-callout.tsx`'s which T7 left alone for the same reason). Replace with a real product dashboard (recent activity across the installed Services) in its own plan. **T7 fix round 1 update:** the sidebar's "Dashboards > Light Sidebar / Dark Sidebar" theme-demo submenu (same family of leftover) was flattened to a single `Dashboards -> /` leaf; `/dark-sidebar` (`app/(protected)/dark-sidebar/`, `Demo1DarkSidebarPage` - it just re-renders the light dashboard's content under a dark sidebar) is now menu-orphaned but not deleted, since it renders the SAME content this row already tracks for a redesign - delete it alongside that redesign rather than as a separate step. | [sprint-4/23-design-language-alignment](../plans/sprint-4/23-design-language-alignment.md) | Medium | Open |
| BL-SS-058 | **Ideation triage board has instructional copy** ("Drag across columns to change status, or within a column to reorder priority.") - a foolproof-UI violation (PRINCIPLES.md: no on-screen how-to text) found incidentally while capturing T7's AC-DLA-62 sidebar-sweep evidence (`32-ideation-triage-board-*.png`), pre-existing and outside this slice's named ACs. Remove the sentence; if the drag affordance needs discovery, that is a guided-tour candidate (BL-085), not inline copy. | [sprint-4/23-design-language-alignment](../plans/sprint-4/23-design-language-alignment.md) | Low | Open |
| BL-SS-059 | **Per-site (not per-file) focus-ring audit for `outline-none`/`outline-hidden`** - T7 (AC-DLA-59) fixed the two sites with genuinely NO ring anywhere in their file (`slider.tsx`'s thumb, the search-dialog borderless input's forced `ring-0!`) and spot-checked one representative shadcn-derived primitive (`tabs.tsx`) to confirm the "ring lives elsewhere in the same file" pattern holds; it did not mechanically verify all ~25 remaining `outline-hidden` sites (menu items in `dropdown-menu.tsx`/`menubar.tsx`/`context-menu.tsx`/`accordion-menu.tsx`/`select.tsx`/`command.tsx`, mostly relying on `focus:bg-accent`-style background highlighting rather than a ring, which is a valid WCAG 2.4.7 indicator but wasn't individually confirmed present at each site). A follow-up could extend `a11y-guardrails.inventory.test.ts` with a brace-aware per-declaration check (mirroring `findButtons`) instead of the current coarse per-file signal. | [sprint-4/23-design-language-alignment](../plans/sprint-4/23-design-language-alignment.md) | Low | Open |
| BL-SS-061 | **Frame-trace `lib/motion.ts` / `sheet.tsx`'s `x`/`y`/`scale` shorthand animations for compositor-only execution** - the T8 animation review (AC-DLA-67) approved the diff but flagged as low-tier: `motion`'s `x`/`y`/`scale` transform shorthands used in `lib/motion.ts` (`surfaceVariants`) and `sheet.tsx`'s slide-in/out are LIKELY WAAPI-composited (motion's default engine promotes transform/opacity animations off the main thread when it can), but this was not confirmed with an actual DevTools performance/frame trace this slice. Worth a real trace pass to confirm no main-thread layout/paint work is happening on these hot paths (every dialog/sheet/popover open-close). | [sprint-4/23-design-language-alignment](../plans/sprint-4/23-design-language-alignment.md) | Low | Open |
| BL-SS-060 | **Tenant privacy-policy URL from branding for signup consent** - T7 fix round 1 item 5 removed the signup form's "Privacy Policy" link (`/privacy-policy` resolved to no route anywhere in the app - a 404 on a legally-referenced consent link) and left the checkbox copy self-contained ("I agree to the terms and conditions."). A real per-tenant (or platform-default) privacy-policy URL, likely a `tenant_branding` field surfaced through the same public branding endpoint the signup page already reads, would let the consent checkbox link out again without hardcoding a page the product doesn't publish. | [sprint-4/23-design-language-alignment](../plans/sprint-4/23-design-language-alignment.md) | Low | Open |
| BL-SS-062 | **Workflow status picker / transition action for scoped status machines (omnichannel_contact)** - plan 25 review round 1 finding 7: `omnichannel_contact` registered `has_status=False` because its real lifecycle machine is workspace-SCOPED (`omnichannel_contact_lifecycle`, `scope_id=workspace_id`) with no single tenant-wide status list to resolve, so `workflow_service.metadata()`'s unscoped `Status.entity_type==` lookup and `entity_actions.entity_transition_status`'s `status_machine.transition(entity.entity_type, ...)` call both break for it (empty picker / `UnknownStatusEntity`). A proper fix needs a generic reverse pointer (e.g. `WorkflowEntity.status_entity_type` + a workspace-context field on the transition-status action config) that both the metadata endpoint and the transition action resolve through the SCOPED status registry - not a module special case. `entity.status_changed` (the trigger) is unaffected - it already fires via the existing `StatusEntity.workflow_entity_type` reverse pointer at emission time. | [sprint-4/25-omnichannel-contact-data-model](../plans/sprint-4/25-omnichannel-contact-data-model.md) | Medium | Open |
| BL-SS-063 | **`custom_fields_json` should be `JSON(none_as_null=True)`** - plan 25's `Contact.custom_fields_json` column follows the module's existing JSON column convention but was flagged in review round 1 as worth a follow-up cleanup pass (accepted as-is for this round; the column already defaults to `{}` at every write path, so the None-as-SQL-null gap has not been observed in practice, but the house convention is `none_as_null=True` on every JSON column). | [sprint-4/25-omnichannel-contact-data-model](../plans/sprint-4/25-omnichannel-contact-data-model.md) | Low | **Closed** - review round 2 finding B: column flipped to `JSON(none_as_null=True)`, plus a `jsonb_typeof(...) = 'object'` guard on `value_counts`/`_strip_values` and a non-dict-blob guard on the ThreadItem registry-intersection read, for LEGACY rows that predate the flip. |
| BL-SS-064 | **`entity.update` coerces every written value to `str` (countryCode not upper-cased, priority unvalidated)** - `app/workflow_engine/actions/entity_actions.py entity_update` calls `render_field` (returns `str`) then `setattr`s it verbatim onto whatever column type the whitelist points at; a workflow-authored `countryCode` patch lands lower/mixed-case (the contact-panel/API path upper-cases it, this path doesn't) and `priority` isn't checked against `VALID_PRIORITY`. Generic gap (not omnichannel-specific) - the action has no per-field type/enum validation hook. | [sprint-4/25-omnichannel-contact-data-model](../plans/sprint-4/25-omnichannel-contact-data-model.md) | Low | **Closed** - round 3 (codex triage B11): `WorkflowEntity.apply_update` generic hook added; `omnichannel_contact` registers one that routes `entity.update` through `ConversationService.patch_thread` (the SAME seam the internal PATCH and public gateway PATCH use) instead of a raw `setattr`, so `countryCode` upper-cases + `priority`/`language` validate identically, and the write now fans out ONE `contact.updated` realtime + webhook event too (moot for any OTHER `entity.update`-writable entity that doesn't register a hook - those still coerce to `str`, tracked generically by the hook mechanism itself). |
| BL-SS-066 | **`entity_update`'s raw-`setattr` fallback (no `apply_update` hook) still coerces every value to `str` for entities other than `omnichannel_contact`** - BL-SS-064's underlying gap is closed for `omnichannel_contact` via the new `WorkflowEntity.apply_update` hook (B11, round 3), but the core `user`/`role`/`tenant`/`connection`/`template`/`workflow` entities have no hook and still go through the old `render_field` + `setattr` path with no per-field type/enum validation. A future entity that needs typed/validated `entity.update` writes should register its own `apply_update` (the generic seam now exists); revisit whether any CURRENT core entity's writable whitelist actually needs one. | [sprint-4/25-omnichannel-contact-data-model](../plans/sprint-4/25-omnichannel-contact-data-model.md) | Low | Open |
| BL-SS-065 | **Hide the Customized/Platform-defaults badge for scoped status entities** - `EntityFlow` (`components/platform/status-engine/entity-flow.tsx`) renders `graph.source === 'tenant' ? 'Customized' : 'Platform defaults'` unconditionally, but a SCOPED entity (e.g. `omnichannel_contact_lifecycle`) is tenant-owned from birth and never forks from a platform default - the badge's "Customized" reading is misleading there (review round 2, finding K). The wire `StatusGraphResponse`/frontend `StatusGraph` type carries no `scoped`/`scopeId` field to gate on today (only the CREATE-input types do) - fix needs BOTH a backend field (e.g. `StatusGraphResponse.scoped: bool`, `types/status-engine.ts StatusGraph.scoped`) and hiding the badge (or a distinct "Workspace-owned" label) when `scoped` is true. | [sprint-4/25-omnichannel-contact-data-model](../plans/sprint-4/25-omnichannel-contact-data-model.md) | P3 | Open |
| BL-SS-067 | **Suppress no-op `contact.updated` fan-out from workflow `entity.update`; consider run-scoped deferral** - plan 25 D15 (final review follow-up): `entity.update` on `omnichannel_contact` routes through `ConversationService.patch_thread`, which COMMITS mid-run and announces (realtime + `contact.updated` webhook) IMMEDIATELY - even when the patched value is identical to the current one (a re-set) and even if a LATER node in the same run fails (no rollback of the announcement). Two candidate follow-ups: (1) short-circuit `patch_thread` when the diff is empty so a no-op `entity.update` node fires no fan-out; (2) a run-scoped deferral so `entity.update` writes commit/announce only once the whole run succeeds - would need per-entity transaction plumbing the executor doesn't have today (accepted trade-off for now, consistent with `WorkflowEntity.apply_update`'s documented "may commit" contract). | [sprint-4/25-omnichannel-contact-data-model](../plans/sprint-4/25-omnichannel-contact-data-model.md) | Low | Open |
| BL-SS-068 | **Contact phone dedupe pass + unique index on `(tenant_id, workspace_id, phone_digits)`** - plan 26 (A2) introduces the normalized `contacts.phone_digits` column and enforces per-workspace phone uniqueness in the SERVICE only (create + import Test). A DB unique index was deliberately NOT added because live workspaces may already hold duplicate numbers (the inbound stitch is best-effort and the gateway can create; see BL-SS-023). Ship a dedupe/merge pass first, then add the partial unique index so uniqueness is structural. | [sprint-4/26-omnichannel-contacts-module](../plans/sprint-4/26-omnichannel-contacts-module.md) | Medium | Open |
| BL-SS-069 | **GC for contact-export CSV blobs** - plan 26 stores the export file key in `background_jobs.result_json.fileKey` (D-A2-14, deliberately not a `*_key` column so the storage-migration drift gate stays meaningful). The job-retention pruner deletes the ROW, leaving the blob in the active storage connection. Add a delete-on-prune hook (or a guarded sweep) for `omnichannel.contacts_export` artefacts; related to BL-126. | [sprint-4/26-omnichannel-contacts-module](../plans/sprint-4/26-omnichannel-contacts-module.md) | Low | Open |
| BL-SS-070 | **Imported lifecycle changes bypass the status machine** - plan 26 D-A2-13: the contacts importer assigns a lifecycle stage DIRECTLY (a set-based bulk data load, like `migrate_records`), so an imported move fires no transition notification and no `entity.status_changed` workflow run; the interactive bulk action keeps full machine semantics. Revisit if a customer expects import-driven lifecycle changes to trigger automations (would need per-row transitions on the UPDATE path). | [sprint-4/26-omnichannel-contacts-module](../plans/sprint-4/26-omnichannel-contacts-module.md) | Low | Open |
| BL-SS-071 | **Segment membership counts** - plan 26 ships segments without a per-segment contact count (respond.io shows one). Counting every segment on every list render is N extra aggregate queries; add a cached/lazy count endpoint (or a single grouped query) when the segment rail grows. | [sprint-4/26-omnichannel-contacts-module](../plans/sprint-4/26-omnichannel-contacts-module.md) | Low | Open |
| BL-SS-072 | **Per-user (private) contact segments** - plan 26 segments are workspace-wide and visible to every member with `contacts.read`; respond.io has no private segments either, but saved views usually grow one. Add an owner + visibility flag when A3's custom inbox views land on the same object. | [sprint-4/26-omnichannel-contacts-module](../plans/sprint-4/26-omnichannel-contacts-module.md) | Low | Open |
| BL-SS-073 | **Contact delete / merge / block** - plan 26 deliberately ships no destructive contact action (D-A2-15): deleting a contact means deciding what happens to its thread history, and merge + block are roadmap Phase B2. Covers the respond.io "Blocked Contacts" view too. | [sprint-4/24-omnichannel-respondio-parity-roadmap](../plans/sprint-4/24-omnichannel-respondio-parity-roadmap.md) | Medium | Open |
| BL-SS-080 | **Import wizard template/config routes lack context, so `cf_<key>` columns are not offered in the column picker** - the pre-upload `GET /imports/config/{entityType}` and `GET /imports/template/{entityType}` routes have no job yet and so resolve the importer's STATIC columns only (`ImporterDef.dynamic_columns` needs a `context_json`, e.g. `workspaceId`, that doesn't exist pre-upload); `cf_*` columns are fully functional once mapped (Test + Import), just invisible on the "Download template" checklist. Thread `?context=` through those two routes to close the gap. | [sprint-4/26-omnichannel-contacts-module](../plans/sprint-4/26-omnichannel-contacts-module.md) | Low | Open |
| BL-SS-081 | **Contacts list renders blank when workspace resolution 403s for a role lacking `workspaces.read`** - plan 26's `useActiveWorkspace` (D-A2-10) is inherited unchanged from the A1/Inbox surface, which assumes every Contacts caller also holds `workspaces.read`; a role with `contacts.read` alone never resolves a workspace id and the page renders an empty list with no error, rather than a clear "you need workspace access" state. Either grant `workspaces.read` alongside `contacts.read` in the tenant Admin default, or surface the 403 explicitly on the Contacts page. Plan 30 (review round 2) found the same root cause on `/omnichannel/reports`: for that role `useActiveWorkspace` yields no workspace, and while the page now renders (empty body, no error), the `ReportPicker` renders ZERO options when its selected value is not a member of the (empty) catalog - it should fall back to the default report label rather than a blank trigger. Fix both surfaces together. | [sprint-4/26-omnichannel-contacts-module](../plans/sprint-4/26-omnichannel-contacts-module.md) | Low | Open |
| BL-SS-077 | **`modules/omnichannel/bootstrap.py seed_demo_conversations` hardcodes global literal ids** - (`cnt-001..005`, `tpl-001..003`, `chn-demo`) that collide (a PK clash for contacts/templates, a cross-tenant CHANNEL reuse for `chn-demo` specifically) if the function is ever called for a second tenant. Today both call sites (`scripts/bootstrap_db.py`, `scripts/init_db.py`) always pass `DEFAULT_TENANT_ID`, so this is dormant - low priority, dev-only seed tooling, not customer-facing. Fix = per-tenant-unique ids (or a tenant-suffixed id scheme) the day this seed is ever wired to a second demo tenant. **Pre-merge follow-up (plan 27):** the function now gates on `tenant_id == DEFAULT_TENANT_ID` (raises loudly otherwise) and the `chn-demo` lookup is tenant-scoped, so a wrong call fails fast instead of half-writing cross-tenant rows - the underlying per-tenant-unique-id design is still not built. | [sprint-4/27-omnichannel-inbox-views-events](../plans/sprint-4/27-omnichannel-inbox-views-events.md) | Low | Open |
| BL-SS-078 | **Pending-run sweep for un-correlated workflow runs whose dispatch failed** - `create_run_for_event`'s non-eager dispatch guard (D-A3-10 / B4) leaves a run row durably Pending when `.delay()` (or any dispatch-side error) fails, so it stays Pending for manual re-dispatch; no automatic redrive exists for un-correlated runs (only serialized runs with a `correlation_key_digest` are recovered, via `recover_serialized_scopes`). Add a periodic sweep (mirrors the serialized recovery beat task) that finds stale un-correlated Pending runs past some grace window and re-enqueues them. | [sprint-4/27-omnichannel-inbox-views-events](../plans/sprint-4/27-omnichannel-inbox-views-events.md) | P2 | Open |
| BL-SS-079 | **Per-entity tracking in `useDeferredAction` so two different rows can be parked concurrently (rail refuses a second delete today)** | [sprint-4/27-omnichannel-inbox-views-events](../plans/sprint-4/27-omnichannel-inbox-views-events.md) | P3 | Open |
| BL-SS-080 | **AutoCount SQL presets in the task editor** - ship the SO/PO header+line and Debtor/Creditor/Item/Location/ItemGroup/Agent queries from `sprint-4/22-autocount-db-etl-autocount-sql.md` as selectable per-entity presets (pre-filled `keyColumns`/`watermarkColumn`/`docDateColumn`/line-column config), replacing the bare `SELECT * FROM schema.table` starter. Validate the SQL on live MSSQL first (derived-table wrap, 8155). | [sprint-5/01-autocount-db-company](../plans/sprint-5/01-autocount-db-company.md) | Medium | **Closed** - pulled into [sprint-5/02-autocount-document-mapping](../plans/sprint-5/02-autocount-document-mapping.md) (presets + seeded mapping). |
| BL-SS-081 | **Attach a second (API) connection to an AutoCount company** - plan 01 keeps ONE `connection_id` per company (grill Q3/Q4). A customer onboarded DB-first who later needs stock (BL-SS-041) or write-back (BL-SS-042) needs an API connection on the SAME company (same `database_name` namespace). Adds `api_connection_id`, `sourceKind` becomes `db|api|both`, Overview gains attach/detach. | [sprint-5/01-autocount-db-company](../plans/sprint-5/01-autocount-db-company.md) | Medium | Open |
| BL-SS-082 | **autocount module migration `0006_autocount_entity_backfill` imports the LIVE `AcCompany` ORM model** - the model carries `0007`'s `sorento_company_code`, so upgrading a DB stamped at `0005` fails INSIDE 0006 (`column does not exist`). Same class as the core backfill lesson: a data migration must query a frozen table snapshot (`sa.table(...)`/raw SQL), never the current model. Bit the local DB during sprint-5/01 S2 (columns pre-applied by hand); hit PRODUCTION for real 2026-09-06 (module stamp stuck before 0007, every AutoCount write 500ing). | [sprint-5/01-autocount-db-company](../plans/sprint-5/01-autocount-db-company.md) | Medium | Fixed by fix/autocount-module-migrations-any-stamp (0006's `upgrade()` is now a documented no-op; the same masters backfill runs at HEAD via `update_tenant`'s seed-if-absent loop) |
| BL-SS-083 | **Task editor Mapping tab stays in its pre-birth 404 state after the first query save** - `task-editor-view.tsx` `onSave` births the `ac_entity_config` row but never calls `mapping.reload()`, so the Mapping tab shows "The mapping could not be loaded." (Add field disabled) until a page refresh. Pre-existing since plan 22 S4; sprint-5/01 makes Add-entity-then-save the DEFAULT path for every DB company, so it now hits every operator. Fix: reload mapping (and entity state) after a successful first save. | [sprint-5/01-autocount-db-company](../plans/sprint-5/01-autocount-db-company.md) | Medium | Open |
| BL-SS-084 | **Wire the Sorento contract-v2 document fields** (`customer_code/name`, `supplier_code/name`, `agent_code`, line `product_code/name`, `warehouse_code`, `from_so_numbers`, document `/deletions`, `shipping_orders` entity) once the addendum lands on Sorento: flip the consumer connection's `sorento_contract_version` default to 2 and remove the payload gate. Operator path (since `fix/sorento-contract-version-field`, 2026-09-06): Settings > Integrations > the Sorento connection > Edit > "Contract version" = `2` > Save > Test - the Test reads Sorento's `GET /api/v1/external/contract` and refuses a version newer than Sorento advertises ("Sorento advertises contract 1.x; choose version 1 or upgrade Sorento"), so the prod flip is per tenant, operator-driven and verified before the next push; new connections already default to `2`, an existing connection with no stored value stays at `1` until edited (no backfill). What remains here is removing the version-1 payload gate once every prod tenant is on 2. | [sprint-5/02-autocount-document-mapping](../plans/sprint-5/02-autocount-document-mapping.md) | High | Open |
| BL-SS-085 | **xlsx → ESB cutover playbook** - the FIRST AutoCount push of a document previously loaded by Sorento's xlsx import must ADOPT its ref-less lines in place (Sorento `_sync_lines`, addendum §9: match by business key, then `line_number`; same row ids, allocations/claims intact) - only lines absent in AutoCount are deleted or cancelled. Document the go-live sequence (masters → SO/PO/SPO, reconcile off until the first full load) and a dry-run report of adopted vs unmatched lines per document. | [sprint-5/02-autocount-document-mapping](../plans/sprint-5/02-autocount-document-mapping.md) | High | Open |
| BL-SS-086 | **PO/SPO overlap as a hard activation gate** - plan 02 reports a header staged by BOTH the `purchase_order` and `shipping_order` task as a warning; promote to a blocking gate once the filter-formula UX has settled. | [sprint-5/02-autocount-document-mapping](../plans/sprint-5/02-autocount-document-mapping.md) | Low | Open |
| BL-SS-087 | **Refuse a watermark column inside `keyColumns`** - the local proof saved the product master task with `keyColumns: [AutoKey, LastModified]`, so every source_ref carried the watermark (`ac_sim:174|2026-09-05T03:10...`) and document lines (`ac_sim:174`) could never resolve the ref rung on Sorento (surfaced as `ref_mismatch`). `validate_source_config` must 422 on `watermarkColumn in keyColumns` (field error `keyColumns`), the Query tab must withhold the chosen watermark from the key picker (foolproof-UI), and the activation preview should warn when a ref would change across an update. Save-time 422 + mutually exclusive pickers landed in plan 02 (`808f84e`, `e538250`); the activation-preview `watermarkInKey` warning for legacy configs landed too (`b0ef891`). | [sprint-5/02-autocount-document-mapping](../plans/sprint-5/02-autocount-document-mapping.md) | High | Closed |
| BL-SS-088 | **Simulate's `doc_key_identity` hardcodes `raw.get("DocKey")`** (`modules/autocount/mapping.py`) while the real run path uses `flat_source_ref()` honouring the task's `keyColumns` - Simulate rejects headers the real sync handles (test-report finding, plan 02 section 3). Make Simulate derive the document key the same way the run does. | [sprint-5/02-autocount-document-mapping](../plans/sprint-5/02-autocount-document-mapping.md) | Medium | Open |
| BL-SS-089 | **Surface activation-preview `warnings.*` in the Review & Activate tab** - `watermarkInKey`, `overlappingDocuments`, `contractVersionMismatch` are backend-only today (`AutocountPreviewOk` has no `warnings` field, `activate-tab.tsx` renders none), so the operator never sees them; also mirror the FE mock `DOCUMENT_PREREQUISITES` with a parity test, and stop `errors["keyColumns"]` being overwritten in `validate_source_config`. | [sprint-5/02-autocount-document-mapping](../plans/sprint-5/02-autocount-document-mapping.md) | Medium | Open |
| BL-SS-090 | **Batched line fetch for document tasks** - plan 03 pages headers but still runs ONE `lineQuery` per changed header (N+1, bounded per page). Rewrite to one `IN (:doc_keys)` query per page (or a temp-table join on MSSQL) once the operator-authored scalar `WHERE ... = :doc_key` convention has a documented batched form. | [sprint-5/03-autocount-bulk-document-load](../plans/sprint-5/03-autocount-bulk-document-load.md) | Medium | Open |
| BL-SS-091 | **Operator "reset baseline" action** - with change-only staging (plan 03 D2) a consumer that wiped its data (the Sorento lane DB reset on 2026-09-05) receives nothing until the task's `ac_row_hash` rows are cleared; today that is a psql `delete`. Add a guarded per-task action (typed confirm) that clears the hashes and arms an immediate full pass. | [sprint-5/03-autocount-bulk-document-load](../plans/sprint-5/03-autocount-bulk-document-load.md) | Medium | Open |
| BL-SS-092 | **Push cap per run as a setting** - `list_pending_for_entity(limit=5000)` is a code constant; with paged extraction the drain cadence should be tunable per deployment (`AUTOCOUNT_PUSH_CAP_PER_RUN`). | [sprint-5/03-autocount-bulk-document-load](../plans/sprint-5/03-autocount-bulk-document-load.md) | Low | Open |
| BL-SS-093 | **Sink drops the consumer `warnings` key** - `SorentoSink` never reads `warnings` from an ingest response, so `warehouse_unresolved` / `overlapping_documents` style warnings Sorento returns are invisible in the ESB run log (live-config session finding BL-D). | [sprint-5/03-autocount-bulk-document-load](../plans/sprint-5/03-autocount-bulk-document-load.md) | Medium | Open |
| BL-SS-094 | **Preview under-counts mapping failures** - the activation preview reported 0 would fail for warehouse (1 failed on the run) and did not count the 10 product rows that fail mapping (empty required `name`); the preview should map every fetched row through the same engine path as the run and report failures per row (live-config session findings BL-B/BL-C). | [sprint-5/03-autocount-bulk-document-load](../plans/sprint-5/03-autocount-bulk-document-load.md) | Medium | Open |
| BL-SS-095 | **A DRAFT/paused paged task's page-in-flight abort can strand staged rows forever** - security review round 2, F2. `run_autocount_sync`'s paged branch commits a page's hashes/cursor BEFORE checking for a cooperative abort, so an abort landing after a page finishes leaves that page's rows genuinely staged on a now-`JOB_ABORTED` job while the cursor has already moved past it. For an ACTIVE (auto-pushing) task this is harmless - `list_pending_for_entity` still delivers those rows exactly once on the next successful run, since it reads STAGED rows ACROSS jobs, not job-scoped. For a DRAFT/paused task run manually, though, review only ever happens per-job (`list_pending_for_job`, gated on the job reaching `NEEDS_REVIEW`), so those rows are invisible forever - no red test in plan 03's review round demanded a fix (the tester's own real-entry-point test for this exact interleave is a REGRESSION GUARD, already green, because it only exercises the ACTIVE path), and the literal "discard the page and re-stage it next run" fix would break the pre-existing, still-required `test_an_abort_stops_after_the_page_in_flight`. Needs a design that tells the two cases apart (e.g. only discard-and-retry when the task is NOT auto-pushing) before it can be implemented safely. | [sprint-5/03-autocount-bulk-document-load](../plans/sprint-5/03-autocount-bulk-document-load.md) | Medium | Open |
| BL-SS-096 | **`SorentoSink.write_batch` opens a fresh `httpx.Client` per chunk** - `_call`'s `with httpx.Client(timeout=self._timeout, transport=self._transport) as client:` builds a brand-new client (and pays a TLS handshake) for every chunk of a multi-chunk push, instead of reusing one connection-pooled client across the whole batch (S5b review round 7 polish - the concurrent path already parallelises the handshakes rather than serialising them, which is most of the practical win, so this is a smaller, lower-priority follow-up). | [sprint-5/03-autocount-bulk-document-load](../plans/sprint-5/03-autocount-bulk-document-load.md) Priority raised to Medium 2026-09-06 (`fix/sorento-batch-size`): the ingest batch size default dropped from 1,000 to 200 (`AUTOCOUNT_SINK_BATCH_SIZE`), so a push now opens about 5x the chunk count - and therefore 5x the fresh clients + TLS handshakes - for the same population. | Medium | Open |
| BL-SS-097 | **A population-defining edit defers the fresh baseline to the next reconcile, and the UI does not say so** - live finding 2026-09-05 (real company, sales_order). `EtlService.update_task` clears `ac_row_hash` and the open `pass` when `query`/`lineQuery`/`fromDate`/`filterFormula` change, but deliberately KEEPS the top-level `sqlWatermark`/`lastKey` (a narrower population does not change what the same column means). The next tick therefore runs an INCREMENTAL pass from the old mark: tonight it re-read only the 68,068 headers past the mark, left 80,000 already-pushed headers with NO hash, and the 570 rows that had FAILED under the old query were not retried until a reconcile pass was forced by hand. Correct by design (the reconcile re-baselines everything), but the operator gets no signal: re-saving a query should either set `next_reconcile_at = now` (so the fresh baseline starts on the next tick) or show a "re-baseline pending until the next reconcile" notice on the task. | [sprint-5/03-autocount-bulk-document-load](../plans/sprint-5/03-autocount-bulk-document-load.md) | Medium | Open |
| BL-SS-098 | **Retryable sink verdicts drop the record's own `errors`** - live finding 2026-09-05. Sorento answers `outcome=retryable` with an `errors` list naming the unresolved reference (`customer_ref`/`sales_agent_ref`/`product_ref`/`warehouse_ref`); `SorentoSink` folds that into the generic "a referenced master has not synced yet" text on `run.error`/`pushFailures` and the STAGED row keeps `error` NULL and `errors_json` NULL. SO doc key 45274227 stayed STAGED across four pushes with nothing on our side saying WHICH master is missing; Sorento's server log had only batch summaries. Persist the record's `errors` on the staged row (`errors_json`) and surface it in the staging drawer; Sorento is adding a per-record INFO log line on their side. A quarantined (`failed`) verdict has the same gap - `staged.mark` (`repositories/autocount_repository.py:630-641`) writes only `status`/`pushed_at`, so `error`/`errors_json` stay NULL on a `failed` row too even though the `WriteResult` message carried Sorento's errors; the fix needs to cover both verdicts. | [sprint-5/03-autocount-bulk-document-load](../plans/sprint-5/03-autocount-bulk-document-load.md) | Medium | Open |
| BL-SS-099 | **Formula builder rejects raw source columns on MASTER entities** - live finding 2026-09-06 (real company, product `name`). `mapping-editor-body.tsx` (around lines 99-100) passes `builderVariables = []` for master entities, so the builder's client-side validator flags every source column (`Description`, `ItemCode`, ...) as "Unknown name" and keeps Apply disabled, while the backend save gate (`config.result_columns`) and the runtime facts (the raw row) both accept them. The operator can only reach a working master formula through the raw `PUT .../entities/<entity>/mapping` payload. Feed the master builder the task's `result_columns` (the same list the document builder gets for header columns) so `if(trim(coalesce(Description, "")) == "", ItemCode, Description)` can be authored in the UI. | [sprint-5/03-autocount-bulk-document-load](../plans/sprint-5/03-autocount-bulk-document-load.md) | Medium | Open |
| BL-SS-100 | **A blank required source value fails BEFORE the row's formula can rescue it** - live finding 2026-09-06. `MappingRow.coerce` (`mapping.py:493-501`) short-circuits a blank/NULL source value to `None` and the `is_required` check fires first, so a formula written to supply a fallback (`if(trim(coalesce(Description, "")) == "", ItemCode, Description)`) never runs when the row is sourced from the blank column. Ten AutoCount items with an empty `Description` (AutoKeys 6, 8, 11, 292, 6650, 8196, 10868, 10887, 10898, 11161) failed on required `name` and SO doc key 45274227 stayed retryable at Sorento for the missing product ref. Workaround used tonight: re-source the `name` row from `ItemCode` (never blank) and keep the same formula. Fix: evaluate the formula (facts = the raw row) before the required check, or document on the row that a formula cannot rescue its own blank source. | [sprint-5/03-autocount-bulk-document-load](../plans/sprint-5/03-autocount-bulk-document-load.md) | Medium | Open |
| BL-SS-101 | **`bootstrap_modules` swallows a module migration failure and the container goes healthy** - prod incident 2026-09-06 (BL-SS-082's failure mode, live for real): the autocount module stamp got stuck before 0007 (App Store showed 0.1.0), `run_module_migrations` raised, `bootstrap_modules` caught it and let the app boot anyway, and the health check reported healthy with every AutoCount write 500ing (`column "etl_status" does not exist`) - the failure was silent until an operator hit a 500. Propose either failing the container's health check, or refusing to boot the module's routes/registration, when `run_module_migrations` raises for a module - a deploy must not be able to silently ship a stale module schema behind a green health check. | [module platform](../engineering/module-platform-and-app-store.md) Amendment (fix lane review, 2026-09-06): failing the container health check is the WRONG shape - it takes core + omnichannel down for one module and contradicts per-module isolation (D8). The seam is `require_module` (`app/dependencies.py:176-197`): return 503 when `name in ERRORED_MODULES`, so the broken module's routes refuse instead of 500ing (`load_modules` has its own try/except, so the routes still load when the migration failed - today they load and 500). `module_loader.py:248` already logs ERROR with a greppable "bootstrap failed" marker; surface that, do not touch the health check. | High | Open |
| BL-SS-102 | **The ESB has no explicit-clear path for a master field under Sorento contract 2.1** - sprint-5/04 made `CanonicalMaster.sink_payload` omit every None-valued key (2.1: `null` on a master = clear, absent = leave alone), so once Sorento answers `2.1` the ESB can never CLEAR a master field: a customer whose AutoCount `EmailAddress` was blanked keeps its old Sorento email forever. Sorento's ruling (2026-09-06, matches their xlsx importer rule under D14 - present column + blank cell = clear, absent column = leave): a MAPPED field whose AutoCount value is blank must arrive as `null` (clear); only UNMAPPED fields are omitted; `credit_limit` / `payment_terms_*` stay absent (no longer in their schema). The 2.1 wire needs no change for this. The seam is on the ESB side: an enabled mapped field resolving to None is a real clear, an unmapped field is an omission - that distinction exists in the mapping engine (`MappingRow.is_enabled` + the row set per entity), not in the canonical model (`sink_payload` sees only attribute values), so the follow-up is to carry a "mapped fields" set from the engine into `sink_payload` (emit `null` for a mapped-but-blank field, omit an unmapped one). Do not ship the seam before Sorento answers 2.1 on the target host (on 1.x/2.0 `null` and absent are the same write). | [sprint-5/04-sorento-contract-2-1-masters](../plans/sprint-5/04-sorento-contract-2-1-masters-acceptance-criteria.md) | Medium | Open |
| BL-SS-103 | **Migrations 0010/0011/0012 still reach the live ORM through `backfill.py`** - `backfill_document_line_mapping_pickers` (`backfill.py:279-286`) and `disable_line_rows_missing_from_preview` (`:383-391`) run `db.query(AcEntityConfig)` (and write `AcFieldMapping` rows), which SELECTs every column the CURRENT model maps - so the next column added to `ac_entity_config` or `ac_field_mapping` re-creates the 2026-09-06 outage (`UndefinedColumn` inside the migration, `bootstrap_modules` swallows it, module frozen at a stale stamp) at THREE stamps at once. 0010's "safe by construction" docstring is true only for today's column set. Remedy: query/insert through a frozen `sa.table(...)` snapshot of only the columns those helpers need (the fix lane's `existing_columns` guard covers the raw-SQL helpers, not these ORM ones). CI proposal (NOT built in the fix PR; the reviewer's replay took about 2s): a `deploy.yml` step that runs module Alembic `0001 -> head` on an empty scratch Postgres AND `upgrade 0002 -> seed rows -> upgrade head`, so a model-vs-stamp regression fails the pipeline instead of a deploy. | [module platform](../engineering/module-platform-and-app-store.md) | High | Open |
| BL-SS-104 | **Omnichannel reports: materialized daily rollup table + incremental refresh job** - every report and dashboard tile aggregates the LIVE `conversation_events` / `conversation_messages` tables on each request. That is correct and cheap at current volumes (see BL-SS-105's measurement), but a tenant with millions of events will eventually want a `report_daily_rollup` table refreshed incrementally by a beat task, with the live query kept as the fallback for the current (partial) day. | [sprint-4/30-omnichannel-dashboard-reports](../plans/sprint-4/30-omnichannel-dashboard-reports.md) | Medium | Open |
| BL-SS-105 | **Omnichannel reports: `(tenant_id, created_at)` composite index on `conversation_messages` if volume grows** - D-A9-12's measurement (S2, run on the live s30 Postgres): 1,000,000 seeded `conversation_messages` rows (100k for the target tenant) in a throwaway workspace, `EXPLAIN ANALYZE` on the `messages` report query WITH and WITHOUT `ix_conv_messages_tenant_created`. With correct planner statistics (`ANALYZE` run, as autovacuum does in production) the query completes in **~37-49 ms via the existing single-column `tenant_id` index either way** - no seq scan at this scale - so the index was **deferred, not shipped**. The builders kept the explicit window-bound `WHERE` (redundant with the per-bucket `CASE` predicates, but it lets Postgres range-scan). Revisit if a tenant's `conversation_messages` grows an order of magnitude, or if a report starts timing out. The revision would be index-only (plan reserves `0013`, rebased onto head at merge). | [sprint-4/30-omnichannel-dashboard-reports](../plans/sprint-4/30-omnichannel-dashboard-reports.md) | Medium | Open |
| BL-SS-106 | **Omnichannel reports: team dimension turned on (filter + `groupBy=team` + `teamName`)** - D-A9-13 routes every team read through ONE `team_column()` accessor that returns `None` until plan 28 / A8 ships the column; `reports/meta` advertises `dimensions.team.available=false` and `teamId` / `groupBy=team` are a typed 422 until then. Turning it on is: return the column, add `team` to the affected `supportsGroupBy` lists, add `teamName` to the grouped row shapes. **Blocked on A8.** | [sprint-4/30-omnichannel-dashboard-reports](../plans/sprint-4/30-omnichannel-dashboard-reports.md) | High | Open |
| BL-SS-107 | **Omnichannel dashboard: team presence panel** - online status + assigned-count per member, respond.io's dashboard has one. Needs a presence source (the WS pub/sub rooms are the obvious seam) that does not exist yet. | [sprint-4/30-omnichannel-dashboard-reports](../plans/sprint-4/30-omnichannel-dashboard-reports.md) | Low | Open |
| BL-SS-108 | **Omnichannel reports: scheduled report emails** - a `schedule.cron` workflow that renders a report through `render_email` and sends it on a cadence. The export job already produces the CSV; the missing pieces are a report-render template context and a workflow action to attach the artefact. | [sprint-4/30-omnichannel-dashboard-reports](../plans/sprint-4/30-omnichannel-dashboard-reports.md) | Low | Open |
| BL-SS-109 | **Omnichannel reports v2: lifecycle funnel, time-in-stage, contacts added / merged** - roadmap Phase D. The dashboard ships lifecycle stage COUNTS only; the funnel (stage-to-stage conversion) and time-in-stage need lifecycle transition events that A5's lifecycle work would have to emit. | [sprint-4/30-omnichannel-dashboard-reports](../plans/sprint-4/30-omnichannel-dashboard-reports.md) | Low | Open |
| BL-SS-110 | **Omnichannel reports: tenant-wide roll-up across all workspaces** - every report is single-workspace by D-A9-16 (`/omnichannel/workspaces/{wsId}/reports/...`). A tenant with several workspaces has no combined view. Needs a workspace-set parameter plus a decision on how per-workspace dimensions (channel, lifecycle stage) merge. | [sprint-4/30-omnichannel-dashboard-reports](../plans/sprint-4/30-omnichannel-dashboard-reports.md) | Low | Open |
| BL-SS-111 | **Omnichannel reports: comment log on the Users report** - respond.io renders a second table (per-agent internal comments) beneath the users table. Deferred with the rest of the second-table surfaces. | [sprint-4/30-omnichannel-dashboard-reports](../plans/sprint-4/30-omnichannel-dashboard-reports.md) | Low | Open |
| BL-SS-112 | **Omnichannel reports: left rail of report names at `>= lg`** - the report chooser is a `SearchSelect` (AC-RPT-44); respond.io uses a persistent left rail on wide viewports. Purely presentational, and the `SearchSelect` stays the mobile shape either way. | [sprint-4/30-omnichannel-dashboard-reports](../plans/sprint-4/30-omnichannel-dashboard-reports.md) | Low | Open |
| BL-SS-113 | **Omnichannel reports: previous-period comparison line + delta percentage** - respond.io shows both the current and the preceding equivalent period on every metric card. Needs a second windowed query per metric plus a delta presentation on `DurationStatCard` / the state tiles. | [sprint-4/30-omnichannel-dashboard-reports](../plans/sprint-4/30-omnichannel-dashboard-reports.md) | Low | Open |
| BL-SS-114 | **Omnichannel reports: `first assignment -> first response` and `last assignment -> first response` metrics** - respond.io's Responses report carries all three first-response flavours; this slice ships the contact-message-to-agent-reply one only. The `assigned` events needed for the other two are already written (the assignment log reads them). | [sprint-4/30-omnichannel-dashboard-reports](../plans/sprint-4/30-omnichannel-dashboard-reports.md) | Low | Open |
| BL-SS-115 | **Platform: export blob garbage collection for `background_jobs.result_json.fileKey`** - report exports (this plan) and A2's contacts export both write a CSV to storage and record its key on the job row; the retention pruner deletes the JOB rows but nothing deletes the BLOBS, so every export leaks one object forever. Shared with A2 - one sweeper covering every job type that writes a `fileKey`. | [sprint-4/30-omnichannel-dashboard-reports](../plans/sprint-4/30-omnichannel-dashboard-reports.md) | Medium | Open |
| BL-SS-116 | **Omnichannel reports: run the first-reply tie dedupe BEFORE the sample cap, not after** - plan 30 N-2 dedupes tied first-agent rows in Python after `duration_samples` has already fetched them, so a burst of same-second agent messages consumes the 100k `REPORT_MAX_SAMPLE_ROWS` budget as N rows while contributing one sample; a tenant with heavy bulk sends could hit `SampleCapExceeded` early. Move the tiebreak into SQL (correlated `MIN(id)` among the tied rows, or a window function) so the cap counts contacts, not tied messages. | [30-omnichannel-dashboard-reports.md](../plans/sprint-4/30-omnichannel-dashboard-reports.md) (review round 2 residual) | Low | Open |
| BL-SS-117 | **Omnichannel reports: stale-while-revalidate the report catalog on a workspace switch** - switching workspace on `/omnichannel/reports` replaces the whole page chrome (header, picker, filter bar) with the spinner while `reports/meta` refetches, because `useReportMeta` clears `meta` on every `workspaceId` change and the page's loading branch is all-or-nothing (plan 30 N-1 made that branch correct, not gentler). Keep the previous catalog rendered (dimmed) until the new one lands, and only fall back to the spinner on first load. | [30-omnichannel-dashboard-reports.md](../plans/sprint-4/30-omnichannel-dashboard-reports.md) (review round 2 residual) | Low | Open |
| BL-SS-118 | **A CANCELLED broadcast whose chunk chain already died keeps a dangling ledger forever** - `BroadcastService.cancel()`'s SENDING branch delegates the sweep (`repo.skip_remaining_queued` + finalize) to the job's NEXT chunk checkpoint (`run_one_chunk`'s `CANCELLED` branch), but `run_due_broadcasts`'s stuck-sweep only scans `Status.key == "SENDING"` - if the chunk chain has already died (crashed process, lost Celery task) before that checkpoint runs, the broadcast stays CANCELLED with `queued`/unclaimed recipients and `finished_at = NULL` forever. Needs its OWN "stuck CANCELLED" branch (not a bare extension of the SENDING sweep, whose `has_dispatchable_recipients` early-`continue` is wrong here - those queued rows are abandoned, not "real work still queued"): unconditionally `repo.skip_remaining_queued(reason="cancelled")` then `reconcile_broadcast` + `finalize_broadcast`. | [sprint-4/29-omnichannel-broadcasts](../plans/sprint-4/29-omnichannel-broadcasts.md) | Medium | Open |
| BL-SS-119 | **`start_scheduled_broadcast`'s SENDING claim is not atomic with its `background_jobs` row creation** - `BroadcastRepository.claim_status` commits INTERNALLY as part of its own atomic-claim pattern (mirrors `BackgroundJobRepository.claim`), so the claim is already durable before `JobService.create()` runs; if `handler_for(type)` ever raises (e.g. a process that never ran the module boot hook, so `omnichannel.broadcast_send` isn't registered), the broadcast is left stuck SENDING with `job_id NULL` (the 15-minute stuck-sweep in `run_due_broadcasts` is the only recovery - mitigation already in place, just slow). Real fix: give `claim_status` an optional `commit: bool = True` and pass `commit=False` from `start_scheduled_broadcast` ONLY (the other two call sites - `send()`'s schedule branch, `cancel()` - keep committing immediately); re-verify `test_start_scheduled_broadcast_claim_race_only_one_wins` (AC-BRD-42) still holds under the new commit timing. Pinned by `test_start_scheduled_broadcast_claim_not_atomic_with_job_creation_bl_ss_119` (flip its assertion once fixed). | [sprint-4/29-omnichannel-broadcasts](../plans/sprint-4/29-omnichannel-broadcasts.md) | Medium | Open |
| BL-SS-120 | **A `broadcasts.read`-only role sees an empty list with no explanation** - the Broadcasts list resolves its workspace via `GET /omnichannel/workspaces` (gated `workspaces.read`); a role holding only `broadcasts.read` never resolves a workspace id and the page renders an empty list, no error (same class as **BL-SS-081** for contacts). Either grant `workspaces.read` alongside `broadcasts.read` in the tenant Admin default, or surface the 403/empty-workspace state explicitly on the Broadcasts page. | [sprint-4/29-omnichannel-broadcasts](../plans/sprint-4/29-omnichannel-broadcasts.md) | Low | Open |
| BL-SS-121 | **Sorento: 202 + background worker for large document ingest batches (their side, cross-repo note)** - prod incident 2026-09-06: a 1,000-record `purchase_order` ingest with per-record supplier back-create ran past Sorento production nginx's 60s proxy timeout and returned 504, so the ESB recorded a push failure for a batch Sorento may have partly committed. ESB-side mitigation shipped in `fix/sorento-batch-size`: records per ingest POST = `AUTOCOUNT_SINK_BATCH_SIZE` (default 200, ceiling 1,000 = Sorento's per-request limit, read at call time). The durable fix is Sorento's: accept a large batch with `202 Accepted` + a job id and process it in a worker, with the ESB polling the verdicts (or Sorento raising the proxy timeout for the ingest routes only). Until then a 504 on a batch is AMBIGUOUS (committed or not) - the ESB's next pass re-offers the rows by hash, which is safe only because ingest is idempotent per `source_ref`. Track Sorento's answer here. | [sprint-5/03-autocount-bulk-document-load](../plans/sprint-5/03-autocount-bulk-document-load.md) | Medium | Open |
| BL-SS-122 | **Teams form Members/Leads picker is capped at the first 200 tenant users** - `use-team-form.tsx` loads `userService.list({pageSize: 200})` once because `MultiSelect` (and `SearchSelect`) have no remote-search / async-options mode; a tenant with more than 200 users silently loses the tail of the picker. Add an async-options mode to `MultiSelect` (search-as-you-type against `GET /users?search=`) and switch the Teams form (and any other user picker) onto it. Surfaced by plan 28 review round 1, nit 17. | [sprint-4/28-teams-core-and-omnichannel-assignment](../plans/sprint-4/28-teams-core-and-omnichannel-assignment.md) | Low | Open |
| BL-SS-123 | **Saved views: prune unresolvable ids at expansion so a view degrades to no-scope, not no-results** - `InboxViewService.expand()` passes stored `teamIds` (and the pre-existing `tagIds`/`channelIds`) straight through; an id whose team/tag/channel was since deleted narrows the tenant-scoped `IN` to zero rows, so the view silently shows an empty inbox. Drop ids that no longer resolve (tenant-scoped) at expansion time and surface a one-line "some filters no longer apply" hint, keeping the save-time 422 as is. Surfaced by plan 28 review round 2 (post-approval read). | [sprint-4/28-teams-core-and-omnichannel-assignment](../plans/sprint-4/28-teams-core-and-omnichannel-assignment.md) | P2 | Open |
| BL-SS-124 | **Orphan `team_assignment_settings` rows survive a core team delete** - the reference guard only counts `contacts.assigned_team_id`, so a team with a configured strategy but no assigned threads deletes cleanly and leaves its `app_omnichannel.team_assignment_settings` row behind (harmless - `list_settings_for_display` only renders ACTIVE resolvable teams - but it accumulates). Add cleanup in the `teams.delete` deferred handler (module hook via the capability seam) or a periodic sweep. | [sprint-4/28-teams-core-and-omnichannel-assignment](../plans/sprint-4/28-teams-core-and-omnichannel-assignment.md) | P2 | Open |
| BL-SS-125 | **Rail entry for an inactive "My teams" team is indistinguishable from an active one** - `GET /teams/mine` carries `isActive` but the inbox rail renders every entry identically (a read filter is still legitimate on an inactive team, D-A8-20). Add a muted "(inactive)" affordance on the rail entry and the mobile `SearchSelect` option. | [sprint-4/28-teams-core-and-omnichannel-assignment](../plans/sprint-4/28-teams-core-and-omnichannel-assignment.md) | P3 | Open |
| BL-SS-126 | **Full-suite timer flakes in `timezone-card.test.tsx` and `resource-form.deferred` (D5 class)** - both pass in isolation and fail 1-2 times per full vitest run under CPU load (real `setInterval` poll ticks racing tiny windows / 5s test timeouts). Apply the D5 fix pattern (fake timers, or a window that cannot expire inside the assertion) to both. | [sprint-4/28-teams-core-and-omnichannel-assignment](../plans/sprint-4/28-teams-core-and-omnichannel-assignment.md) | P3 | Open |
| BL-SS-127 | **Plan-27 default agent role: the Inbox renders nothing without `workspaces.read`** - an agent role holding `conversations.read/assign` but not `workspaces.read` never resolves a workspace (`useActiveWorkspace`) and sees an empty inbox with no error (same class as BL-SS-081 for Contacts). Review the default agent role shape: either bundle `workspaces.read` into it or make the inbox surface the 403 explicitly. | [sprint-4/28-teams-core-and-omnichannel-assignment](../plans/sprint-4/28-teams-core-and-omnichannel-assignment.md) | P2 | Open |
| BL-SS-128 | **`teamService.remove()` is unreferenced by production frontend code after the deferred-delete switch** - row/bulk/form delete ride `teams.delete` through the pending-actions engine (review round 1, finding 3), so only `team-service.mock.test.ts` calls `remove()`. Ruling (plan 28 §8): KEEP it - the synchronous `DELETE /teams/{id}` (204 / 409 `team_in_use`) stays the API contract (covered by `test_teams.py`, used by the gateway-less admin scripts and the mock's own 409 test); revisit if the sync route is ever retired. | [sprint-4/28-teams-core-and-omnichannel-assignment](../plans/sprint-4/28-teams-core-and-omnichannel-assignment.md) | P3 | Open |
| BL-SS-129 | **`_make_tag_context` picks an arbitrary added tag when several are added in one patch and the trigger has no `tagId` configured** - `workflow_nodes.py` uses `sorted(delta)[0]` (deterministic but arbitrary) for `trigger.tagId`/`trigger.tagName` when a `ContactProfileService.patch` call adds more than one tag at once and the trigger's own `tagId` filter is unset. Emit the full added-tag set as `trigger.tagIds` instead of picking one, so an author isn't silently missing tags. Main is at BL-SS-117 as of this branch's cut; A4/A8 are expected to claim ids before this merges. | [sprint-4/31-omnichannel-workflow-parity](../plans/sprint-4/31-omnichannel-workflow-parity.md) | Low | Open |
| BL-SS-130 | **Evidence-dir naming mismatch** - AC-WFP-39 names `31-evidence/a5a/`; the actual agent-browser runs live at `31-evidence/{S0,S3,E2E}/`. Reconcile the AC wording to the folder convention the tester's later slices settle on (or vice versa). | [sprint-4/31-omnichannel-workflow-parity](../plans/sprint-4/31-omnichannel-workflow-parity.md) | Low | Open |
| BL-SS-131 | **Merge checklist: both the core AND module Alembic chains need re-parenting, plus the manifest version bump and an A8 fold - enumerated (review round 1) so nothing is missed at the actual merge.** (1) **Core**: `alembic/versions/wf_run_pause_s31_park_resume.py` (`revision="wf_run_pause_s31"`) currently chains `down_revision = "b7c1d2e3f4a5"`; `origin/main`'s core head is `teams_core_s428` (from A8) - re-point it there (`workflows_http_s31` already chains onto `wf_run_pause_s31` and needs no further edit). (2) **Module**: `0013_omni_workflow_fires` currently chains onto `0010_omni_contacts_module`; `origin/main`'s module head is `0012_omni_team_assignment` (A8) - re-chain `0013`'s `down_revision` onto `0012`, keeping `0014_omni_round_robin_cursor` -> `0015_omni_workflow_waits` -> `0016_omni_business_hours` in order behind it. (3) **Manifest**: `modules/omnichannel/manifest.json` bumps `0.4.5 -> 0.7.0` (or whatever the actual post-A4/A8/this-branch sequence lands on). (4) **A8 fold**: this branch's `0014_omni_round_robin_cursor` (`omnichannel_assign_conversation` + `workspaces.round_robin_cursor`) must be reconciled against whatever A8 (teams core / omnichannel assignment) already landed on main under the same name/columns before re-chaining - a naive re-chain could double-declare the same column/table. Re-run `alembic upgrade head` + `run_module_migrations` on a REAL Postgres after re-chaining (conftest's `create_all` will not catch a broken chain). | [sprint-4/31-omnichannel-workflow-parity](../plans/sprint-4/31-omnichannel-workflow-parity.md) | Medium (merge-blocking) | Open |
| BL-SS-132 | **AC-WFP-37 server-side permission enforcement is not implemented** - the API does not reject a workflow create/update/publish/run that carries a node type the caller lacks permission for; today's rejection is only the incidental "unregistered node type" gate (B-4 in review round 1), which is a different mechanism with a different failure mode. Deferred to S5, alongside the permission-gated node types (`workflows.http`, etc.) it is meant to protect. **CLOSED in S5**: `WorkflowService.assert_node_permissions` (renamed from `assert_code_permitted` in review round 1 - the old name no longer matched what it does; a back-compat alias is kept) now walks `required_node_permissions(doc)` - every distinct `ActionDef.permission` a node in the doc declares, not just `code.run`'s hardcoded `workflows.code` - and gates create/update/publish/manual-run/debug generically. `code.run` itself now carries `permission="workflows.code"` on its `ActionDef` (previously implicit); `http.request` carries `permission="workflows.http"`. A future gated ActionDef needs no core change. | [sprint-4/31-omnichannel-workflow-parity](../plans/sprint-4/31-omnichannel-workflow-parity.md) | Medium | Closed (S5) |
| BL-SS-133 | **`_resolve_active_channel_id` tie-break + `ContactChannelIdentity` fallback** - the S3 review's SF-1 fix resolves `trigger.channelId` from the contact's most recent channel-bound MESSAGE; a contact with two channel identities and simultaneous messages has an arbitrary tie-break, and a contact with an identity but zero channel-bound messages resolves `None`. Fall back to `ContactChannelIdentity` (most recently seen) and make the tie-break explicit. | [sprint-4/31-omnichannel-workflow-parity](../plans/sprint-4/31-omnichannel-workflow-parity.md) | Low | Open |
| BL-SS-134 | **AC-WFP-05 live-port evidence re-run in S6** - the two-port rendering (`answer`/`timeout`, `inside`/`outside`) was evidenced in S0 against the MOCK catalog, before the backend `ActionDef`s existed; the registry gate (B-4) means the S3 live run could not show those nodes at all. Re-record the port-wiring evidence against the live palette in S6, and note the gap in the Test Execution Report until then. **CLOSED in S6**: live-wired both branching actions against the real registry (`omnichannel.ask_question` answer/timeout, `omnichannel.business_hours` inside/outside), screenshot `31-evidence/S6/19-fully-wired-1280.png` shows the full published graph with labelled edges exactly like the IF node's true/false convention. | [sprint-4/31-omnichannel-workflow-parity](../plans/sprint-4/31-omnichannel-workflow-parity.md) | Low | Closed (S6) |
| BL-SS-135 | **`http.request` has no `code_authorized_by`-style stamp for the event/scheduled-trigger path** - `workflows.http` is enforced at create/update/publish/manual-run/debug (generalized `assert_node_permissions`), but a `workflow.trigger`/`schedule.cron`/`entity.*`-fired run of an already-published HTTP-bearing graph does not re-check the permission (mirrors every OTHER publish-time-only gate in the engine, e.g. `storage.*`/`entity.update` connections - `code.run` is the one exception, because its stamp ALSO captures runner-health-at-publish-time, which a plain permission check can't). If a tenant revokes `workflows.http` after publish, an event-triggered run of that graph still fires until the next publish. **Reviewer caveat (round 1)**: `assert_node_permissions` returns early when `actor is None` (the system/seed publish path - see `entity_events.create_run_for_event`), so a system-authored publish of an HTTP-bearing graph skips the gate entirely AND produces no stamp of any kind - unlike `code.run`, which would at least fail closed via its separate `code_authorized_by` mechanism on that same path. Add a `http_authorized_by`-style version stamp (or a generic per-permission stamp set) if this drift window needs closing. | [sprint-4/31-omnichannel-workflow-parity](../plans/sprint-4/31-omnichannel-workflow-parity.md) | Low | Open |
| BL-SS-136 | **Business hours: a window with `from == to` is rejected as malformed** - AC-WFP-55 says "an end before a start returns 422" while §5.4 defines overnight windows as `to <= from` (explicitly VALID, spans midnight). S5 resolved the overlap by treating ONLY the degenerate `from == to` case as the 422 ("end time must differ from the start time"); a genuine `to < from` (e.g. `22:00`-`02:00`) is accepted as overnight. Confirm this reading with the user/reviewer and reconcile the UAC wording if it disagrees. **Related tension (review round 1, see BL-SS-137)**: AC-WFP-51's "same-contact runs never interleave" is not literally true across a park - a parked run releases its serialized lease, so a second same-key run CAN start while the first is waiting; that second run's own Ask node then hard-fails with "This contact already has an open question." (AC-WFP-44's designed behaviour). The code is correct; the AC-51 wording and BL-SS-137 are what need reconciling. | [sprint-4/31-omnichannel-workflow-parity](../plans/sprint-4/31-omnichannel-workflow-parity.md) | Low | Open |
| BL-SS-137 | **Serialized lease vs a parked run - no "block the correlation scope while parked" mode** - a parked run is `waiting`, not `running`, so the serialized drain's `_live_running_run_id` check happily starts the NEXT pending run in the same correlation scope while one is parked (plan §7 risk register). Correct for the plain Ask flow (the answering message is CONSUMED, so no second run is ever created for it) but a genuine semantic edge for a workflow with both an Ask node and a second trigger path feeding the same correlation key while one instance is waiting - see the AC-WFP-51/AC-44 tension noted at BL-SS-136. Pinned by `test_a_parked_run_does_not_block_its_correlation_scope` (green - documents current behaviour, does not gate it). Add an opt-in mode that treats `waiting` as scope-blocking (like `running`) for workflows that want strict single-instance-per-key semantics even across a park. | [sprint-4/31-omnichannel-workflow-parity](../plans/sprint-4/31-omnichannel-workflow-parity.md) | Low | Open |
| BL-SS-138 | **`http.request` header secrets should be sourceable from `connections.credentials_json` (Fernet-encrypted), not typed as literal config** - the run-trace boundary now redacts every `redacted=True` field's value (review B1, closes the literal-secret leak into `WorkflowRunNode.input_json`), but the workflow's OWN document (`draft_definition_json`, published `workflow_versions.definition_json`, every run's `definition_snapshot_json`) still stores the header value as authored - consistent with how the rest of the graph treats config, but it means a literal API key typed into an HTTP header sits in plaintext in three places, readable by anyone with `workflows.read`/`workflows.manage`. Give `http.request` a way to reference a connection's encrypted credential (mirrors how `email.send`/`storage.*` already resolve `requires_connection` rather than embedding secrets in node config) instead of a literal header value. | [sprint-4/31-omnichannel-workflow-parity](../plans/sprint-4/31-omnichannel-workflow-parity.md) | Medium | Open |
| BL-SS-139 | **A running job orphaned by a deploy drain / crash stayed `running` forever and paused its task** - prod incident 2026-09-07 16:59Z: a PO sync job was mid-run when the blue/green deploy stopped the old colour (30s drain); `background_jobs` stuck `running`, `ac_sync_run` left open, every scheduler tick skipped the task ("still in progress"), and after 60 minutes `scheduler.py`'s `STALE_JOB_AFTER` only wrote a `JOB_STUCK` skip and paused - no heartbeat, no startup sweep, manual SQL was the only fix. Fixed by `fix/job-lease-orphan-sweep`: `background_jobs.heartbeat_at` (core revision `bgjob_heartbeat_orphan`) stamped before/after every page and per push chunk in its own short transaction (0 rows = a fence, the run stops without overwriting a terminal status); `JobService.fail_orphaned_running_jobs` (`background_job_orphan_after_minutes`, default 15, floor 5) fails the orphan (only a type declaring `JobHandlerDef.heartbeats=True`) and fans out to module `on_job_orphaned` hooks under a savepoint each (autocount closes the open run row, tenant-scoped; staged rows untouched); runs at startup (`background_job_orphan_sweep_on_startup`) and from the scheduler, which sweeps a stale RUNNING in-flight job and proceeds only when it actually failed it. | [storage & background jobs](../engineering/storage-and-background-jobs.md) | High | Fixed by fix/job-lease-orphan-sweep |
| BL-SS-140 | **`test_auto_push_one_failing_chunk_keeps_the_other_chunks_verdicts_at_any_concurrency` (round6b) flaked under concurrency 3** - `_active_task_staged_supplier_job`'s 30 staged rows all landed on the SAME `created_at` (SQLite `func.now()` second-resolution, all 30 rows inserted in one flush), so `list_pending_for_entity`'s deterministic-looking `created_at ASC, id ASC` order tie-broke on a per-run-random UUID; the test's "the second chunk" assumption (the chunk containing `AED_VSOFT:15`) only held when the fixture's insertion order survived into query order. **CLOSED (`a5246f37`)** - the fixture now stamps `created_at = base + i seconds` in `refs` order, the same shape `test_autocount_push_marks_per_chunk.py`'s own `_stage` helper already used; 10/10 green in a row. | [fix/push-marks-per-chunk](../plans/sprint-5/03-autocount-bulk-document-load.md) | P2 | Closed |
| BL-SS-141 | **Fixed by fix/push-marks-per-chunk** - prod finding 2026-09-07 (Sorento `api_call_log`, sales_order task, 6h window): ~657 ingest requests x 200 = ~131k offers for 23k distinct SOs (~5 offers per document); separately, `sales_order` staged 148,068 = PUSHED 20,000 / STAGED 128,068 with most runs recording `pushed_count 0` and `ac_sync_run.error NULL` despite Sorento answering every one of 638 requests a clean 200 with `created`/`updated` verdicts (0 failed, 0 retryable) - root cause: `SyncService.auto_push` handed up to 5,000 STAGED rows to ONE `write_batch` call, which chunked at 200 internally and returned verdicts only once EVERY chunk succeeded; a single chunk-level 502 from Sorento's own nginx (upstream momentarily unreachable) discarded every OTHER chunk's already-delivered verdict too, and the summary's `error` was never persisted onto the run row at all. Fixed: `write_batch`/`delete_batch` gained a per-chunk `on_chunk` callback so marks commit as each chunk resolves (a later chunk's fault can no longer undo an earlier one's delivery); a TRANSIENT 5xx (502/503/504 only, `settings.autocount_sink_retry_attempts`, default 3, bounded backoff) retries in place and a chunk that still fails after exhausting attempts fails ONLY that chunk (the loop continues to later chunks) - a plain 500/4xx/anchor/transport fault still stops the whole push, unchanged; `ac_sync_run.requests`/`requests_failed`/`first_failure` (migration `0015_autocount_run_requests`) persist the push's own request accounting onto the run row (`run.error` now also carries the summary's error even on a non-truncated run); `ac_staged_record.last_offered_at` (same migration) + `list_pending_for_entity`'s NULLS-FIRST ordering stop a permanently-`retryable` head from starving fresh rows now that a partial-batch outcome is common. Live prod numbers above are what motivated the fix; re-verify against a live 24h Sorento window once deployed. This lane is DB + API diagnostics only (`ac_sync_run.requests`/`requests_failed`/`first_failure` land on the row and the wire schema, nothing renders them) - see BL-SS-142 for the Runs list surface. | [sprint-5/03-autocount-bulk-document-load](../plans/sprint-5/03-autocount-bulk-document-load.md) | High | Closed |
| BL-SS-142 | **The Runs list has no surface for the new push-request accounting** - `ac_sync_run.requests`/`requests_failed`/`first_failure` (fix/push-marks-per-chunk, migration `0015_autocount_run_requests`) and `SyncRunItem`'s matching wire fields exist end to end (DB + API), but no frontend column/detail panel shows them - an operator reading the Runs list still cannot tell a chunk-level push fault happened without a `psql`/API call, the exact gap BL-SS-141 fixed for the RUN ROW itself. Add a "requests" column (or a compact "N/M failed" badge) and surface `firstFailure` in the run detail view. | [sprint-5/03-autocount-bulk-document-load](../plans/sprint-5/03-autocount-bulk-document-load.md) | P2 | Open |
| BL-SS-143 | **Sorento `sinkConcurrency` has no "return to unset" path, and no PATCH-level provider config validation** (feat/sink-concurrency-ui) - (1) the connection form's push-concurrency field is a plain select (its options are "1".."4", never blank), so an operator who once stored an explicit value has no affordance to clear it back to "follow the platform default"; a stored `"1"` on a connection pins it even after the platform default later rises. (2) `IntegrationProvider` exposes `fields()`/`test()` only - there is no PATCH-level hook that validates a field's stored value against its own `options`, so an out-of-range or hand-edited `config_json` value ("0", "9", "abc") is caught only sink-side, at push time, by `SorentoSink._resolve_concurrency` falling back to the platform default and logging one warning; the operator gets no immediate save-time feedback that their input was ignored. Both would need either a dedicated "Use platform default" option/affordance on the select, or a generic provider-config-validation seam on the connection PATCH endpoint - out of scope for this lane. | [sprint-5/02-autocount-document-mapping-sorento-addendum](../plans/sprint-5/02-autocount-document-mapping-sorento-addendum.md) | P3 | Open |
| BL-SS-144 | **`PO.UDF_ShipOrder` per-company routing of non-SPO-numbered documents is not built** (feat/spo-container-number) - some AutoCount companies carry a `PO.UDF_ShipOrder` UDF column (`'T'`/`'F'`) that lets an operator flag a purchase order as a shipping-order booking WITHOUT relying on the `SPO-` `DocNo` prefix the generic preset filters on today (`_SPO_FILTER_FORMULA`/`_PO_FILTER_FORMULA`). The live company this lane's `container_number` fix was built against (AED_SORENTO) has 0 such rows today, so this is documentation only, not a shipped feature: a company that DOES use the UDF would need a per-company `filterFormula` override (already an editable task field) plus, if it wants the header selected too, a customised header query - the generic `_PO_HEADER_QUERY` deliberately selects no per-company UDF column (S1 of this lane). Revisit if a live company's SPO/PO split turns out to need it. | [sprint-5/02-autocount-document-mapping-sorento-addendum](../plans/sprint-5/02-autocount-document-mapping-sorento-addendum.md) | P3 | Open |
| BL-SS-145 | **A purchase_order task's stored header query now drifts by CREATION DATE, not by operator edit** (feat/spo-container-number review round 1) - `_PO_HEADER_QUERY` (shared by `PO_PRESET` and `SPO_PRESET`) now selects `h.Ref AS Ref`; the container_number backfill (module Alembic 0016) rewrites this for EXISTING `shipping_order` tasks only, never `purchase_order` ones (`PO_PRESET.header` maps no field from `Ref`, so fetching it for a PO is harmless dead weight, never wired to `container_number`). A pre-existing PO task therefore keeps its pre-Ref stored query forever, while a brand-new PO task (created via "Use preset" after this deploy) selects `Ref` from day one - two byte-different "the preset, unmodified" states coexist with no backfill reconciling them. Harmless today (nothing reads the extra column for PO), but a FUTURE PO-family byte-identity backfill (mirroring this lane's shipping_order one) would misclassify an old, un-backfilled PO task as "customised" purely because it predates this deploy. Note this drift if that backfill is ever written. | [sprint-5/02-autocount-document-mapping-sorento-addendum](../plans/sprint-5/02-autocount-document-mapping-sorento-addendum.md) | P3 | Open |
| BL-SS-146 | **Widen the line fingerprint sweep's aggregate set with a money-bearing column** (feat/line-fingerprint-sweep) - the sweep's fingerprint query hashes `LineCount`/`QtySum`/`TransferedSum`/`MaxDtlKey`; a same-line-count, same-quantity edit that only changes price or discount (e.g. `SubTotal`) leaves all four identical and is invisible to the sweep, waiting for the next daily reconcile instead. Add `SubTotalSum` (or another suitable money aggregate) to `_SO_FINGERPRINT_QUERY`/`_PO_FINGERPRINT_QUERY` if that gap needs closing sooner than reconcile. | [sprint-5/03-autocount-bulk-document-load](../plans/sprint-5/03-autocount-bulk-document-load.md) | Low | Open |
| BL-SS-147 | **Migration Retry / Complete-anyway have no real backend route for `omnichannel.respondio_migration`** - plan 33 S0's action registry deferred both to the generic `jobs.abort`/`jobs.complete`/`/jobs/{id}/retry`/`/jobs/{id}/complete` seams, which S6 discovered are ALL hard-scoped to `StorageMigrationService` (`app/deferred_actions/handlers.py`, `app/api/v1/jobs.py`) - wiring a respond.io migration job id through them would 404 or silently act on the wrong job. S6 dropped both actions from `use-migration-actions.tsx` rather than ship a broken wire; the supported path today for a failed/needs_review job is to create a FRESH job with the same mapping (`migration_refs` makes it a no-op over what already landed). Build a dedicated `POST /omnichannel/migration/jobs/{id}/retry` (re-enqueue the same payload from its last cursor) if operators need in-place retry. | [sprint-4/33-omnichannel-respondio-migration](../plans/sprint-4/33-omnichannel-respondio-migration.md) | Medium | Open |
| BL-SS-148 | **Migration job-history search/sort/filter runs in Python over the tenant's full job set, not a SQL query** - `MigrationService.list_jobs` (S6, AC-MIG-02) fetches every one of a tenant's `omnichannel.respondio_migration` rows (capped at `MAX_LIST_SCAN_JOBS = 5000`) and searches/sorts/filters in memory, because `spaceLabel`/`workspaceName`/`mode` live inside `payload_json` and a portable cross-dialect JSON-path clause (this suite runs on in-memory SQLite, production on Postgres) was not worth building for a list bounded by construction (`migration_in_progress` forbids more than one non-terminal job per workspace). Revisit with real DB-level filtering if BL-SS-121 (incremental top-up runs) ships and per-tenant job counts grow past the cap. | [sprint-4/33-omnichannel-respondio-migration](../plans/sprint-4/33-omnichannel-respondio-migration.md) | Low | Open |
| BL-SS-149 | **Re-verify `SOURCE_TO_CHANNEL_TYPE` against the vendor's live channel catalog on a schedule** - `modules/omnichannel/respondio/channel_map.py` is the ONLY place a respond.io channel `source` value maps onto a Foundryx `channel_type`; a new source value on the vendor's side currently falls through to "Skip" silently (an underivable identity is dropped and reported, never fabricated - D-A6-10) rather than warning that the map itself may be stale. Add a scheduled check (or a preflight warning) comparing the map's key set against a saved vendor catalog snapshot. | [sprint-4/33-omnichannel-respondio-migration](../plans/sprint-4/33-omnichannel-respondio-migration.md) | Low | Open |
| BL-SS-150 | **Fixed by plan 32 S6 in the same commit** - the channel detail page (`use-channel-form.tsx`) rendered "Channel not found" for EVERY non-WhatsApp channel because `Promise.all([channelService.get(id), channelService.getProfile(id)])` failed the whole page the instant `getProfile` returned its EXPECTED `409 channel_type_unsupported` (D-A7-17); the channel fetch itself always succeeded. Reproduced on the seeded `chn-demo-ig` channel too, so it predates S6 (landed in S3, never live-verified against a real non-WhatsApp channel until this slice's E2E run). Fixed: the profile fetch now runs independently and degrades to `profile: null` on any error; only the channel fetch failing is a real 404. | [sprint-4/32-omnichannel-channels-messenger-instagram](../plans/sprint-4/32-omnichannel-channels-messenger-instagram.md) | High | Fixed (S6) |
| BL-SS-151 | **`omnichannel.send_message`'s template mode has no publish-time refusal for a non-WhatsApp target** - plan 32 S6's brief asked for "refuse template mode off-WhatsApp at publish via the registry show_when/validation", but the action has no `channelId` field (it resolves the channel implicitly via `_channel_for_contact`, often from a dynamic `{{ trigger.contact.id }}`), and the core `validate_definition`/`parse_definition` publish gate is deliberately DB-less (used by create/update/publish alike). The RUNTIME refusal is correct and tested (`messaging_policy.assert_kind_supported` -> `PolicyRejected("kind_not_supported", ...)`, surfaces as a clear `ActionError` on the run), but a publish-time static check would need either a `channelId` field added to the action (breaking today's implicit-channel UX) or a DAG-aware validator that infers the channel from an upstream `message_received`/`conversation_opened` trigger's fixed `channelId` config - out of scope for S6. | [sprint-4/32-omnichannel-channels-messenger-instagram](../plans/sprint-4/32-omnichannel-channels-messenger-instagram.md) | Low | Open |
| BL-SS-152 | **RESOLVED by the independent tester pass (2026-09-07)** - both deferred live-verifications now confirmed: (1) a dedicated tenant + a `channels.read`-only "Viewer" role user sees the Channels list but NO "Connect channel" control (screenshots `32-evidence/E2E/08-viewer-no-connect-control-{1280,375}.png`); (2) a real workflow with `message_received` + `channelType=Messenger` was built by real drag/drop, activated (the separate `is_active` switch under the workflow's own Settings tab, not part of Publish - a click-path gotcha worth noting for future workflow E2E, not a plan-32 defect) and published, then a real Messenger webhook POST produced exactly one `Success` run in the Logs tab while a same-instant WhatsApp webhook POST produced none (screenshot `32-evidence/E2E/28-workflow-logs-messenger-fired-1280.png`). See `32-omnichannel-channels-messenger-instagram-test-report.md` (independent tester report) AC-CHN-58/AC-CHN-62/AC-CHN-11 rows. | [sprint-4/32-omnichannel-channels-messenger-instagram](../plans/sprint-4/32-omnichannel-channels-messenger-instagram.md) | Low | Closed |
| BL-SS-153 | **Messenger / Instagram message templates** (generic, button, receipt, media templates) as a first-class composer + workflow surface - plan 32's §8 candidate; A7a deliberately ships send-only WhatsApp templates (broadcasts stay WhatsApp-only, D-A7-21). | [sprint-4/32-omnichannel-channels-messenger-instagram](../plans/sprint-4/32-omnichannel-channels-messenger-instagram.md) | Medium | Open |
| BL-SS-154 | **Persistent menu, ice breakers and greeting text** per Messenger / Instagram channel (a Profile-tab equivalent for these types) - not modelled this slice. | [sprint-4/32-omnichannel-channels-messenger-instagram](../plans/sprint-4/32-omnichannel-channels-messenger-instagram.md) | Medium | Open |
| BL-SS-155 | **Handover protocol** (pass thread control to or from the Page inbox / another app) - not modelled this slice. | [sprint-4/32-omnichannel-channels-messenger-instagram](../plans/sprint-4/32-omnichannel-channels-messenger-instagram.md) | Low | Open |
| BL-SS-156 | **Cross-channel contact merge**: fuse a WhatsApp contact and a Messenger / Instagram contact into one profile, moving identities (pairs with plan 25's G24 / B2) - explicitly NOT built this slice (D-A7-4, same person on two channel types stays two contacts). | [sprint-4/32-omnichannel-channels-messenger-instagram](../plans/sprint-4/32-omnichannel-channels-messenger-instagram.md) | High | Open |
| BL-SS-157 | **Message tags beyond `HUMAN_AGENT`** (`CONFIRMED_EVENT_UPDATE`, `POST_PURCHASE_UPDATE`, `ACCOUNT_UPDATE`) and one-time notifications, if and when Meta's roadmap settles. | [sprint-4/32-omnichannel-channels-messenger-instagram](../plans/sprint-4/32-omnichannel-channels-messenger-instagram.md) | Low | Open |
| BL-SS-158 | **Import sweep**: point the eight `from ..adapters.whatsapp_cloud import get_adapter` sites at `adapters/__init__.py` and drop the re-export (D-A7-9). | [sprint-4/32-omnichannel-channels-messenger-instagram](../plans/sprint-4/32-omnichannel-channels-messenger-instagram.md) | Low | Open |
| BL-SS-159 | **Instagram Login** (IG-direct, no linked Facebook Page) as a second connect topology - this slice's connect flow only offers page-linked IG accounts (AC-CHN-44). | [sprint-4/32-omnichannel-channels-messenger-instagram](../plans/sprint-4/32-omnichannel-channels-messenger-instagram.md) | Medium | Open |
| BL-SS-160 | **Broadcasts on Messenger / Instagram** once a compliant outbound mode exists (pairs with BL-SS-157) - broadcasts stay WhatsApp-only this slice (D-A7-21; the broadcast channel picker now filters to WHATSAPP, security review round 1 should-fix). | [sprint-4/32-omnichannel-channels-messenger-instagram](../plans/sprint-4/32-omnichannel-channels-messenger-instagram.md) | Low | Open |
| BL-SS-161 | **Instagram story replies, story mentions and media shares** as first-class message types instead of `UNSUPPORTED` placeholders (AC-CHN-41 stores them as placeholders with payload preserved, by decision). | [sprint-4/32-omnichannel-channels-messenger-instagram](../plans/sprint-4/32-omnichannel-channels-messenger-instagram.md) | Medium | Open |
| BL-SS-162 | **Outbound reactions on Messenger / Instagram** (inbound already lands on the existing reaction path; the capability row declares `outboundReaction: false` for both types this slice). | [sprint-4/32-omnichannel-channels-messenger-instagram](../plans/sprint-4/32-omnichannel-channels-messenger-instagram.md) | Low | Open |
| BL-SS-163 | **Per-page rate-limit awareness** (Meta's calls-per-page budget) surfaced in the channel Configuration tab, pairing with BL-SS-082. | [sprint-4/32-omnichannel-channels-messenger-instagram](../plans/sprint-4/32-omnichannel-channels-messenger-instagram.md) | Low | Open |
| BL-SS-164 | **A channel CATALOG screen** (respond.io's card grid) once more than three types ship - A7a deliberately uses a `SearchSelect` step instead (F5). | [sprint-4/32-omnichannel-channels-messenger-instagram](../plans/sprint-4/32-omnichannel-channels-messenger-instagram.md) | Low | Open |
| BL-SS-165 | **`channel_type` as a DB-level enum** plus a migration for the pruned `DOUYIN` / `XIAOHONGSHU` values, should any row ever carry them (F6). The frontend side of this gap (`CHANNEL_CAPABILITIES[...]` throwing on an unmodelled type) was fixed in security review round 1 (`channelCapabilities()` now falls back to a neutral `UNKNOWN` record) - this row is the remaining backend DB-enum work. | [sprint-4/32-omnichannel-channels-messenger-instagram](../plans/sprint-4/32-omnichannel-channels-messenger-instagram.md) | Low | Open |
| BL-SS-166 | **Live HTTPS consumer-webhook delivery cannot be exercised locally** - the platform's SSRF host guard refuses `http://` callback URLs even under `ENVIRONMENT=development` (`Callback URL must use https://.`), so an `agent-browser`/tester pass with no local TLS proxy cannot capture a real delivered consumer-webhook payload to independently confirm AC-CHN-56/57-style claims end to end. The independent tester pass for plan 32 substituted a code-path + wire-shape proof (`inbound_service.py`'s `enqueue_event` payload is the SAME `MessageItem.model_dump()` the internal API returns, confirmed to carry `channelType`) instead of a live capture. A local TLS dev-receiver recipe (or a documented dev-mode carve-out for `http://localhost`) would close this gap for future E2E passes touching consumer webhooks. | [sprint-4/32-omnichannel-channels-messenger-instagram](../plans/sprint-4/32-omnichannel-channels-messenger-instagram.md) | Low | Open |
| BL-SS-167 | **The canned dev-safe Meta page pool is service-wide and exhausts after a couple of E2E passes** - `_DEV_PAGES` (`pg-701`/`702`/`703`, only `pg-701`/`pg-702` carry a linked IG account) backs `list_pages` for EVERY tenant in dev mode, and connect uniqueness (D-A7-3) is service-wide, not per-tenant. After the S6 coder's own evidence run connected `pg-702`+`pg-703` (Messenger) and `pg-701`'s IG account on the `default` tenant, the independent tester pass's dedicated tenant found the Instagram professional-account picker offered ZERO accounts ("No matches.") purely from residue - both linkable IG accounts were already connected elsewhere. Not a code defect (confirmed working as designed - the exclusion IS service-wide per AC-CHN-34), but it silently degrades future connect-wizard E2E passes against the same Postgres instance. Worth either a periodic cleanup script for `chn-demo`-adjacent test channels, or widening `_DEV_PAGES` so more than one fresh Instagram connect can happen per environment lifetime. | [sprint-4/32-omnichannel-channels-messenger-instagram](../plans/sprint-4/32-omnichannel-channels-messenger-instagram.md) | Low | Open |
| BL-SS-168 | **Watermark/`mids[]` receipt bulk-apply commits once per targeted message, not once per webhook event** - `InboundService._handle_status` loops `_resolve_status_targets` (which can return every outbound row on a thread for a single Messenger/Instagram `read` watermark) and calls `_apply_receipt` per message, and `_apply_receipt` itself does `self.db.commit()` before its broadcast/realtime/consumer-webhook fan-out. A busy thread with N outstanding outbound messages turns one inbound webhook into N separate commits (N round trips, N fan-outs queued individually) instead of one batch UPDATE + one commit + one fan-out pass. Correct today (rank-forward guard still applies per row, nothing double-applies) but a throughput/latency concern once a tenant has long-running high-volume threads; worth a batched variant that stays on the same `_apply_receipt` semantics but commits once per event. | [sprint-4/32-omnichannel-channels-messenger-instagram](../plans/sprint-4/32-omnichannel-channels-messenger-instagram.md) | Low | Open |
| BL-SS-169 | **Visitor file / image uploads on web chat** (anonymous ingest: per-visitor quota, sniff gate, size cap, retention, abuse controls) - deliberately off in v1 (D-A7B-20). | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Medium | Open |
| BL-SS-170 | **Pre-chat as a form-engine form** (a curated subset of field types, published per channel) instead of the three fixed toggles (D-A7B-23). | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Low | Open |
| BL-SS-171 | **Wildcard-subdomain allowed origins** (`https://*.acme.com`) for web chat AND the plan-11H embed, with the `frame-ancestors` broadening reviewed as its own change (D-A7B-14). | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Medium | Open |
| BL-SS-172 | **Email the visitor a transcript** when the conversation closes (needs A7d email out and a verified address, D-A7B-31). | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Low | Open |
| BL-SS-173 | **Visitor-side typing indicator and agent presence** ("Sara is typing") over the existing realtime room. | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Low | Open |
| BL-SS-174 | **Multichannel launcher**: WhatsApp / Messenger / Telegram deep links inside the same bubble (respond.io's "growth widget"), pairing with G21 / C2. | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Medium | Open |
| BL-SS-175 | **Proactive messages**: open the widget automatically on a page rule or after N seconds, authored as an A5 workflow. | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Medium | Open |
| BL-SS-176 | **Anonymous-visitor retention job**: purge web chat contacts with no message and no host identity after N days, with a per-tenant setting (GDPR). | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Medium | Open |
| BL-SS-177 | **Widget localisation**: per-channel language strings for greeting, pre-chat labels and system copy. | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Medium | Open |
| BL-SS-178 | **Custom launcher icon upload per channel** (sniff-gated, served CSP-sandboxed, reusing the branding asset route). | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Low | Open |
| BL-SS-179 | **Visitor context capture** (page URL, referrer, UTM, user agent) written into A1 typed contact fields at session start, with an explicit per-channel opt-in. | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Medium | Open |
| BL-SS-180 | **An installable npm loader package and platform install guides** (WordPress, Shopify, Squarespace) mirroring respond.io's install matrix. | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Low | Open |
| BL-SS-181 | **Rate-limit the loader route at the edge** and add a per-channel session-creation ceiling, once real traffic exists. Amended review round 3 (2026-09-09): name the two routes that need it most explicitly - `GET /public/omnichannel/webchat/{widgetKey}/frame-policy` and the webchat CORS preflight (`OPTIONS` on any `/public/omnichannel/webchat/` path) are both unauthenticated AND, since review round 2 (B4) removed the route-level IP throttle (it was counting the Next.js middleware's own shared address, not the attacker), unthrottled. Both share `_origins_cache` (`webchat_visitor_service.py`), which only helps the REPEATED-key case (a real panel's repeat loads, or a prober retrying one key) - a distinct-key probe is a miss every time and reaches the three-query `resolve_live_channel` lookup unbounded, by design. Accepted as a security matter for now (three indexed queries, no information disclosed beyond what `POST /session`'s 200-vs-404 already reveals) but it is the honest home for this residual until an edge rate limit exists. | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Low | Open |
| BL-SS-182 | **AC-WEB-62 deferred - no verified respond.io website-chat `ChannelSource` value.** The vendor's official `@respond-io/typescript-sdk` (`src/types/contact.ts`, `dev` branch, fetched live 2026-09-08) exports the exhaustive `ChannelSource` union and it has NO website-chat / web-chat / livechat member at all (only `facebook`/`instagram`/`line`/`telegram`/`viber`/`twitter`/`wechat`/`custom_channel`/`gmail`/`other_email`/`twilio(_whatsapp)`/`message_bird(_whatsapp)`/`nexmo(_whatsapp)`/`whatsapp`/`whatsapp_cloud`) - the same list `SOURCE_TO_CHANNEL_TYPE` already documents. This lane has no respond.io API token and the one sandbox workspace checked in plan 24 had no channel connected, so a live `GET /space/channel` value could not be obtained either. Per D-A6-10's own rule, an unverified guess is worse than an honest skip: NO row was added, so a real respond.io website-chat source value (if the vendor's REST API differs from its published SDK type) continues to map to "Skip this channel" until re-verified against a live workspace or a newer SDK release. | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Medium | Open |
| BL-SS-183 | **CRITICAL - the web chat origin allowlist (D-A7B-11, AC-WEB-23/24) cannot discriminate between customer websites in a real browser.** `webchat_public.py` checks `request.headers.get("origin")` on the panel's OWN `fetch()` call - but that fetch executes from JS running INSIDE the panel iframe, so the browser sets `Origin` to the PANEL's own origin (e.g. `https://app.foundryx.my`) on EVERY embed, regardless of which customer website (`https://shop.acme.com`, `https://evil.example`, ...) hosts the `<iframe>`. Reproduced live in plan 34 S6's E2E (`34-evidence/S6/README.md`, screenshots 18-19): the channel's genuinely-ALLOWED origin `http://localhost:3013` gets the SAME uniform 404 as an intentionally off-list origin, both confirmed by `curl` (`Origin: http://localhost:3013` -> 200, `Origin: <panel's own origin>` -> 404) and a live HAR capture. The ONLY origin value that can ever satisfy this check is the panel's own deployed origin - which, once added to a channel's allowlist (as this slice's E2E had to do to exercise anything past session-start), lets **every** website embed that channel, defeating the allowlist's purpose entirely (R6/R7 in the plan's own risk list). `frame-ancestors` CSP on the panel document is unaffected and still restricts embedding correctly - but direct navigation to the panel URL (no iframe at all) bypasses `frame-ancestors` altogether and still succeeds once the panel's own origin is allowlisted, so today ANY internet user can reach a fully working, unauthenticated chat session for any channel whose widget key they know, with zero origin restriction. Needs a redesign of how the top-level embedding origin reaches the session-start check - e.g. the LOADER (which runs in the customer's own top-level page and therefore performs fetches with the correct `Origin`) mints the session itself and relays the token into the panel via `postMessage`, or the panel learns the true top origin from `event.origin` on an inbound loader handshake message and passes it as an explicit, separately-verified field. Out of scope for S6 (gateway/guide/workflow/wire-up/E2E) - flagged for the security reviewer as the top-priority finding of this slice; the S1/S2 pytest suite (33+ tests) never caught this because it sets the `Origin` header directly on the test client, which a real browser can never be told to do for a same-document fetch. **RESOLVED 2026-09-09 on `sprint-4/34-channel-web-chat` (`fix(omnichannel): plan 34 loader-minted web chat session`): the LOADER now mints the session from the customer's own top-level document (the only fetch that carries the embedding site's `Origin`, unforgeable from another site), keeps the visitor token in the HOST page's `localStorage` namespaced per widget key, and hands the whole session to the panel over `postMessage` with the exact panel origin as target; the panel never starts a session, never stores a token, and renders NOTHING without one (a directly navigated panel URL is a blank document). `POST /session` keeps the channel allowlist (now decidable) and the uniform 404; the Bearer-authed message routes echo the app's own origin only; the `/public/omnichannel/webchat/` prefix answers its own CORS preflight, since a customer origin lives on the CHANNEL allowlist and never in the service's `CORS_ORIGINS` env. Regression tests pin WHO may send which origin (a session start from the panel's own origin is the uniform 404) instead of setting `Origin` freely on the test client.** | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Critical | Resolved |
| BL-SS-184 (provisional) | **A visitor token cannot be renewed inside a long-lived tab.** Only the LOADER mints a session (BL-SS-183's fix, which is what made the origin check decidable), and the loader only runs on page load - so a tab left open past the token's 30-day expiry gets "Could not send your message" until the visitor reloads, with no prompt telling them to. Accepted as residual risk RR6 in plan 34 section 7 (review round 1, 2026-09-09): a renewal route callable from inside the panel would carry the PANEL's origin and be undecidable against the channel allowlist all over again, so the fix needs its own design (e.g. the loader re-minting on a timer and re-posting the session frame). | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Low | Open |
| BL-SS-185 (provisional) | **Unverified pre-chat `name` still lands on `contacts.first_name`/`last_name`.** Review round 1 (B3) moved pre-chat `email`/`phone` off the inbound stitch keys entirely, but `name` is not a stitch key (nothing resolves a contact by it), so it is still written write-if-empty onto `contacts.first_name`/`last_name` from an unauthenticated caller (`_apply_pre_chat`, `webchat_visitor_service.py`). Defensible today, but it means an anonymous visitor can still set the display name of a contact they created. Revisit if any future feature ever matches or searches by contact name. | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Low | Open |
| BL-SS-186 (provisional) | **Honeypot timing oracle on the web chat message route.** Review round 1 (S2) made a honeypot hit return the SAME shape, status and key set as a real send, closing the structural signal - but a trap hit does no DB work at all, so it returns measurably faster than a real send. Timing is a far weaker oracle than a distinguishable response, but it is the one signal left; note it rather than build a fake-delay countermeasure until it is shown to matter. | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Low | Open |
| BL-SS-187 (provisional) | **No in-product "promote this verified `visitorProfile` value" action for an agent.** The consumer guide's §12a tells an integrator to `PATCH /contacts` once THEY verify a visitor-declared email/phone themselves, but there is no button in the agent inbox that takes `ThreadItem.visitorProfile`'s unverified value and writes it onto the contact's real `email`/`phone` - the common case today is a manual retype. Add a one-click "use this value" action once agents ask for it. | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Low | Open |
| BL-SS-188 (provisional) | **Frame-policy cache staleness has no cache-bust on the widget-config write.** Review round 1 (S8) accepted up to 60s of staleness on `GET .../frame-policy` (an origin edit, a channel deactivation, or a module switch-off can take that long to reach the panel's `frame-ancestors` header); review round 2 bounded and split the cache's TTLs (S-new-1) but did not add an active invalidation hook. Worth an explicit `reset_origins_cache()`-style bust (or a targeted per-key delete) from the channel/widget-config save path once this surface carries real traffic and 60s of stale framing becomes an operational complaint rather than a theoretical one. | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Low | Open |
| BL-SS-189 (provisional) | **Host identity assertion has no expiry or nonce, and rotation is single-secret.** Carried over unchanged from round 1 (RR3, plan 34 section 7): a leaked `{userRef, hash}` pair impersonates that customer's user until the widget secret is rotated (which invalidates every assertion at once, D-A7B-6). Intercom and respond.io share this property, and the dual-secret gap is already documented in `verify_host_identity`'s docstring; adding expiry means adding a clock-skew contract to every integrator's server for a threat a rotation already answers. Revisit if a customer reports a leaked assertion in the wild. | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Low | Open |
| BL-SS-190 (provisional) | **`_origins_cache` (`webchat_visitor_service.py`) is a single unsegmented LRU pool, not a defence against a determined prober.** Review round 2 (S-new-1) bounded it (hard cap 5000, LRU-evicted) and split its TTL (60s positive / 15s negative); review round 3 accepted that as correct but noted a sustained distinct-key probe still evicts every legitimate POSITIVE entry through the same pool, degrading real panel loads back to three queries each until the probe stops - the pre-bounded-cache baseline, not a regression, but worth fixing properly. Consider separate positive/negative pools (or a small dedicated negative-only pool) so an enumeration probe can only ever evict other negative entries, never a real widget key's warm positive one. | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Low | Open |
| BL-SS-191 (provisional) | **Revisit the frame-policy cache's negative TTL (15s) once BL-SS-188's cache-bust lands.** The 15s/60s split (S-new-1) and the 60s `frame-ancestors` staleness bound (S8) are both still correct as documented, but they were sized assuming no active invalidation. Once BL-SS-188 ships a cache-bust from the channel/widget-config save path, the accepted staleness windows shrink to "until the next save" for the common case, and it is worth asking whether the negative TTL should drop further (or the positive TTL can safely rise) now that staleness is bounded by an event instead of only by a clock. | [sprint-4/34-omnichannel-channel-web-chat](../plans/sprint-4/34-omnichannel-channel-web-chat.md) | Low | Open |
| BL-SS-192 | **`UDF_ICB_*` cross-book linkage as a per-company preset option** - AED_SORENTO's inter-company (ICB) plugin UDFs resolve a PO/SPO line's source SO in a DIFFERENT AutoCount book (19 lines all-time, 2026-09-08 probe); today that is a documented per-company operator addition to the line query (`22-autocount-db-etl-autocount-sql.md` §4b), mapped by hand to the four `from_so_external_*` input fields, never baked into `PO_PRESET`/`SPO_PRESET` (D5) since a per-company UDF on one book breaks every other book sharing the preset. Worth a preset-level toggle once more than one company needs it. | [sprint-5/06-autocount-line-linkage](../plans/sprint-5/06-autocount-line-linkage.md) | Low | Open |
| BL-SS-193 | **SPO lines typed by hand carry no `FromDocDtlKey` (11,144 all-time)** - an AutoCount process rule, not something the feed can recover: an SPO line entered manually (not via the stock "Transfer from P/O") never gets `FromDocType`/`FromDocDtlKey` populated, so `from_po_line_ref`/`from_po_number` stay absent for it regardless of any ESB-side fix. Document the operator process (always transfer, never hand-type an SPO line) as the mitigation. | [sprint-5/06-autocount-line-linkage](../plans/sprint-5/06-autocount-line-linkage.md) | Low | Open |
| BL-SS-194 | **`update_tenant` runs the module backfills unfiltered across all tenants** - same shape as the 0016/0017/0018 precedent: `backfill_document_line_linkage` (and its siblings) scan every `ac_entity_config` row service-wide rather than filtering to the tenant being updated, so a single tenant's `update_tenant` call does redundant cross-tenant work every time (harmless today - each row's own tenant/company scoping still holds - but wasteful at scale). Add a `tenant_id` filter to the query these helpers run. | [sprint-5/06-autocount-line-linkage](../plans/sprint-5/06-autocount-line-linkage.md) | Low | Open |
| BL-SS-195 | **Link aggregates (count + key sum) cannot detect a same-key swap between two lines or a `FromSODocList`-only change** - `LinkedSOCount`/`FromSOKeySum`/`LinkedPOCount`/`FromPOKeySum` are order-insensitive sums, so two lines swapping which SO key they point at (net sum unchanged) or a `FromSODocList` value changing without moving the header-level counts/sums leaves the document's fingerprint bit-identical and it never re-stages. Consider a per-line `CHECKSUM_AGG`-style digest folded into the header aggregate, or a dedicated fingerprint query that hashes the line-linkage columns directly instead of summing them. | [sprint-5/06-autocount-line-linkage](../plans/sprint-5/06-autocount-line-linkage.md) | Low | Open |
| BL-SS-196 | **`ac_sim`'s `sales_order` task query is a hand-translated Postgres dialect variant, never byte-identical to the MSSQL SO preset text `backfill_sales_order_ref` compares against** - found live during the sprint-5/07 AC-07-12 replay on `foundryx_service_s36`: `ac_sim`'s connection is `dbType: postgresql`, so its stored query uses `LEFT JOIN LATERAL ... ON true` instead of `OUTER APPLY` (Postgres has no such syntax) and drops the `d.Qty IS NOT NULL` line-fingerprint filter - structurally different text the byte-identity backfill correctly classifies as "customised" (a DISABLED `Ref -> ref` row + one warning, same as the production `Sorento` task, never an auto-swap). Not a defect - `test_backfill_leaves_a_customised_query_alone_and_disables_the_ref_row_without_it` pins exactly this behaviour - but it means `ac_sim` needs the SAME one-line manual operator step (plan section 2.4) as a customised production task, not the "simulation companies get `ref` for free" story the plan's risk section assumed. Consider a dialect-aware preset-text comparison (mirroring how `list_mapping_presets` substitutes `{database}`) if `ac_sim` should auto-track future preset columns. | [sprint-5/07-autocount-so-ref-repush](../plans/sprint-5/07-autocount-so-ref-repush.md) | Low | Open |
| BL-SS-197 | **Re-push all clears `ac_row_hash` in the request transaction; the structural design is a `repush_requested_at` stamp consumed by the reconcile itself** - the request path now re-checks the in-flight guard after `clear_all` and before its commit (sprint-5/07 D9), which narrows but cannot close the window in which a run claimed concurrently reads an empty baseline mid-page. Stamping the config and letting the next reconcile run `clear_all` inside its own run transaction closes it structurally and also removes the "Added = N, Updated = 0" reporting artefact of a re-baselined reconcile. | [sprint-5/07-autocount-so-ref-repush](../plans/sprint-5/07-autocount-so-ref-repush.md) | Medium | Open |
| BL-SS-198 | **`CompanyService.refetch_entity` (API entities' "Refetch history") resets watermark state without the in-flight guard** - the closest sibling of Re-push all does not check `SyncJobRepository.first_unfinished` before rewinding, so a running vendor read can overwrite the rewound watermark on completion. Apply the same guard (and the same 409 shape) the repush route uses. | [sprint-5/07-autocount-so-ref-repush](../plans/sprint-5/07-autocount-so-ref-repush.md) | Low | Open |
| BL-SS-199 | **Runs tab cannot distinguish re-baselined adds from genuine adds** - after a Re-push all the following reconcile reports every document as `Added` (the baseline is empty) although Sorento answered `updated`; an operator reading the Runs list sees 149k "new" documents. Surface the re-baseline on the run row (a badge or a `Re-pushed` column) or fold the count into `Updated` when the run was triggered by a re-push. | [sprint-5/07-autocount-so-ref-repush](../plans/sprint-5/07-autocount-so-ref-repush.md) | Low | Open |
| BL-SS-200 | **No frontend guard catches a hand-composed destructive confirm outside a `ResourceAction` registry** - `confirm-carve-outs.inventory.test.ts` only regex-matches `confirm: {` inside `ResourceAction` arrays, so a component that composes `AlertDialog` + `AlertDialogAction` directly (how the sprint-5/07 round-1 typed-confirm shipped and passed the guard) is invisible to it. Extend the inventory to flag `AlertDialogAction` usage outside the named carve-out files and the sanctioned primitives, so the PRINCIPLES.md hard-fail is enforced by a test and not only by review. | [sprint-5/07-autocount-so-ref-repush](../plans/sprint-5/07-autocount-so-ref-repush.md) | Medium | Open |
