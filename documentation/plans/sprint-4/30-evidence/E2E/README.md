# Plan 30 - Formal [E2E] run (AC-RPT-53, AC-RPT-54)

**Run date:** 2026-09-06. **Session:** `agent-browser --session s30t`. **Stack:** frontend `:3008`
(this worktree, already running, cwd-verified), backend `:8009` restarted once mid-setup to add
`CORS_ORIGIN_REGEX='^http://[a-z0-9-]+\.localhost:3008$'` (tenant subdomains needed it; the
previously-running process lacked it) - same DB `foundryx_service_s30`, `CELERY_TASK_ALWAYS_EAGER=true`.

## Tenants created (operator UI, real clicks)

- **`p30-20260906074444`** ("P30 Dashboard Reports 20260906074444") - Admin
  `p30admin-20260906074444@example.com` / `P30Admin1234!`. Omnichannel installed via the operator's
  tenant-detail **Modules** tab (Actions -> Install).
- **`p30-noomni-20260906074444`** ("P30 NoOmni 20260906074444") - Admin
  `p30-noomni-admin-20260906074444@example.com` / `P30NoOmni1234!`. Omnichannel left NOT installed
  (used for the AC-RPT-54 "no module -> no menu" probe).

## Setup (recorded verbatim - API/DB calls with no UI path, or the invite-mail-link click)

1. **Agent user + role** (real clicks): created role "P30 Agent Role" (Permissions tab: Conversations
   = Reply+Assign+Run shortcut+Read; Reports & Analytics = **Read only**, NOT Export) and user
   "P30 Agent User" (`p30-agent-20260906074444@example.com`), assigned the role. No password field on
   invite - the invite email lands in the `email_outbox` (dev, no SMTP connection); read the
   `auth.invite` row's `html_body` via `psql` for the `token=` query param (a setup API/DB read, not a
   UI shortcut) and opened `/change-password?token=...` (the literal mail-link the user would click)
   to set `P30Agent1234!`.
   - **Role-editor Escape gotcha found and worked around (not a plan-30 defect, logged for the
     record):** the Permissions tab's per-resource `MultiSelect` treats **Escape** as *cancel the
     pending selection*, not just *close the popover* - the first attempt to grant "Omnichannel
     Workspaces: Read" (needed so the Agent's `useActiveWorkspace` call succeeds) showed the "Read"
     chip in the UI, but `Escape` before Save silently reverted it (confirmed via
     `role_permissions` - the row never landed). Redone by scrolling the popover closed instead of
     pressing Escape; the second Save persisted `workspaces.read` correctly (`psql` cross-check).
     This is a pre-existing RBAC role-editor UX quirk outside plan 30's scope - not filed as a plan-30
     defect, but worth a backlog note for the role-editor's own owner.
   - Final Agent permissions (`GET /auth/me`): `conversations.assign/read/reply/shortcut`,
     `reports.read`, `workspaces.read` - **holds `reports.read`, does NOT hold `reports.export`** -
     exactly the AC-RPT-39/47 scenario the brief asked for.
2. **Workspace members** (real clicks): Workspaces -> General -> Members -> added both P30 Admin and
   P30 Agent User.
3. **Channel** (real clicks): Channels -> Connect channel -> simulated Embedded Signup (Meta env
   unset in this worktree's `.env`, dev-safe sandbox path) -> "Foundryx Events Co." attached to the
   General workspace.
4. **3 contacts** (real clicks, Contacts -> Add contact): P30 Alice `+60111000951`, P30 Bob
   `+60111000144`, P30 Carol `+60111000350`.
5. **Inbound CONTACT messages** (dev-safe inbound route, setup API call - no UI path exists to inject
   a WhatsApp-side message): `POST /omnichannel/webhooks/{channelId}` with an unsigned Meta-shaped
   payload per contact (dev + no `META_APP_SECRET` = signature check open) so each thread has a real
   inbound message to measure first-response against. Verified via `psql` that all 3 messages landed
   (`sender_type=CONTACT`) before touching the Inbox.

## Activity generated by REAL clicks (Inbox)

- **P30 Alice**: Assign to me (P30 Admin) -> replied "Hi Alice, happy to help..." -> Close
  (reason **General Inquiry**) -> Reopen. (screenshots 03-07)
- **P30 Bob**: Assign to P30 Agent User -> replied "Yes, your booking is confirmed..." -> Close
  (reason **Sales Inquiry**). (screenshots 08-09)
- **P30 Carol**: Assign to me (P30 Admin) -> replied "The event starts at 7pm sharp." -> Unassign ->
  Snooze. (screenshots 10-11)

This yields real, non-flat numbers: tiles `Open 1 / Assigned 1 / Unassigned 1 / Snoozed 1`; the
Conversations report `Closed 2 / Reopened 1` (no synthetic `opened` events fire when a message
matches an ALREADY-EXISTING contact by phone digits - correct backend behaviour, not a bug; the
`opened` event only fires when the inbound pipeline creates a brand-new contact).

## Journey (real clicks from `/`, never a typed URL - the one exception logged below)

1. Sidebar -> Omnichannel -> **Dashboard** (`12-dashboard-last7-1280.png`): tiles match the fixture
   state exactly, Lifecycle `New Lead 3/100%` (all 3 contacts still New Lead), timezone label
   "Asia/Kuala_Lumpur", `Opened vs closed` chart renders a real green Closed bar on 6 Sept.
2. Changed the date-range preset to **Last 30 days** (`13-dashboard-last30-1280.png`) - range
   updates to `8 Aug - 6 Sept`, chart re-buckets (AC-RPT-53's required "chart re-buckets" proof).
   Scrolled down (`14-dashboard-lower-1280.png`): First response time (median 6m10s / p90 6m53s /
   avg 5m9s / 3 samples - real numbers off the reply timings), Resolution time correctly
   **"No data in this range."** (no `opened`/`reopened` cycle-start pairs in this fixture - the
   correct empty state per AC-RPT-45, not a bug), Top agents "P30 Admin - 2 closed, 6m10s median".
3. Repeated the dashboard at **375px** (`15-dashboard-375-top.png` - filter bar collapses to
   full-width stacked rows, tiles stack one per row; `16-dashboard-375-lifecycle-chart.png` -
   lifecycle tiles wrap to 2 columns, chart keeps a readable axis). No horizontal page scroll
   (`document.documentElement.scrollWidth === clientWidth === 375`, verified via `eval`).
4. Sidebar -> **Reports**, cycled the report `SearchSelect` through all seven keys at 1280px:
   - Conversations (`17-...png`) - Opened 0 / Closed 2 / Reopened 1, 3-series chart.
   - Responses (`18-...png`) - median/P90/average/sample tile + 7-bucket "By duration" table
     (2m-5m: 1/33.3%, 5m-10m: 2/66.7% - matches the 3 real reply timings).
   - Resolutions (`19-...png`) - correct empty state (no resolution pairs in this fixture).
   - Messages (`20-...png`) - Incoming 3 / Outgoing 3, two-series chart.
   - Users (`21-...png`) - embedded `ResourceList`: P30 Admin (assigned 2, closed 2, unique
     contacts 3, messages sent 3, median first response 6m10s), P30 Agent User (assigned 1, rest
     zero/dash - correct, since Admin sent every reply through the browser session regardless of
     who the thread was assigned to).
   - Leaderboard (`22-...png`) - #1 P30 Admin, #2 P30 Agent User, dense rank.
   - Assignment log (`23-...png`) - 4-row paginated log, newest-first, `source: Agent` on every row
     (all assigns/unassigns were performed by a logged-in user via the UI, never the API gateway or
     a workflow) - exact AC-RPT-26/27 shape.
5. Filtered by user **P30 Admin** on Assignment log (`24-...png`) - tiles/table stay at
   3 assigned / 1 unassigned (every event's actor OR `to_value` is Admin - correct per AC-RPT-09's
   actor-OR-to_value rule). Reset to All users before exporting.
6. **Exported Assignment log** (`25-export-assignments-1280.png`) - network log:
   `POST .../reports/assignments/export` 201 -> `GET .../export/{jobId}/file` 200 (eager-inline dev
   job finishes inside the hook's poll window). Downloaded `assignments-export (2).csv` - parsed with
   Python `csv`: header row = human labels (`Event ID, Date, Contact ID, Contact, Event, Previous
   assignee ID, Previous assignee, Assigned to ID, Assigned to, Source, Actor user ID, Actor`), 4 data
   rows (matches the on-screen count), every `Date` cell rendered `YYYY-MM-DD HH:MM:SS +08:00`
   (Asia/Kuala_Lumpur offset appended, not bare UTC), no BOM (not required by any AC - just noted).
   - **Formula-injection sanitization proof**: edited P30 Carol's first name to `=SUM(1+1)` (real
     click, Contacts -> Edit), re-exported the Assignment log - the CSV cell reads
     `'=SUM(1+1)` (leading single-quote neutralizer), confirming `sanitize_cell` runs on every cell
     including a name sourced from "inbound WhatsApp profile name"-shaped data. Reverted Carol's name
     back to "P30 Carol" afterwards (real click) to leave the tenant clean.
   - **Exported Leaderboard** too - `leaderboard-export (1).csv` parsed: header
     `Rank, User ID, Name, Team, Assigned, Closed, Unique contacts, Messages sent, Comments, Median
     first response (s), Median resolution (s)`, 2 rows, rank 1/2 preserved, `Team` blank (A8 not
     landed, per spec).
7. **Switch-report-keeps-filters** (AC-RPT-44): set the User filter to "P30 Admin" on Conversations,
   switched to Users - URL kept `userId=...&report=users`
   (`34-reports-switch-keeps-filters-1280.png`).
8. **Reload-restores-state** (AC-RPT-43): the ONE exception to "never navigate by URL" in this run -
   opened the exact URL from step 7 fresh (simulating an F5 reload / bookmark, which by definition
   requires a URL, not a click) - the report, date range, and user filter all restored
   (`35-reports-reload-restores-state-1280.png`).
9. Repeated the Reports journey at **375px**: Conversations (`26-...png` - filter bar full-width
   stacked, Export full-width button), Assignment log (`27-...png` - table scrolls inside its own
   container, page itself has no horizontal scroll per the same `eval` check).

## Permission-gated Agent journey (AC-RPT-39/47/50)

10. Logged out, logged in as **P30 Agent User** (holds `reports.read`, NOT `reports.export` -
    confirmed via `GET /auth/me`). Sidebar shows Dashboard + Reports (correct - the Agent DOES hold
    `reports.read`, so the menu-hide half of AC-RPT-54 doesn't apply to THIS role; the "role lacking
    `reports.read`" case is proven instead via the noomni-tenant Admin below, who never sees the
    section at all because the MODULE isn't installed). Reports page
    (`28-agent-reports-no-export-1280.png` before the workspace-permission fix,
    `29-agent-reports-with-data-no-export-1280.png` after) - **the Export button is absent** for the
    Agent on every report, both with and without data. Confirmed at 375px too
    (`30-agent-reports-375.png`).
11. **API probe** - direct call as the Agent: `GET .../dashboard` -> `200` (holds `reports.read`);
    `POST .../reports/leaderboard/export` -> **`403`** (lacks `reports.export`) - the API is the real
    gate, the UI hiding is only the courtesy layer.

## Isolation probes (AC-RPT-54)

12. **Cross-tenant 404 matrix** - the `default` tenant's Admin JWT (`demo@example.com`) against
    tenant A's `wsId`: `GET .../dashboard` `404`, `GET .../reports/meta` `404`, all seven
    `GET .../reports/{key}` `404`, `POST .../reports/leaderboard/export` `404`,
    `GET .../reports/leaderboard/export/{fakeJobId}/file` `404` - uniform 404 on every one of the ten
    routes, never a 403, never data.
13. **No-module tenant** - logged in as the `p30-noomni-...` Admin (holds core `reports.read` like
    every tenant Admin, but the omnichannel MODULE is not installed for this tenant):
    - Sidebar (`31-noomni-tenant-no-menu-1280.png`) - no "Omnichannel" section at all.
    - Desktop mega menu, "Apps" dropdown (`32-noomni-desktop-megamenu-1280.png`) - User Management /
      Developers sections only, no Omnichannel/Dashboard/Reports.
    - Mobile mega menu at 375px (`33-noomni-mobile-megamenu-375.png`) - same absence.

## Console / network

`agent-browser console` showed zero errors for the entire run - the only entries were two
pre-existing `Missing Description or aria-describedby for {DialogContent}` a11y warnings on the
Inbox close-conversation dialog, unrelated to plan 30 (same warning noted in the S0/S4 evidence
runs).

## Not independently re-verified in this pass (covered by pytest/vitest instead)

- The exact DST-spanning-bucket math (AC-RPT-12), the 120-bucket/100000-sample caps (AC-RPT-08/13),
  and the golden two-dialect SQL compile (AC-RPT-11) are numeric/internal properties proven by the
  backend suite (`test_omnichannel_reports_dashboard.py`), not something a browser click can assert
  differently from what pytest already pins byte-for-byte.


## Re-record after review round 2 (coder, 2026-09-06 21:15-21:23 local, session `agent-browser --session s30c`)

**Why:** the Opus re-read (N-3) found that screenshots 12-35 above were captured against a build
(`BUILD_ID` 17:31) that predated the round-1 frontend fixes (page edits 16:38-16:41; the README's
"already running" frontend). So every screenshot from 12 to 35 was re-recorded against a clean build
of the round-2 tree: `rm -rf .next && npm run build` in `s30/service_frontend` at 2026-09-06 21:09
(**`BUILD_ID UU2Fo2BsXkfYNqnCrOnZw`**), served by `npx next start -p 3008` (pid 16966, cwd-verified `s30`), backend
`:8009` restarted from the round-2 tree with the `foundryx_service_s30` `DATABASE_URL` override +
`CELERY_TASK_ALWAYS_EAGER=true` (pid 15471, cwd-verified `s30`). The old `:3008` (pid 54564) and
`:8009` (pid 53020) processes were killed only after `lsof -p <pid> | grep cwd` confirmed both were
this worktree's. Screenshots 00-11 (the Inbox activity that SEEDED the numbers) are unchanged - they
record data creation, not a rendered surface the fixes touched.

Same tenants and accounts as the original run; no new data was created, so every number below is
the same real fixture the original run produced.

| # | Capture | Proves |
|---|---|---|
| 12 | `12-dashboard-last7-1280.png` | Sidebar -> Omnichannel -> Dashboard (real click): tiles `Open 1 / Assigned 1 / Unassigned 1 / Snoozed 1`, Lifecycle `New Lead 3 / 100%` (the S-3 denominator = 3 total contacts, all staged) |
| 13 | `13-dashboard-last30-1280.png` | Preset -> Last 30 days; URL flips to `preset=last30&from=2026-08-08&to=2026-09-06`, chart re-buckets |
| 14 | `14-dashboard-lower-1280.png` | First response `6m10s / 6m53s / 5m9s / 3 samples` - **identical to the original run on the N-2 (tie-break) build**, so the dedupe changed nothing for real, non-tied data; Resolution "No data in this range."; Top agents P30 Admin 2 closed |
| 15, 16 | `15-dashboard-375-top.png`, `16-dashboard-375-lifecycle-chart.png` | 375px: filter bar stacks full-width, tiles one per row; `scrollWidth === clientWidth === 375` via `eval` |
| 17 | `17-reports-conversations-1280.png` | **N-1 proof**: sidebar -> Reports (real link click) renders the catalog straight away - Report `SearchSelect`, Export button, filter bar, "Conversations over time" - never "Couldn't load the reports." (`document.body.textContent.includes("Couldn't load")` was `false` on every reports capture in this run) |
| 18 | `18-reports-responses-1280.png` | Responses: tile + "By duration" breakdown |
| **18b** | `18b-reports-responses-groupby-user-url-1280.png` | **S-5 proof (new capture)**: Group by -> By agent; the URL gains `&groupBy=user` and the breakdown becomes the per-agent table (P30 Admin 3 samples / 6m10s, P30 Agent User 0). The Export request built from this state carries the same `groupBy` (pinned by `page.test.tsx`; the N-4 backend test pins the per-agent CSV header) |
| 19-23 | `19-...resolutions`, `20-...messages`, `21-...users`, `22-...leaderboard`, `23-...assignments` (1280) | Each report via the Report `SearchSelect`. Switching away from Responses dropped `groupBy=user` from the URL (the report-switch rule) |
| 24 | `24-reports-assignments-userfilter-1280.png` | User -> P30 Admin on Assignment log: URL carries `userId=...`; 3 assigned / 1 unassigned unchanged (actor-OR-to_value) |
| 25 | `25-export-assignments-1280.png` | Export clicked (User reset to All first). Backend log: `POST .../reports/assignments/export` **201** -> `GET .../export/{jobId}/file` **200** (eager-inline job) |
| 26, 27 | `26-reports-conversations-375.png`, `27-reports-assignments-375.png` | 375px reports; `scrollWidth === clientWidth === 375` on both |
| 34 | `34-reports-switch-keeps-filters-1280.png` | User=P30 Admin set on Conversations, switched to Users: URL keeps `userId=...&report=users` |
| 35 | `35-reports-reload-restores-state-1280.png` | The ONE sanctioned URL navigation (F5/bookmark): opened the step-34 URL fresh - Report `Users`, preset `Last 7 days`, User `P30 Admin` all restored |
| 28-30 | `28-agent-reports-no-export-1280.png`, `29-agent-reports-with-data-no-export-1280.png`, `30-agent-reports-375.png` | Logged out (user menu -> Logout), logged in as **P30 Agent User** (`reports.read`, not `.export`): Reports renders the catalog (N-1 holds for this role too), **no Export button** at 1280 (Conversations and Users) or 375 (`eval` over every `button` = `false`) |
| 31-33 | `31-noomni-tenant-no-menu-1280.png`, `32-noomni-desktop-megamenu-1280.png`, `33-noomni-mobile-megamenu-375.png` | Logged out, logged in as the **noomni** tenant Admin: no "Omnichannel" section in the sidebar (`eval` = `false`), the desktop Apps mega menu lists User Management only, the 375px Apps menu lists User Management + Developers only |

Harness notes for this re-record (not product defects):

- The plan-23 motion wrappers swallow a plain CDP `click` on sidebar links and on the header avatar /
  Apps trigger (the "Reports" link click returned OK but never navigated). Those were dispatched on
  the REAL element via `agent-browser eval` - `a.click()` for the Next `Link`, and a
  `pointerdown -> mousedown -> pointerup -> mouseup -> click` sequence for the Radix triggers - per
  the lane-harness notes. Every other control (all `SearchSelect`s, options, viewport) took normal
  `@ref` clicks. `find text` did not resolve `SearchSelect` options; `snapshot -i` refs did.
- `wait --url` with a query-string glob never matched here; `wait --load networkidle` + a short settle
  was used instead.
- After **Logout** the browser briefly landed on `chrome-error://chromewebdata/` before the next
  `open .../signin` succeeded (both tenants). Sign-in itself worked first time in every case, so this
  is the post-signout redirect target, not the login flow - noted for whoever owns the signout
  callback URL in a multi-port lane; outside plan 30.
